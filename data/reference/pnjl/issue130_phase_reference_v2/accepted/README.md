# accepted

This is a candidate downstream phase-map view generated from structured render tables. Native, unresolved and interpolated rows remain explicitly labelled; `candidate_pending_author_review` is not strict certification. The layer is opt-in and keeps `runtime_consumption=false` until a separate consumer audit and author promotion decision.
