# Issue #130 phase-reference v2 candidate

The public semantic layers are `strict`, `render` and `accepted`. The former v1 `derived` layer is retained only as an internal build input and provenance record. `render` is a complete structured display package; `accepted` is an opt-in downstream phase-map candidate. All layers are solver-free and keep `runtime_consumption=false` pending author review.
