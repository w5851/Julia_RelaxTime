# Historical legacy evidence

This directory contains lightweight, source-free audits of external or
historical implementations. It is not a runtime input directory.

Current extraction package:

- `legacy_extraction_v1/`: inventory and cross-check of the eight external
  trees previously staged under `D:\Desktop\legacy`.
  Its magnetic-source ledger is
  [`legacy_extraction_v1/magnetic_external_crosscheck_v1.md`](legacy_extraction_v1/magnetic_external_crosscheck_v1.md),
  and the Mott integral statistics are registered in the validation
  provenance layer rather than as acceptance targets.

Older reuse audits in this directory remain historical records. They explain
why early include-driven projects, unmanifested outputs, and experimental
benchmarks were not copied into the current `Models` and `src/relaxtime`
contracts.
