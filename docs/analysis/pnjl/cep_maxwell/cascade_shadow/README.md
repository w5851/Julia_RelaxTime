# Cascade And Shadow Evidence

This group contains two distinct shadow evidence packages:

- `cep_cascade_production_shadow_v1/`: the earlier production-cascade versus dense/oracle shadow, with a `hybrid_required` verdict.
- `cep_maxwell_endpoint_local_production_shadow_v4_full_31352083775/`: the earlier full endpoint-local shadow, with a `full_hybrid_candidate` verdict.

They are grouped because both are shadow-stage evidence, not because their runs or contracts are interchangeable. Each package keeps its own source/postprocess provenance, tables, figures, and promotion boundary.

The later endpoint-local contract and shadow packages remain under `../endpoint_local/`; the phase-reference freeze retains its generation-time paths and hashes.
