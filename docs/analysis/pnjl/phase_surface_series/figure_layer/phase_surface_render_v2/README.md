# Issue #130 phase-surface render v8

This package is the semantic v8 snapshot of the public figure before the v9 low-temperature continuation. The strict and derived tables are immutable inputs. The v8 image is retained as a pre-refinement display candidate and is not the current public alias; v9 replaced it at the stable public path.
