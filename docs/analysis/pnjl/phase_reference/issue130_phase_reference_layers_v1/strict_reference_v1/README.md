# strict_reference_v1

This layer preserves the v6/C2 native crossover and spinodal support, all prior Maxwell rows including unresolved geometry status, and appends the 276 real Maxwell expansion targets from run `32354095831`. It is an input candidate for author review, not a phase-reference promotion.
