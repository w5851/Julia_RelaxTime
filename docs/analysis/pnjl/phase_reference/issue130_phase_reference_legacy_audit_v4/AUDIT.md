# PNJL accepted-primary legacy retirement audit v4

当前 verdict：`legacy_physical_deletion_candidate`。运行时已固定为 accepted primary，strict 只能显式开启；legacy 不再是 fallback/rollback。物理删除仍受 path-retirement blocker 和独立 allowlist PR 约束。

本文件只解释合同和证据边界；完整表格见 `tables/`。
