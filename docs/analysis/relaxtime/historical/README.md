# Historical Relaxation-Time Evidence

This group contains preserved comparison and validation-review artifacts that are outside the current phase-guided transport mechanism line:

- `literature_comparison/`: Julia/Fortran and literature-target comparison figures.
- `meson_density/`: freeze-out meson-density plot-review evidence.
- `validation/`: historical Julia/Fortran swap-comparison evidence.

All three subgroups are historical or diagnostic-only. Their source result CSVs, target data, and numerical semantics remain unchanged; the move only clarifies the directory namespace and updates current references. The figure registry's cleanup and retirement records retain their original paths as historical execution snapshots.
