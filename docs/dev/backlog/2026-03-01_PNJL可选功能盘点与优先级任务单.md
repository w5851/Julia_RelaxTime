# PNJL 可选功能盘点与优先级任务单

更新日期：2026-05-03

当前状态：长期路线图（backlog 型文档），已从 `docs/dev/active/` 迁移到 `docs/dev/backlog/` 继续滚动维护；
本次迁移用于消除 active 治理告警，并明确该文档不属于当前执行批次任务单。

> 目的：基于 `2026-03-01_待加入功能.md` 的讨论条目，结合当前仓库代码现状，输出“已实现/未实现”盘点与可执行的优先级任务单。

---

## 1. 现状盘点（已实现 / 部分实现 / 未实现）

### 1.1 已实现（可视为基线能力）

- [x] 参数定义与初始化、模型工厂与多重派发主入口
  - 证据：`src/models/factory.jl`, `src/models/Models.jl`, `src/models/entrypoints.jl`
- [x] 热力学势与核心热力学计算（含 Polyakov 有效势、压强/密度/熵/能量）
  - 证据：`src/models/pnjl/core/Thermodynamics.jl`, `src/models/pnjl/core/Integrals.jl`
- [x] 自洽方程求解与多约束模式（FixedMu/FixedRho/FixedAsymmetricRho/FixedEntropy/FixedSigma）
  - 证据：`src/models/pnjl/solver/ConstraintModes.jl`, `src/models/pnjl/solver/ImplicitSolver.jl`
- [x] 相图相关基础能力（S-shape、Maxwell、crossover 扫描）
  - 证据：`src/models/pnjl/analysis/PhaseTransition.jl`, `scripts/pnjl/calculate_phase_structure.jl`
- [x] 扫描链路（T-μ / T-ρ / 双分支）与自适应 ρ 加密
  - 证据：`src/models/scans/TmuScan.jl`, `src/models/scans/TrhoScan.jl`, `src/models/scans/DualBranchScan.jl`, `scripts/pnjl/run_adaptive_trho_scan.jl`
- [x] 磁场扩展（Landau 能级、IMC、磁化热力学量）
  - 证据：`src/models/pnjl/core/MagneticIntegrals.jl`, `src/models/pnjl/core/MagneticThermodynamics.jl`
- [x] 导数与自动微分（ForwardDiff + ImplicitDifferentiation）
  - 证据：`src/models/pnjl/derivatives/ThermoDerivatives.jl`
- [x] 运输/弛豫时间相关模块（截面、传播子、输运系数）
  - 证据：`src/relaxtime/TransportCoefficients.jl`, `src/relaxtime/RelaxationTime.jl`
- [x] 回归测试、门禁脚本与 CI 工作流
  - 证据：`tests/unit/pnjl/**`, `tests/unit/relaxtime/**`, `.github/workflows/*.yml`

### 1.2 部分实现（有基础但未形成完整产品能力）

- [x] Polyakov 势“多参数化方案切换”
  - 现状：已落地统一可切换框架，支持 `log / poly / fukushima` 三种势参数化；通过配置 `polyakov.scheme` 切换。
- [~] 相图自动化产线
  - 现状：已有扫描、相变判据与生产管线雏形（含 `src/models/phase/ProductionPhasePipeline.jl`、`src/models/phase/CEPDetector.jl` 与 `scripts/pnjl/calculate_phase_structure.jl`），但“CEP 自动定位+报告化产物”仍需进一步产品化。
- [ ] 数据输出标准化
  - 现状：已支持文本/结果目录沉淀，但 HDF5/NetCDF 等标准科学格式未系统化。

### 1.3 未实现（讨论项中明确提及但仓库未落地）

- [ ] 有限体积效应（离散动量/边界条件）
- [ ] 显式同位旋化学势 `μI` 与 π 超流链路
- [ ] 显式矢量相互作用 `G_V` 模块化开关与扫描
- [ ] GPU/CUDA/OpenCL 加速
- [ ] MPI/OpenMP 级并行参数扫描框架
- [ ] 非平衡动力学（TDGL/Langevin/spinodal 动力学）
- [ ] 气泡成核率（bounce）计算
- [ ] 中子星/并合用 EoS table 产线
- [ ] ML 代理模型与参数反演
- [ ] GUI / Web 交互平台
- [ ] Docker/Binder 一键运行环境
- [ ] UrQMD/SMASH 等事件发生器接口

---

## 2. 带优先级可勾选任务单（建议执行序）

> 说明：优先级定义
> - **P0**：直接提升当前主线研究效率与可复现性
> - **P1**：明显增强模型物理覆盖范围
> - **P2**：中长期扩展（平台化/跨学科）

### 2.1 P0（近期建议）

- [x] **[P0] 建立 Polyakov 势参数化可切换框架**
  - 范围：抽象势函数接口，支持至少“当前对数势 + 1 种替代势”。
  - 验收：配置文件可切换；同一输入下可复现实验对比曲线。
  - 完成记录（2026-03-25）：
    - 实现：`src/models/pnjl_physics/PNJLCore.jl` 引入多重派发 tag（`LogPolyakovPotential` / `PolynomialPolyakovPotential` / `FukushimaPolyakovPotential`），统一入口 `polyakov_potential`。
    - 配置：`src/Constants_PNJL.jl` 与 `config/models/pnjl/default.toml`、`config/models/pnjl/unittest.toml`、`config/models/rpnjl/default.toml` 新增 `polyakov.scheme` 与扩展参数（`a3`、`fukushima_a_MeV`、`fukushima_b_MeV3`）。
    - 兼容：`src/models/rpnjl/RPNJLModel.jl` 改为复用统一基势后叠加 Vandermonde `kappa` 项。
    - 验证：`tests/unit/pnjl/test_pnjl_core.jl`、`tests/unit/models/test_rpnjl_model.jl`、`tests/unit/constants/test_constants_pnjl.jl` 本地通过。

- [x] **[P0] 相图产线标准化（扫描→判据→报告）**
  - 范围：固化脚本入口、参数模板、结果汇总模板（含 phase line/可能 CEP 标注）。
  - 验收：一条命令产出完整相图结果目录与摘要报告。
  - 阶段进展（2026-03-26）：
    - [x] 1.2-A 配置驱动入口首版：`scripts/pnjl/calculate_phase_structure.jl` 支持 `--config`，并引入 `run_manifest.json`。
    - [x] 1.2-B 默认模板自动加载：按 `model_kind` 自动读取 `config/models/<model>/phase_pipeline_default.toml`。
    - [x] 1.2-C 轻量复现预设：支持 `--preset=smoke`，用于快速复现实验路径。
    - [x] 1.2-D 可回放清单增强：manifest 新增 `preset` 与 `effective_config`。
    - [x] 1.2-E 快速 smoke：新增 `tests/integration/models/test_phase_cli_smoke.jl` 并接入 `tests/integration/runtests.jl` 的 smoke 集。
    - [x] 1.2-F 阶段验收收口：完成阶段记录、测试证据归档与后续剩余项识别。
    - [x] 1.2-G 报告结论模板化：`phase_report.md` 新增 `## Conclusion`，`phase_summary.json` 新增 `conclusion` 结构化字段。
  - 本轮验收命令与结果（2026-03-26）：
    - `julia --project=. -e 'include("tests/integration/models/test_phase_cli_smoke.jl")'`（通过）
    - `julia --project=. -e 'ENV["INTEGRATION_PROFILE"]="smoke"; include("tests/integration/runtests.jl")'`（通过）
    - `julia --project=. -e 'include("tests/unit/models/test_phase_pipeline.jl")'`（通过）
    - `julia --project=. -e 'include("tests/integration/models/test_phase_pipeline_smoke.jl")'`（通过）
  - 当前结论：该条目按既定验收标准已完成，后续增强转入新条目管理（如数据输出标准化、报告扩展）。

- [ ] **[P0] 数据输出标准化（CSV + HDF5 首版）**
  - 范围：在不破坏现有输出的前提下增加 HDF5 导出层。
  - 验收：同一批计算可同时输出 CSV 与 HDF5，字段一致性测试通过。

- [ ] **[P0] 依赖审计与架构文档自动更新守护**
  - 范围：修正依赖图脚本对文档示例 include 的误判；避免伪违规。
  - 验收：`dependency-audit` 在主干 push 上稳定通过，且无文档噪声误报。

### 2.2 P1（中期建议）

- [ ] **[P1] 有限体积效应首版**
  - 范围：引入体积参数与动量离散化策略，覆盖核心热力学量。
  - 验收：`V -> ∞` 极限可回归到当前连续体结果。

- [ ] **[P1] `μI` 与同位旋不对称扩展**
  - 范围：引入 `μu != μd` 的显式控制接口与扫描脚本。
  - 验收：新增同位旋扫描单测与参考图。

- [ ] **[P1] 矢量相互作用 `G_V` 开关化**
  - 范围：在模型参数层加入 `G_V`，打通求解与扫描。
  - 验收：`G_V=0` 与当前主线数值一致；`G_V>0` 结果可稳定收敛。

- [ ] **[P1] 并行扫描框架（先 Threads，后 MPI）**
  - 范围：扫描任务并行调度、失败重试、结果合并。
  - 验收：在固定网格上相对串行版本实现可重复加速。

- [ ] **[P1] 输运/涨落量统一报告层**
  - 范围：将 `relaxtime` 模块常用观测量统一出表与绘图模板。
  - 验收：一键导出固定格式报告（图 + 表 + 元数据）。

### 2.3 P2（长期建议）

- [ ] **[P2] GPU 加速 PoC（积分核）**
- [ ] **[P2] 非平衡动力学（TDGL/Langevin）原型**
- [ ] **[P2] 成核率（bounce）计算模块**
- [ ] **[P2] 中子星/并合 EoS table 管线**
- [ ] **[P2] ML 代理模型（求解器替代/参数反推）**
- [ ] **[P2] GUI / Web 交互界面**
- [ ] **[P2] Docker/Binder 交付**
- [ ] **[P2] 事件发生器（UrQMD/SMASH）对接接口**

---

## 3. 执行建议（最小可行路线）

- [x] 里程碑 M1（2~3 周）：完成 P0 前两项（势切换框架 + 相图产线标准化）。
- [ ] 里程碑 M2（3~4 周）：完成 P0 后两项（标准化输出 + 依赖审计守护）。
- [ ] 里程碑 M3（4~6 周）：启动 P1（有限体积 / `μI` / `G_V`）三选一做首个物理扩展。

---

## 4. 备注

- 本文档属于长期 backlog，不作为当前执行批次任务单。
- 当某个条目进入实际开发时，应拆成新的 `docs/dev/active/` 任务单承接。
- 本任务单为“能力建设导向”，不改变当前已通过回归的主线路径。
- 建议每完成一个任务即补充：
  - 对应测试（unit/smoke）
  - 文档（API + 使用示例）
  - 执行台账（命令、产物、结果）

## 5. 本次复核结论（2026-05-03）

- 已从 `docs/dev/active/` 迁移到 `docs/dev/backlog/`，避免长期路线图持续占用 active 配额。
- 状态更新：该文档保留为 backlog 路线图，不再作为 active 治理对象。
- 后续建议：当 P0/P1 条目形成明确实施任务单后，再拆分到独立 active 文档执行，避免本路线图过载。
