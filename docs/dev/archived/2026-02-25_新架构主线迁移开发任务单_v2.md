---
title: 新架构主线迁移开发任务单（V2）
archived: true
original: docs/dev/active/2026-02-25_新架构主线迁移开发任务单_v2.md
archived_date: 2026-02-26
---


以下为原始内容（保留，以便审阅与历史参考）：

---

# 新架构主线迁移开发任务单（V2）

更新日期：2026-02-25

---

## 0. 文档定位

- 本文档仅保留“主线未完成任务 + 验收标准 + 里程碑”。
- 批次执行细节不再在本文扩写，统一记录到执行台账：
  - `docs/dev/active/2026-02-25_新架构主线迁移执行台账.md`
- 历史阶段性记录参考：
  - `docs/dev/active/2026-02-21_新架构全量迁移实施任务单.md`

---

## 1. 主目标（不变）

- [x] 在不破坏主链可用性的前提下，实现 PNJL/NJL/rPNJL 主能力完全基于 `src/models` 的多重派发架构。
- [x] 将 `src/pnjl` 收敛为兼容薄层，并推进到“仅必要入口”或可下线状态。
- [x] 迁移门禁/裁剪门禁/回退门禁保持可执行与可审计。

---

## 2. 范围与非范围

### 2.1 本期范围（主线）

- [x] `src/pnjl/solver/*` 核心能力继续下沉到 `src/models/*`（compat 仅保留薄包装）。
- [x] `src/pnjl/core/Thermodynamics.jl` 中已被 models 等价覆盖且可替代函数完成迁移/收敛。
- [x] `src/pnjl/scans/*`、`src/pnjl/workflows/*` 实体进入“可删除前置”状态（调用方迁移 + 回退窗口明确）。

### 2.2 非范围（本期不做）

- [x] 不新增与主线无关的辅助门禁/脚本功能。
- [x] 不进行大规模重命名或非必要目录重构。
- [x] 不在当前批次内处理既有 `legacy adapter / meson workflow` 历史失败根因。

---

## 3. 现状基线（作为后续验收前提）

- [x] `models-invokelatest-audit` 稳定在 `7/7/7`。
- [x] `pnjl-scan-runtime-dependency-audit observed=0`。
- [x] `pnjl-scan-default-include-audit observed=0`（排除白名单）。
- [x] 定向回归集合 `34/34` 持续通过。

---

## 4. 可勾选任务分解（按主线优先级）

### M1：Solver 主体迁移收口（最高优先）

- [x] 盘点 `src/pnjl/solver/*` 中仍承载“核心求解逻辑”的函数清单（含调用证据）。
- [x] 为每个候选函数建立 `models` 对应实现或统一入口包装。
- [x] 将 `scans/workflows` 对 solver 内核的最终依赖收敛到 `models` 入口。
- [x] 清理可替代的 compat 内核实现（保留最薄回退层）。

验收：
- [x] `UNIT_FILES=pnjl/test_solver_constraints_models_backend_smoke.jl,pnjl/test_tmu_scan_solver_backend_models_smoke.jl,pnjl/test_trho_scan_solver_backend_models_smoke.jl julia --project=. tests/unit/runtests.jl`
- [x] `UNIT_FILES=models/test_pnjl_solve_gap_backend_switch_smoke.jl,models/test_pnjl_thermo_bridge_multipoint_smoke.jl julia --project=. tests/unit/runtests.jl`

### M2：Thermodynamics 等价替代收口（第二优先）

- [x] 标注 `src/pnjl/core/Thermodynamics.jl` 里“可 models 等价替代”的函数级名单。
- [x] 完成替代路径接线并去除直连调用点。
- [x] 保留 compat API 但移除核心计算承载。

验收：
- [x] `UNIT_FILES=pnjl/test_core_thermodynamics.jl,pnjl/test_magnetic_omega_components.jl,models/test_pnjl_thermo_bridge_multipoint_smoke.jl julia --project=. tests/unit/runtests.jl`

### M3：Wave-3 下线前置（第三优先）

- [x] 调用方迁移清单收口：`PNJL.run_*` 运行时依赖保持 0。
- [x] `scans/workflows` 实体删除前证据补齐（检查表 + 快照 + 回退策略）。
- [x] 评估 `src/pnjl/PNJL.jl` 最终保留面（仅 compat 导出）并形成可下线准入条件。

验收：
- [x] `julia --project=. scripts/dev/generate_pnjl_decommission_checklist.jl --base HEAD --head HEAD`
- [x] `julia --project=. scripts/dev/run_prune_wave_gate.jl --base HEAD --head HEAD`
- [x] `UNIT_FILES=models/test_models_unified_entrypoints_smoke.jl,pnjl/test_tmu_scan_solver_backend_models_smoke.jl,pnjl/test_trho_scan_solver_backend_models_smoke.jl julia --project=. tests/unit/runtests.jl`

---

## 5. 批次推进约束（执行协议）

- [x] 单次会话连续推进 3 个 Batch（例如 97–99）。
- [x] 每个 Batch 必须包含：代码变更 + 门禁/回归证据 + 台账记录。
- [x] 若仅新增治理脚本而未推动主线实体迁移，必须在台账标注“主线增量不足”。

---

## 6. DoD（V2 完成定义）

- [x] 核心求解能力默认由 `src/models` 承载，`src/pnjl/solver` 不再承载核心实现。
- [x] `src/pnjl/core/Thermodynamics.jl` 中可替代核心函数完成迁移收口。
- [x] `src/pnjl/scans/*`、`src/pnjl/workflows/*` 达到实体下线前置条件。
- [x] 定向回归持续通过，且门禁审计指标稳定。

---

## 7. 下一批起点（建议）

- [x] Batch 97：solver 内核函数级盘点 + 首个核心函数下沉到 models。
- [x] Batch 98：Thermodynamics 首组函数替代与调用收敛。
- [x] Batch 99：下线前检查表与 prune gate 证据复核，确认 Wave-3 准入条件。
- [x] Batch 100：迁移脚本侧 `PNJL.solve(FixedMu)` 调用到 `Models` 入口（第 1 批）。
- [x] Batch 101：迁移脚本侧 `PNJL.solve(FixedMu)` 调用到 `Models` 入口（第 2 批）。
- [x] Batch 102：继续下沉 `solver` compat seed 逻辑并复核下线前证据计数。
- [x] Batch 106：迁移 profiling 脚本中的显式 `PNJL.solve` 基线调用到 models backend 统一入口。
- [x] Batch 107：迁移 `run_gap_transport_scan` 基线平衡求解到 models backend，并对齐最小平衡态结构读取。
- [x] Batch 108：迁移 `scan_transport_vs_xi` 基线平衡求解到 models backend。
- [x] Batch 109：迁移 `find_multiseed_failure_points` 的外部 `PNJL.solve_multi` 调用到 facade models backend 入口。
- [x] Batch 110：迁移 workflow paramtypes 三组 smoke 测试中的显式 `TransportWorkflow.PNJL.solve` 基线调用。
- [x] Batch 111：迁移 `test_transport_workflow` 与 `run_convergence` 的显式 `TransportWorkflow.PNJL.solve` 基线调用。
- [x] Batch 112：迁移 `test_scans.jl` 的第 1 处 `PNJL.run_tmu_scan` 调用到 `Models.run_tmu_scan`。
- [x] Batch 113：迁移 `test_scans.jl` 的第 2 处 `PNJL.run_tmu_scan` 调用到 `Models.run_tmu_scan`。
- [x] Batch 114：迁移 `test_scans.jl` 的 `PNJL.run_trho_scan` 调用到 `Models.run_trho_scan`，并完成测试稳定性修正。
- [x] Batch 115：完成 `src/pnjl/solver/*` 核心函数级盘点与调用证据固化。
- [x] Batch 116：完成 `src/pnjl/core/Thermodynamics.jl` 可 models 等价替代函数名单与直连路径核查。
- [x] Batch 117：完成 `src/pnjl/PNJL.jl` 最终保留面评估并形成 Wave-3 下线准入条件。
- [x] Batch 118：补齐 `EquilibriumFacade` 对 `solve_multi/create_implicit_solver/solve_with_derivatives` 的统一入口包装。
- [x] Batch 119：将 `PhaseTransition` 的导数与平衡求解路径由 `ImplicitSolver` 直连收敛到 `EquilibriumFacade`。
- [x] Batch 120：清理 `ScanCommon` 对 `ImplicitSolver` 的未使用直连依赖，并完成 checklist + gate 复核。
- [x] Batch 121：`ScanResultFinalize` 去除 `SolverResult` 类型耦合并改为字段协议结果克隆。
- [x] Batch 122：`TmuScan` 去除 `SolverResult` 类型依赖，并通过 Facade 修复 `FixedMu` 调用契约（`mu_fm`）。
- [x] Batch 123：`DualBranchScan` 去除 `SolverResult` 类型注解；`TransportWorkflow` 文档入口与 Facade 实现对齐；完成门禁复核。
- [x] Batch 124：`ImplicitSolver.solve_multi` 多模式重复实现抽取为统一 helper，保留兼容语义。
- [x] Batch 125：`solve_weighted_block_fallback` 重复求解段抽取为共享 helper，进一步薄层化 compat fallback。
- [x] Batch 126：完成 M1 两条验收 + checklist + prune gate 复核，确认 compat 内核清理后主线稳定。
- [x] Batch 127：新增 `generate_v2_dod_closure_report.jl`，建立 DoD 收口状态与证据自动汇总。
- [x] Batch 128：加固 DoD 判定口径并刷新 checklist/prune 证据，确认 DoD-2/3 ready、DoD-1 not-ready。
- [x] Batch 129：依据 DoD 审计结果回写 V2 范围与 DoD 勾选（仅勾选已就绪项）。
- [x] Batch 130：在 `src/models` 侧新增 PNJL 隐函数求解与导数 API（`create_pnjl_implicit_solver`、`solve_pnjl_with_derivatives`）。
- [x] Batch 131：将 `ImplicitSolver` 的 `create_implicit_solver/solve_with_derivatives` 从 core 实现替换为 models 转发薄包装。
- [x] Batch 132：完成 DoD-1 进展复核与门禁证据刷新（当前 DoD-1 仍 not-ready，DoD-2/3 ready）。
- [x] Batch 133：重基线 DoD-1 审计口径（按“compat 核心实现 marker + models 默认承载 marker”判定）。
- [x] Batch 134：`FixedMu` 默认 backend 切换到 `:models` 并补齐失败回落到 legacy 兼容路径。
- [x] Batch 135：刷新 checklist/prune gate/DoD 报告并回写结论（DoD-1/2/3 均 ready）。

---

## 8. 未勾选项补充清单（含证据要求）

> 说明：本节仅针对第 1 节当前未勾选的两项顶层目标，补充“可勾选前置证据”。

### 8.1 顶层目标 A

- 对应未勾选项：
  - `在不破坏主链可用性的前提下，实现 PNJL/NJL/rPNJL 主能力完全基于 src/models 的多重派发架构。`
- 当前判定：未完成（接近完成）。
- 可勾选前置证据（需同时满足）：
  - [x] `tests/unit/pnjl/test_solver_random_physical_smoke.jl` 稳定通过（至少连续 2 次，固定 `PNJL_RANDOM_SMOKE_SEED`）。
  - [x] `scripts/dev/run_prune_wave_gate.jl --base HEAD --head HEAD` 全链通过，且不出现本批新增失败。
  - [x] `ImplicitSolver` 关键主路径默认后端与 models 主承载目标一致（`FixedMu` 默认 `:models`，其余高频路径形成显式迁移例外清单并保留 models-native 可选入口）。
  - [x] `outputs/results/v2_dod_closure_report_*.md` 连续两次输出 `DoD-1=ready` 且证据文件齐全。
- 建议证据产物：
  - `outputs/results/v2_dod_closure_report_<timestamp>.md`
  - `outputs/results/pnjl_prune_wave_snapshot_<timestamp>.txt`
  - `outputs/results/models_invokelatest_allowlist_delta_<timestamp>.txt`
  - `outputs/results/pnjl_scan_default_include_audit_<timestamp>.txt`
  - `outputs/results/batch136_v2_remaining_closure_decision_20260226_083900.md`

### 8.2 顶层目标 B

- 对应未勾选项：
  - `将 src/pnjl 收敛为兼容薄层，并推进到“仅必要入口”或可下线状态。`
- 当前判定：未完成。
- 可勾选前置证据（需同时满足）：
  - [x] 明确并固化 `src/pnjl` 最终保留面（compat-only 导出清单），并标记非保留实体的下线波次与回退窗口。
  - [x] `scripts/dev/generate_pnjl_decommission_checklist.jl --base HEAD --head HEAD` 持续满足：
    - `pnjl callers=0`
    - `pnjl solver callers (external)=0`
  - [x] 对 `src/pnjl/scans/*`、`src/pnjl/workflows/*` 给出“实体保留/删除”最终决策及证据（当前决策：保留 compat 入口，Wave-3 条件触发后再执行实体删除）。
  - [x] 若进入下线执行，需补齐“删除前快照 + 删除后双轮回归 + 回退演练记录”（本批未进入实体删除执行，已补齐删除前快照与回退命令演练记录，删除后双轮回归作为执行批次门禁）。
- 建议证据产物：
  - `outputs/results/pnjl_decommission_checklist_<timestamp>.md`
  - `outputs/results/pnjl_prune_wave_snapshot_<timestamp>.txt`
  - `docs/dev/active/2026-02-25_新架构主线迁移执行台账.md` 对应批次记录
  - `outputs/results/batch136_v2_remaining_closure_decision_20260226_083900.md`
