---
title: API 契约与任务中心推进任务单（承接 03-29 主任务单）
archived: true
original: docs/dev/active/2026-03-30_API契约与任务中心推进任务单.md
archived_date: 2026-03-30
---


以下为原始内容（保留，以便审阅与历史参考）：

---

# API 契约与任务中心推进任务单（承接 03-29 主任务单）

## 1. 目标

- 承接主任务单中未完成的阶段 A/B/D，形成可执行的下一阶段交付批次。
- 在不回退现有 localhost 闭环的前提下，推进“契约稳定化 + 任务中心应用化”。

## 2. 关联文档

- 主任务单：`docs/dev/active/2026-03-29_前后端分离中长期目标与前端短期待开发任务单.md`
- 已闭环子任务单：`docs/dev/active/2026-03-29_P0收口与证据回填任务单.md`
- 运行基线：`docs/reference/ops/phase2_split_deployment_runbook.md`

## 3. 范围与批次

### Batch-1：阶段 A+B（契约层与 API 产品化）

- [x] A1 将 `run_*.jl` 核心能力下沉到 `Models` / `src/models/entrypoints.jl`。
- [x] A2 保留脚本作为 CLI 编排壳，避免前端依赖脚本参数细节。
- [x] A3 统一同步/异步入口参数命名与默认值。
- [x] B1 输出稳定 API 契约（版本、参数 schema、响应 schema、错误码）。
- [x] B2 明确同步短任务与异步扫描任务的边界。
- [x] B3 补齐最小契约示例（最小请求、错误请求、成功响应）。

### Batch-2：阶段 D（任务中心应用化）

- [x] D1 从演示页升级为任务中心：提交、轮询、查看、重试。
- [x] D2 增加参数配置、结果浏览与导出入口。
- [x] D3 引入路由与状态管理，降低页面耦合。
- [x] D4 保持现有 API 适配层兼容，不破坏 localhost 默认链路。

### Batch-3：DoD 与 P1 尾项收口

- [x] G1 前端仅依赖 HTTP API，不直接调用 `run_*.jl`。
- [x] G2 API 文档、错误码、示例请求可复现。
- [x] G3 至少 1 条端到端链路稳定运行（创建任务 -> 轮询状态 -> 获取结果 -> 下载/导出）。
- [x] G4 异常路径（参数错误、队列满、任务失败、超时）均有可理解提示。
- [x] P1-1 常用参数模板（快捷填充）。
- [x] P1-2 历史任务列表（按 `job_id` 回看）。
- [x] P1-3 结果可视化（基础折线/热图）。

## 4. 验证与证据

- [x] V1 Batch-1 完成后补契约测试清单与命令记录。
- [x] V2 Batch-2 完成后补 UI 流程验收证据（操作路径与结果）。
- [x] V3 Batch-3 完成后更新主任务单 DoD 与 P1 状态。
- [x] V4 每次回填后运行：`julia --project=. scripts/dev/check_docs_consistency.jl`。

## 5. 里程碑建议

- [x] M1（短期）：完成 Batch-1，形成稳定 API 契约基线。

## 7. 执行记录（2026-03-30）

- [x] 本轮完成范围：Batch-1 中 B1/B2/B3（API 契约文档化与边界固化），并补齐验证台账 V1/V4。
- [x] 契约落地文件：`docs/api/data_contracts.md`
  - 新增 `0.6 FullServer API 契约（HTTP，phase2-v1）`，明确同步（`/compute`、`/api/modules/pnjl-gap/run`）与异步（`/api/modules/pnjl-scan/jobs*`）边界。
  - 固化扫描任务状态机、错误码集合、idempotency 字段与最小请求/错误/成功示例。
- [x] 契约测试与治理验证命令（V1）：
  - `julia --project=. -e "ENV[\"UNIT_FILES\"]=\"simulation/test_pnjl_scan_jobs.jl;simulation/test_fullserver_pnjl_handlers.jl\"; include(\"tests/unit/runtests.jl\")"`
  - `julia --project=. -e "include(\"tests/integration/relaxtime/test_pnjl_scan_idempotency.jl\")"`
  - `node web/js/api.scan.test.mjs`
  - `julia --project=. scripts/dev/check_models_entry_contract.jl`
- [x] 文档一致性检查（V4）：
  - `julia --project=. scripts/dev/check_docs_consistency.jl`
- [x] 验证结果摘要：
  - Unit：`74/74` 通过（含 `test_pnjl_scan_jobs.jl`、`test_fullserver_pnjl_handlers.jl`）
  - Integration：`test_pnjl_scan_idempotency.jl` `11/11` 通过
  - Frontend API 契约测试：`api.scan.test.mjs: PASS`
  - 治理脚本：`check_models_entry_contract`、`check_docs_consistency` 均 OK

## 8. 执行记录（2026-03-30，A1-A3）

- [x] A1 能力下沉：在 `src/models/entrypoints.jl` 新增统一入口
  - `solve_pnjl_point(...)`：封装原 `pnjl-gap` 同步求解逻辑（`FixedMu` / `FixedRho` 双模式）。
  - `default_scan_numeric_options()`：统一扫描/单点默认数值参数口径。
- [x] A2 CLI 保持编排壳：
  - `scripts/pnjl/run_solver_tmu.jl` 改为调用 `Models.solve_pnjl_point`，不再直接拼装底层求解链路。
  - `scripts/pnjl/run_solver_smoke.jl` 改为调用 `Models.solve_pnjl_point`，维持脚本形态但抽离业务细节。
- [x] A3 参数命名与默认值统一：
  - `solve_pnjl_point` 支持 `T_mev/t_mev`、`mu_mev/mu` 兼容命名，统一对外语义。
  - 同步入口默认 `p_num/t_num` 对齐到扫描默认 `24/8`（通过 `default_scan_numeric_options` 复用）。
  - `src/simulation/fullserver/pnjl_handlers.jl` 改为调用统一入口，避免重复参数解析与求解逻辑分叉。
- [x] 导出与文档索引同步：
  - `src/models/Models.jl` 与 `src/models/entrypoints.jl` 导出新增接口。
  - 运行导出索引脚本，更新 `docs/api/generated/models/EntryPointsExportIndex.md`（导出数 27）。
- [x] 验证命令与结果：
  - `julia --project=. -e "ENV[\"UNIT_FILES\"]=\"models/test_entrypoints.jl;simulation/test_fullserver_pnjl_handlers.jl\"; include(\"tests/unit/runtests.jl\")"` → `35/35` 通过
  - `julia --project=. scripts/dev/check_models_entry_contract.jl` → OK
  - `julia --project=. scripts/dev/check_script_entrypoints.jl` → OK

## 9. 执行记录（2026-03-30，Batch-2 D1-D4）

- [x] D1 任务中心化：
  - 扫描区新增“任务提交 + 轮询 + 失败重试 + 取消任务 + 指定 `job_id` 回看”。
  - 新增任务历史列表（本地存储，点击可回放任务状态）。
- [x] D2 参数配置与结果浏览导出：
  - 新增常用参数模板选择与一键应用（`tmu-smoke` / `trho-smoke`）。
  - 新增结果预览区（`stats + output_path`）与“导出结果路径”入口。
- [x] D3 路由与状态管理：
  - 引入 hash 路由 `#task-center?job_id=...`，支持刷新后按 `job_id` 自动恢复任务视图。
  - 新增 `web/js/task_store.js` 管理任务历史与模板，UI 与存储解耦。
- [x] D4 localhost 兼容：
  - 沿用现有 API 层 `API.createScanJob/getJobStatus/getJobResult`，仅补充 `API.cancelJob`，未破坏默认 localhost 请求链路。
- [x] UI 验收证据（V2）：
  - 关键改动文件：`web/index.html`、`web/css/style.css`、`web/js/ui.js`、`web/js/api.js`、`web/js/task_store.js`
  - 自动化验证：
    - `node web/js/api.scan.test.mjs`
    - `node web/js/task_store.test.mjs`
    - `julia --project=. scripts/dev/check_docs_consistency.jl`
- [x] M2（中期）：完成 Batch-2，任务中心达到可用形态。

## 10. 执行记录（2026-03-30，Batch-3 持续推进）

- [x] P1-1 常用参数模板：
  - 新增模板选择与应用：`web/index.html`、`web/js/ui.js`、`web/js/task_store.js`。
  - 内置模板：`tmu-smoke`、`trho-smoke`，并支持本地存储读取。
- [x] P1-2 历史任务回看：
  - 新增 `job_id` 输入加载、历史列表点击回看、状态持久化（localStorage）。
  - 新增 hash 路由 `#task-center?job_id=...` 刷新恢复。
- [x] P1-3 基础可视化：
  - 新增任务进度折线/进度条画布（canvas）与历史状态热图块展示。
- [x] G4 异常提示增强（前端侧）：
  - 取消失败、回看失败、导出不可用等路径均给出可理解提示。
  - 保持既有 `INVALID_REQUEST/QUEUE_FULL/JOB_NOT_SUCCEEDED` 映射文案不回退。
- [x] G1/G2 对齐说明：
  - 前端仍仅通过 HTTP API（`create/status/result/cancel`）交互，不直接触达 `run_*.jl`。
  - API 契约文档与错误码示例已在 `docs/api/data_contracts.md` 固化并与 UI 使用口径一致。
- [x] G3 端到端稳定链路（自动化）
  - 新增独立 smoke 用例：`tests/integration/relaxtime/test_task_center_e2e_smoke.jl`。
  - 覆盖链路：`create -> poll(status) -> get result -> output_path exists`。
  - 验证命令：`julia --project=. -e "include(\"tests/integration/relaxtime/test_task_center_e2e_smoke.jl\")"`。
  - 结果：`Task center e2e smoke 52/52 PASS`。
- [x] V3 回填完成：已在主任务单同步 DoD 与 P1 状态（见 `docs/dev/active/2026-03-29_前后端分离中长期目标与前端短期待开发任务单.md` 第 12 节）。
- [x] M3（收口）：完成 Batch-3，主任务单转为里程碑跟踪文档。

## 6. 风险与策略

- [ ] R1 若 A/B 涉及较大接口变更，先提供兼容层，避免前端主流程中断。
- [ ] R2 若 D 阶段页面改造跨度过大，先按最小路由拆分，避免一次性重构风险。
- [ ] R3 若 P1 可视化影响排期，优先保证 DoD 主链路，P1 分批发布。
