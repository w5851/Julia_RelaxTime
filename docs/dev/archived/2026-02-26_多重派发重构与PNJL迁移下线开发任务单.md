---
title: 多重派发重构与 PNJL 迁移下线开发任务单
archived: true
original: docs/dev/active/2026-02-26_多重派发重构与PNJL迁移下线开发任务单.md
archived_date: 2026-02-26
---


以下为原始内容（保留，以便审阅与历史参考）：

---

# 多重派发重构与 PNJL 迁移下线开发任务单

更新日期：2026-02-26

> 本任务单承接上一阶段 O1–O5 闭环结果，目标从“迁移可运行 + 门禁稳定”推进到“实体可下线 + 风险可持续治理”。
> 
> 上一阶段证据与执行细节见：
> - `docs/dev/active/2026-02-21_新架构全量迁移实施任务单.md`
> - `docs/dev/active/2026-02-25_新架构主线迁移执行台账.md`
>
> 本阶段文档分离约束：
> - 开发任务仅维护在本文件；
> - 执行记录仅维护在 `docs/dev/active/2026-02-26_多重派发重构与PNJL迁移下线执行台账.md`；
> - 追加执行记录时按台账模板“直接追加”，不要求回读历史上下文。

---

## 1. 背景与核心目标

- [ ] 核心目标 A：保持 PNJL/NJL/rPNJL 主能力持续由 `src/models` 多重派发路径承载。
- [ ] 核心目标 B：将 `src/pnjl` 收敛到最小兼容面，并按条件完成实体移除。
- [ ] 核心目标 C：在实体移除过程中保持“门禁稳定 + 回归稳定 + 回退可用”。

---

## 2. 范围与非范围

### 2.1 本阶段范围（In Scope）

- [ ] `src/pnjl/scans/*` 与 `src/pnjl/workflows/*` 的实体下线执行准备与触发执行。
- [ ] `src/pnjl/PNJL.jl` 的最终定位决策（保留最薄 compat 或完成实体移除）。
- [ ] 删除前后双轮回归、回退演练与证据归档。
- [ ] 风险项常态化监控（baseline 偏移、隐式 legacy 依赖、外部调用误删风险）。

### 2.2 非范围（Out of Scope）

- [ ] 不新增新的物理模型功能或参数体系。
- [ ] 不扩展与主线迁移无关的 UI/服务端特性。
- [ ] 不重写已稳定通过的 models 核心算法实现。

---

## 3. 现状盘点（起点）

- [x] O1–O5 证据链已跑通，`run_prune_wave_gate` 在闭环口径下通过。
- [x] 当前门禁稳定口径：`models-invokelatest-audit=7/7/7`、`pnjl-scan-runtime-dependency-audit=0`、`pnjl-scan-default-include-audit=0`。
- [x] 回退命令已完成预演与正式演练：`git restore --worktree --staged --source=HEAD -- src/pnjl`。
- [ ] 实体删除尚未真正执行：`src/pnjl/scans/*`、`src/pnjl/workflows/*`、`src/pnjl/PNJL.jl` 仍在仓库中。

---

## 4. 执行批次（可勾选）

### Batch N1：删除前资格确认（实体下线准入）

- [x] 固化“可删除资格快照”：
  - `pnjl callers=0`
  - `pnjl solver callers (external)=0`
  - default include 审计 = 0
- [x] 生成并归档删除前清单与快照。
- [x] 产物路径写入执行台账。

验收命令：
- `julia --project=. scripts/dev/generate_pnjl_decommission_checklist.jl --base HEAD --head HEAD`
- `julia --project=. scripts/dev/run_prune_wave_gate.jl --base HEAD --head HEAD`

### Batch N2：scans/workflows 实体下线执行

- [x] 阻塞已识别：`src/models/entrypoints.jl` 与 tests 仍直接依赖 `src/pnjl/workflows/*`，需先完成入口迁移后再执行实体删除。
- [x] 第一批阻塞拆解完成：`tests/unit/relaxtime/*.jl` 已迁移为通过 `Main.Models` 入口加载 workflow/adapters，不再直接 include `src/pnjl/workflows/*`。
- [x] `src/models/entrypoints.jl` 已新增 workflow 模块获取器（transport/meson/adapters），用于承接测试与调用方入口迁移。
- [x] 第二批阻塞拆解完成：`src/models/entrypoints.jl` 对 `src/pnjl/workflows/*` 的路径依赖已迁移到 `src/models/workflows/*Bridge.jl` 本地桥接层。
- [x] 本轮验证通过：`run_prune_wave_gate` 与 `UNIT_PROFILE=smoke` 均通过，维持 `pnjl-scan-default-include-audit observed=0`。
- [x] 实体删除第一刀完成：`src/pnjl/workflows/WorkflowParamAdapters.jl` 已删除，功能迁移至 `src/models/workflows/WorkflowParamAdapters.jl`。
- [x] workflow 侧引用已切换：`TransportWorkflow.jl`、`MesonMassWorkflow.jl` 通过 models 路径加载 adapters。
- [x] 实体删除第二刀完成：`src/pnjl/workflows/MesonMassWorkflow.jl` 已删除，功能迁移至 `src/models/workflows/MesonMassWorkflow.jl`。
- [x] Meson bridge 已切换到 models 实体：`src/models/workflows/MesonMassWorkflowBridge.jl` 不再回指 legacy 路径。
- [x] 实体删除第三刀完成：`src/pnjl/workflows/TransportWorkflow.jl` 已删除，功能迁移至 `src/models/workflows/TransportWorkflow.jl`。
- [x] tests/scripts 调用方 include 已切换至 models 路径，不再直连 legacy `TransportWorkflow`。
- [x] scans 实体删除完成：`src/pnjl/scans/*` 已迁移至 `src/models/scans/*` 并删除 legacy 目录实体。
- [x] `src/pnjl/PNJL.jl` scans 懒加载路径已切换到 `src/models/scans/*`，保持 compat 入口语义不变。
- [x] 在保留兼容入口语义前提下，执行 `src/pnjl/scans/*` 与 `src/pnjl/workflows/*` 实体删除或迁出。
- [x] 删除后修复所有编译/加载/命名空间回归问题。
- [x] 完成删除后首轮回归（workflow 路径）。

验收命令：
- `julia --project=. scripts/dev/run_prune_wave_gate.jl --base HEAD --head HEAD`
- `UNIT_PROFILE=smoke julia --project=. tests/unit/runtests.jl`

### Batch N3：PNJL.jl 最终收敛（保留或移除）

- [x] N3 第一刀完成：scripts/tests 中 `PNJL.TmuScan/PNJL.TrhoScan` 子模块直连已收敛至 `PNJL.run_tmu_scan/run_trho_scan` 顶层入口。
- [x] N3 现状核验：decommission checklist 显示 `pnjl callers=0`、`pnjl solver callers (external)=0`，兼容入口调用面已清洁。
- [x] N3 决策已切换为选项 B：执行 `src/pnjl/PNJL.jl` 实体移除路径，由 models 入口完全承接。
- [x] N3 第二刀完成：迁移门禁新增 `PNJL.TmuScan/PNJL.TrhoScan` 子模块直连拦截，防止调用面回流。
- [x] N3 第三刀完成：tests/perf 场景已移除 `include src/pnjl/PNJL.jl` 直连；`src/models/derivative_entrypoints.jl` 提供导数入口承接；迁移门禁新增 `src/pnjl/PNJL.jl` 直连 include 拦截规则。
- [x] N3 第四刀完成：`scripts/pnjl/run_tmu_scan.jl` 与 `tests/perf/pnjl/test_bulk_derivative_coeffs_performance.jl` 已切换到 `src/models/Models.jl` 入口，进一步缩减 `PNJL.jl` 直连面。
- [x] N3 第五刀完成：`Models` 新增 `build_default_rho_grid` 统一入口并迁移 6 个 scripts/tests 扫描调用到 `src/models/Models.jl`，`scripts/tests` 中 `include src/pnjl/PNJL.jl` 命中由 54 降至 48（单轮下降 11.1%）。
- [x] N3 第六刀完成：在 `src/models` 暴露 `legacy_pnjl_module` 统一引导加载，并将 `scripts/tests` 其余 48 处 `include src/pnjl/PNJL.jl` 全量迁移到 `include src/models/Models.jl + Models.legacy_pnjl_module()`；命中数由 48 降至 0（单轮下降 100%）。
- [x] N3 第七刀完成：全仓 `using .PNJL*` 残留完成迁移（tests + scripts）；统一改为 `Models.legacy_pnjl_module()` + 显式符号绑定，`using .PNJL` 命中由 65 降至 0（单轮下降 100%）。
- [x] N3 第八刀完成：全仓 `using Main.PNJL*` 残留完成迁移（src + tests）；统一改为显式模块绑定（`PNJL = ...` + `getproperty`），`using Main.PNJL` 命中由 7 降至 0（单轮下降 100%）。
- [x] 基于外部依赖与兼容窗口，确定 `src/pnjl/PNJL.jl` 最终状态：
  - 选项 A：保留最小 compat re-export；
  - 选项 B：实体移除并由 models 入口完全承接（当前采用）。
- [x] 执行对应变更并验证对外入口稳定。

验收命令：
- `julia --project=. scripts/dev/generate_pnjl_decommission_checklist.jl --base HEAD --head HEAD`
- `julia --project=. scripts/dev/run_prune_wave_gate.jl --base HEAD --head HEAD`
- `UNIT_PROFILE=smoke julia --project=. tests/unit/runtests.jl`

### Batch N4：回退窗口关闭与归档收口

- [x] 完成“删除后第二轮回归 + 回退演练”。
- [x] 将本任务单与执行台账映射到归档产物，形成可审计闭环。
- [x] 触发文档归档（按 `docs/dev/README.md` 规范）。

验收命令：
- `julia --project=. scripts/dev/run_prune_wave_gate.jl --base HEAD --head HEAD`
- `UNIT_PROFILE=smoke julia --project=. tests/unit/runtests.jl`
- `git restore --worktree --staged --source=HEAD -- src/pnjl`

---

## 5. 测试与验收标准

### 5.1 必须通过

- [x] `scripts/dev/check_pnjl_migration_guard.jl --base HEAD --head HEAD` 通过（由 `run_prune_wave_gate` 链路内校验）。
- [x] `scripts/dev/run_prune_wave_gate.jl --base HEAD --head HEAD` 通过。
- [x] `UNIT_PROFILE=smoke julia --project=. tests/unit/runtests.jl` 通过。

### 5.2 必须稳定

- [x] `models-invokelatest-audit` 无漂移（维持 7/7/7，或有受控变更证据）。
- [x] `pnjl-scan-default-include-audit observed=0`。
- [x] `pnjl-scan-runtime-dependency-audit observed=0`。

### 5.3 必须可回退

- [x] 回退命令执行成功，且目标范围 clean。
- [x] 回退窗口内至少一次成功演练有产物留痕。

---

## 6. 里程碑

- [x] M1（准入）：删除前资格确认完成（N1）。
- [x] M2（执行）：scans/workflows 实体下线完成（N2）。
- [x] M3（定版）：PNJL.jl 最终收敛决策落地（N3）。
- [x] M4（收口）：回退窗口关闭 + 文档归档完成（N4）。

---

## 7. DoD（完成定义）

- [x] 主能力稳定由 `src/models` 多重派发承载，且门禁连续稳定。
- [x] `src/pnjl` 达到预期最终形态（最小 compat 或实体移除），并有证据说明。
- [x] 删除前后回归与回退演练证据齐备。
- [x] active 文档完成归档迁移，archived 元信息完整可追溯。

---

## 8. 风险与回退策略

- [ ] 风险 R1：默认后端切换引发 baseline 漂移。
  - 回退：冻结默认口径，先走显式开关并补基线。
- [ ] 风险 R2：workflow/scan 仍有隐式 legacy 依赖。
  - 回退：保留 compat 薄层入口，逐条迁移并回归。
- [ ] 风险 R3：实体删除误伤外部调用。
  - 回退：删除前后快照对比 + `git restore`/`git revert` 快速恢复。

---

## 9. 执行记录入口（分离保存）

- [x] 本任务单不再直接维护执行过程记录。
- [x] 执行记录统一追加到：`docs/dev/active/2026-02-26_多重派发重构与PNJL迁移下线执行台账.md`。
