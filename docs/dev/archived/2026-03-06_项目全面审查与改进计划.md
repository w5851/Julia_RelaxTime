---
title: 项目全面审查与改进计划
archived: true
original: docs/dev/active/2026-03-06_项目全面审查与改进计划.md
archived_date: 2026-03-09
---


以下为原始内容（保留，以便审阅与历史参考）：

---

# 项目全面审查与改进计划

更新日期：2026-03-09

> 目的：基于对整个 Julia_RelaxTime 项目的全面审查，系统梳理当前架构迁移完成度、遗留问题、测试覆盖缺口，并给出可执行的分阶段改进计划。

## 当前 active 范围（2026-03-09）

本文件已从“全量审查总表”收窄为“当前仍需推进的收尾任务 + 明确延后 backlog”。

当前这份文档在本轮执行后只剩一个待判断事项：

- [ ] **评估是否还需要继续做 CI workflow 收敛**
  - 该项不是迁移主线 blocker。
  - 若本轮 rPNJL regression 与公式口径验证通过，则本文件可以直接归档，不必继续停留在 active。

以下内容不再视为当前 active：

- 阶段 1 的 legacy 清理与入口收口：已完成。
- 阶段 2 的 `transport_provider` 泛化、RPNJL 深化：明确延后，视研究需要再启。
- 阶段 3 的外部参考验证：等待外部数据或新验证任务单，不作为当前 active 执行项。

收口判断：0.4 已完成，2.3 的公式一致性检查也已完成；若本轮验证通过，则本文件不再需要继续停留在 active，其余未勾选项均应视为 backlog，而不是本轮主线待办。

## 当前进度（2026-03-06）

- 已开始阶段 1 清理：完成 1.1 legacy adapter 下线，删除 `src/models/legacy/LegacyNJLModel.jl` 与 `LegacyPNJLModel.jl`，移除 `factory.jl` 中 `:LegacyNJL` / `:LegacyPNJL` 注册及 `Models.jl` 对应 export，并删除 4 个纯 legacy adapter 测试文件。
- 已完成 2.3 的公式一致性核对：`src/models/rpnjl/RPNJLModel.jl` 中八夸克质量修正、手征势和 Vandermonde 形式与 `docs/reference/formula/models/rpnjl/rPNJL_core.md` 的式 (3.27)–(3.31) 一致；本轮同时发现并修复了 `config/models/rpnjl/default.toml` 中 `kappa` 漂移为 `0.0` 的问题，现已恢复为文档口径 `0.1`。
- 已将 `tests/integration/relaxtime/test_transport_legacy_models_bridge_smoke.jl` 迁移为 `test_transport_solver_backend_bridge_smoke.jl`，保留 `solver_backend=:legacy/:models` 桥接校验，但不再依赖已删除的 legacy adapter 类型。
- 已同步调整一处现役集成测试：`tests/integration/pnjl/test_bulk_viscosity.jl` 由 `LegacyPNJL` vs `PNJL` 比较改为默认 PNJL 路径 vs 显式 `Models.create_model(:PNJL)` 比较。
- 已完成阶段 0 的第一批基础设施落地：新增 `tests/regression/` 统一入口，并将现有 PNJL scan/constraint/magnetic 与 RelaxTime transport/total-cross-section 回归门禁收敛到 `@testset`。
- 已完成 0.2 的首个新增基线：新增 `scripts/dev/export_pnjl_gap_fixedpoint_baseline.jl`、`tests/baselines/pnjl/baseline_pnjl_gap_fixedpoints_v1.csv` 与 `tests/regression/pnjl/test_pnjl_gap_fixedpoint_regression.jl`，覆盖 12 个 PNJL `(T, μ)` 固定点。
- 已完成 0.2 的另外两项：新增 `scripts/dev/export_pnjl_thermo_derivatives_baseline.jl` / `tests/regression/pnjl/test_pnjl_thermo_derivatives_regression.jl` 与 `scripts/dev/export_pnjl_implicit_diff_baseline.jl` / `tests/regression/pnjl/test_pnjl_implicit_diff_regression.jl`，分别覆盖 5 个 thermodynamics fixed points 与 3 个 implicit-diff fixed points。
- 已完成 0.3：新增 `scripts/dev/export_njl_gap_fixedpoint_baseline.jl`、`tests/baselines/njl/baseline_njl_gap_fixedpoints_v1.csv`、`tests/baselines/njl/baseline_njl2_gap_fixedpoints_v1.csv` 以及 `tests/regression/njl/` 下的 NJL/NJL2 gap fixedpoint regression，分别覆盖 5 个 `(T, μ)` 固定点。
- 已完成 0.4：新增 `scripts/dev/export_rpnjl_gap_fixedpoint_baseline.jl`、`tests/baselines/rpnjl/baseline_rpnjl_gap_fixedpoints_v1.csv` 与 `tests/regression/rpnjl/test_rpnjl_gap_fixedpoint_regression.jl`，覆盖 5 个 rPNJL `(T, μ)` 固定点，并接入 `tests/regression/runtests.jl`。
- 已完成 0.6：新增 `scripts/dev/export_phase_pipeline_regression_baseline.jl`、`tests/baselines/phase/baseline_phase_pipeline_v1.csv` 与 `tests/regression/phase/test_phase_pipeline_regression.jl`，覆盖 CEP 位置、2 条一阶边界与 8 个 crossover 采样点。
- 已完成 0.7 的第一项：将 `tests/baselines/relaxtime/baseline_transport_fixedpoints_v1.csv` 从 5 点扩展到 13 点，覆盖 `xi=0`、`xi=0.2` 与 `xi=-0.2` 情形，并让 transport regression 直接以 baseline 行集驱动。
- 已完成 0.7 的第二项：新增 `scripts/dev/export_meson_mass_regression_baseline.jl`、`tests/baselines/relaxtime/baseline_meson_mass_fixedpoints_v1.csv` 与 `tests/regression/relaxtime/test_meson_mass_regression.jl`，覆盖 3 个 meson mass workflow 固定点。
- 已将 `.github/workflows/ci-validation.yml` 与 `.github/workflows/nightly-full-regression.yml` 改为使用 `tests/regression/runtests.jl`，其中 nightly 通过环境变量切换 magnetic nightly baseline。
- 已更新测试分层规范：内部 baseline 回归归入 `tests/regression/`，`tests/validation/` 预留给未来 Fortran / Mathematica / 文献对照。
- 已完成阶段 1.2 收尾：`derivatives/`、`workflows/`、`scans/` 与 `pnjl_physics/core/EquilibriumFacade.jl` 已全部改为直接调用 Models 接口，`src/models/pnjl_physics/core/ThermoFacade.jl` 已删除，`Models.jl` 中 `Thermodynamics` alias 也已下线。
- 已完成阶段 1.4：`src/models/pnjl_physics/PNJLModel.jl` 已移除 `solver_backend` / `fallback_legacy_on_failure` 废弃关键字；`WorkflowParamAdapters.jl` 现仅接受 `QuarkParams` / `ThermoParams`；`transport_provider(thermo_backend::Symbol)` 重载已删除，调用方统一改为 `transport_provider(model)`。
- 已完成阶段 1.5：`src/models/Models.jl` 已移除 `@eval Main` 注入与无模型兼容函数（`calculate_U*`、无模型 `calculate_mass_vec` / `calculate_chiral`、旧 `calculate_thermo` / `calculate_omega` / `calculate_rho` / `calculate_number_densities` 包装）；工作流/验证/脚本调用方均改为通过显式 `Models` 入口访问模块与多重派发接口。
- 已完成阶段 2.2：全代码库已移除 `IncludeOnce` / `include_once!` 依赖；`workflows/`、`scans/`、`pnjl_physics/`、`derivatives/`、`utils/` 等调用方改为显式模块守卫式 `Base.include`；`src/utils/IncludeOnce.jl` 已删除。
- 已明确阶段 2.1 暂缓：当前研究范围内输运系数分支仅要求支持 `PNJLModel`，`transport_provider` 泛化不作为本轮优先事项。
- 已同步清理桥接专用测试：删除 `tests/unit/pnjl/test_thermo_facade.jl` 与 `tests/integration/models/test_pnjl_thermo_bridge_multipoint_smoke.jl`，其余受影响测试改为直接验证 Models / ThermoDerivatives 主路径。
- 本轮验证结果：此前阶段 1.4 的定向验证保持通过；阶段 1.5 新增验证 `tests/unit/runtests.jl`（582/582）、`tests/integration/runtests.jl`（235/235）、`tests/validation/runtests.jl`（66/66）与 `REGRESSION_PROFILE=full REGRESSION_MAGNETIC_SCOPE=nightly julia --project=. tests/regression/runtests.jl`（803/803）均已本地通过。阶段 2.2 收尾后再次验证 `tests/unit/runtests.jl`（582/582）、`tests/integration/runtests.jl`（235/235）、`tests/validation/runtests.jl`（82/82）与 `REGRESSION_PROFILE=full REGRESSION_MAGNETIC_SCOPE=nightly julia --project=. tests/regression/runtests.jl`（803/803）也已本地通过。

---

## 一、整体审查结论

### 1.1 项目现状概述

项目已完成从旧架构到基于多重派发的新架构 (`src/models/`) 的**主体迁移**，核心物理模型（NJL/NJL2/PNJL/rPNJL/PNJLMagnetic）均已在新架构下实现，旧架构目录 `src/legacy_pnjl/` 和 `src/models/pnjl/`（旧版）已物理删除。但迁移尚存"最后一公里"问题——多个兼容层、Facade 桥接、deprecated 参数仍活跃于代码中。

### 1.2 架构迁移完成度评估（按模块）

| 模块 | 完成度 | 评估说明 |
|------|--------|----------|
| **类型层次** (`abstract_model.jl`) | ✅ 100% | 类型树完备，接口契约清晰 |
| **NJLModel / NJL2Model** | ✅ 100% | 完全原生实现，无 legacy 依赖 |
| **PNJLModel** | ✅ 100% | `solve_gap` 废弃关键字已删除，统一走 models 求解链路 |
| **RPNJLModel** | 🔶 70% | 明确标记为"MVP shell"，`number_densities` 直接委托给 PNJLModel，thermal/vacuum 复用 PNJL |
| **PNJLMagneticModel** | 🔶 80% | 标记为"minimum viable version"，大部分委托给 PNJLModel |
| **factory.jl** | ✅ 100% | 仅保留现役模型注册，legacy adapter 分支已删除 |
| **gap_solver.jl** | ✅ 95% | 原生实现完整 |
| **solver/** | ✅ 100% | ConstraintModes/SeedStrategies/Conditions/ImplicitSolver 已全部改为直接走 Models 接口，solver 子树不再依赖 ThermoFacade |
| **ThermoFacade** | ✅ 100% | 已删除；调用方已全部切换为直接 Models 接口 |
| **EquilibriumFacade** | ✅ 90% | 保留 canonical `pnjl_physics/core/EquilibriumFacade.jl`，redirect shim 已删除 |
| **Models.jl 入口** | ✅ 100% | `@eval Main` 注入、旧 thermodynamics/omega 包装和无模型 fallback 已删除，调用方统一改为显式 `Models` 入口 |
| **transport_provider.jl** | 🔶 60% | 当前按研究范围仅需支持 PNJLModel；其余模型暂不泛化 |
| **scans/** | ✅ 95% | TmuScan/TrhoScan/DualBranch 完全原生 |
| **phase/** | ✅ 90% | PhaseCore/CEPDetector/CrossoverLine/PhasePipeline 功能完备，N1/N2 批次已完成 |
| **workflows/** | ✅ 90% | WorkflowParamAdapters 已切换为仅接受 `QuarkParams` / `ThermoParams` |

---

## 二、发现的具体改进点

### 2.1 遗留代码清理（Legacy Cleanup）

| ID | 问题 | 位置 | 影响 |
|----|------|------|------|
| L1 | Legacy 适配器已删除 | `src/models/legacy/LegacyNJLModel.jl`, `LegacyPNJLModel.jl` | 阶段 1.1 已完成 |
| L2 | ThermoFacade 桥接层已删除 | `src/models/pnjl_physics/core/ThermoFacade.jl` | 阶段 1.2 已完成 |
| L3 | `Models.jl` 入口兼容层已清理 | `Models.jl` | 阶段 1.5 已完成 |
| L4 | `IncludeOnce` 模式已清理 | `src/**/*.jl` | 阶段 2.2 已完成 |
| L5 | `src/models/pnjl/core/Integrals.jl` redirect shim | `src/models/pnjl/core/Integrals.jl` | 旧路径残留，应由引用方直接改引新路径 |
| L6 | `src/models/core/EquilibriumFacade.jl` redirect shim | `src/models/core/EquilibriumFacade.jl` | 同上 |
| L7 | Deprecated 参数清理已完成 | `PNJLModel.jl`, `WorkflowParamAdapters.jl`, `transport_provider.jl` | 阶段 1.4 已完成 |

### 2.2 架构与设计改进

| ID | 问题 | 位置 | 影响 |
|----|------|------|------|
| A1 | `transport_provider` 当前仅支持 PNJLModel（暂按范围接受） | `transport_provider.jl:130` | 非 PNJL 模型暂不进入输运工作流范围 |
| A2 | `thermo_kernel.jl` 使用 `isdefined(Main, ...)` fallback | `thermo_kernel.jl` | 不健壮的常量获取方式 |
| A3 | RPNJLModel 为 MVP 壳，缺乏独立物理实现（已降级为 backlog） | `rpnjl/RPNJLModel.jl` | 当前不阻塞主线；仅在研究确有需要时再展开 |
| A4 | `Models.jl` 顶层 `@eval Main` 注入已删除 | `Models.jl` | 阶段 1.5 已完成 |
| A5 | ThermoFacade 调用方清理已完成 | `workflows/`, `derivatives/`, `scans/`, `pnjl_physics/core/EquilibriumFacade.jl` | 阶段 1.2 已完成 |

### 2.3 测试覆盖缺口

| ID | 问题 | 说明 |
|----|------|------|
| T1 | **缺少系统化的回归测试** | 现有回归测试分散在 `scripts/`（作为脚本而非 `@testset`）和 `tests/validation/`（仅 4 个文件），且 validation 中的 baseline 仅覆盖 PNJL scan/constraint/magnetic + relaxtime cross-section |
| T2 | NJL/NJL2 模型回归基线已补齐 | 已新增 2 份 baseline CSV 与 fixedpoint regression，后续只剩 rPNJL 待补 |
| T3 | RPNJLModel 无回归基线 | 当前唯一仍需补齐的模型回归缺口 |
| T4 | Phase diagram pipeline 回归基线已建立 | CEP 位置、一阶边界线、crossover 线已落盘到 `tests/baselines/phase/` |
| T5 | 输运工作流端到端回归已显著补强 | `baseline_transport_fixedpoints_v1.csv` 已扩展到 13 点，另补充了 meson mass workflow 回归 |
| T6 | solver/ImplicitSolver 隐式微分结果无回归 | dx/dT, dx/dmu 导数结果无 baseline |
| T7 | ThermoDerivatives (bulk viscosity coefficients) 无回归 | dP/dT, dP/dmu, B bracket 无 baseline |
| T8 | 验证测试缺少参考数据 | 明确属于外部依赖，暂不作为当前 active 执行项 |

### 2.4 CI 与工程实践

| ID | 问题 | 说明 |
|----|------|------|
| C1 | `nightly-full-regression.yml` 依赖 `scripts/` 中的回归脚本而非 `tests/`（已基本缓解） | 当前仅剩是否进一步合并 workflow 的工程优化选择 |
| C2 | 缺少 `tests/regression/` 分层 | 回归测试散布于 `scripts/` 和 `tests/validation/` |
| C3 | `Models.jl` 模块文档已更新为显式 Models 入口说明 | 阶段 1.5 已完成 |

---

## 三、改进计划

### 阶段 0：回归测试补充（当前 active 主线）

> 理由：在进行任何清理或重构之前，必须先建立完善的数值回归网，确保后续改动不破坏物理正确性。

#### 0.1 建立回归测试框架

- [x] 在 `tests/` 下新增 `regression/` 目录，专门存放数值回归测试
- [x] 统一回归测试模板：加载 baseline CSV → 运行计算 → `@test isapprox(computed, reference; rtol=..., atol=...)` → 报告差异
- [x] 将现有 `scripts/` 中的回归脚本逻辑迁移为 `@testset` 格式

#### 0.2 PNJL 模型回归测试（基于现有 baseline 扩充）

- [x] **PNJL gap 求解回归**：已选取 12 个 (T, μ) 固定点，记录 MeanFieldState (φ_u, φ_d, φ_s, Φ, Φbar) + `omega` 作为 baseline
  - 覆盖区域：hadron 相、quark 相、crossover 附近、一阶相变附近
  - 文件：`tests/regression/pnjl/test_pnjl_gap_fixedpoint_regression.jl`
  - Baseline：`tests/baselines/pnjl/baseline_pnjl_gap_fixedpoints_v1.csv`
  - 容差：`rtol=1e-6, atol=1e-10`

- [x] **PNJL 约束求解回归**：验证 FixedMu/FixedRho/FixedEntropy/FixedSigma/FixedAsymRho 五种约束模式
  - 每种模式至少 3 个固定点（当前实现了 FixedMu 和 FixedRho，各 3 个固定点）
  - 文件：`tests/regression/pnjl/test_constraint_fixedpoint_regression.jl`
  - Baseline：`tests/baselines/pnjl/baseline_pnjl_constraint_fixedpoints_v1.csv`

- [x] **PNJL 热力学导数回归**：dP/dT, dP/dmu, bulk_derivative_coeffs
  - 5 个固定点，覆盖不同温度/密度区域
  - 文件：`tests/regression/pnjl/test_pnjl_thermo_derivatives_regression.jl`

- [x] **PNJL implicit diff 回归**：dx/dT, dx/dmu 隐式微分结果
  - 3 个固定点
  - 文件：`tests/regression/pnjl/test_pnjl_implicit_diff_regression.jl`

#### 0.3 NJL / NJL2 模型回归测试（新建 baseline）

- [x] **NJL3 gap 求解回归**：5 个 (T, μ) 固定点，记录 MeanFieldState + omega
  - 文件：`tests/regression/njl/test_njl_gap_fixedpoint_regression.jl`
  - Baseline：`tests/baselines/njl/baseline_njl_gap_fixedpoints_v1.csv`

- [x] **NJL2 gap 求解回归**：5 个固定点
  - 文件：`tests/regression/njl/test_njl2_gap_fixedpoint_regression.jl`
  - Baseline：`tests/baselines/njl/baseline_njl2_gap_fixedpoints_v1.csv`

#### 0.4 RPNJLModel 回归测试（已完成）

- [x] **rPNJL gap 求解回归**：5 个固定点，使用默认 profile 配置
  - 文件：`tests/regression/rpnjl/test_rpnjl_gap_fixedpoint_regression.jl`
  - Baseline：`tests/baselines/rpnjl/baseline_rpnjl_gap_fixedpoints_v1.csv`

#### 0.5 Scan 链路端到端回归

- [x] **TmuScan 回归**：完整 T-μ 扫描链路，5 行结果 baseline
  - 文件：`tests/regression/pnjl/test_scan_fixedpoint_regression.jl`
  - Baseline：`tests/baselines/pnjl/baseline_pnjl_scan_fixedpoints_v1.csv`

- [x] **TrhoScan 回归**：完整 T-ρ 扫描链路
  - 文件：`tests/regression/pnjl/test_scan_fixedpoint_regression.jl`（与 TmuScan 在同一文件）
  - Baseline：`tests/baselines/pnjl/baseline_pnjl_scan_fixedpoints_v1.csv`

#### 0.6 Phase Diagram Pipeline 回归

- [x] **相图产线回归**：记录 CEP 位置 (T_CEP, μ_CEP)、一阶边界线、crossover 线作为 baseline
  - 文件：`tests/regression/phase/test_phase_pipeline_regression.jl`
  - Baseline：`tests/baselines/phase/baseline_phase_pipeline_v1.csv`

#### 0.7 Transport 回归扩充

- [x] **扩充输运 fixedpoint**：从 5 个点扩充到 13 个，覆盖 `xi=0`、`xi=0.2`、`xi=-0.2` 各向异性情形
- [x] **Meson mass workflow 回归**：3 个固定点的 meson mass 结果
  - 文件：`tests/regression/relaxtime/test_meson_mass_regression.jl`

#### 0.8 回归测试入口与 CI 接入

- [x] `tests/regression/runtests.jl`：统一回归测试入口
- [x] 更新 `.github/workflows/nightly-full-regression.yml` 使用新入口
- [x] 回归测试纳入 CI validation 层

---

### 阶段 1：迁移最后一公里（清理遗留代码）

> 前置条件：阶段 0 回归测试全部通过，且新增 baseline 已存档

#### 1.1 移除 Legacy 适配器

- [x] 删除 `src/models/legacy/LegacyNJLModel.jl`
- [x] 删除 `src/models/legacy/LegacyPNJLModel.jl`
- [x] 从 `factory.jl` 移除 `:LegacyNJL`, `:LegacyPNJL` 注册
- [x] 从 `Models.jl` 移除 `LegacyNJLModel`, `LegacyPNJLModel` 的 export
- [x] 删除或迁移 legacy 相关测试文件：
  - `tests/unit/models/test_legacy_njl_model.jl`
  - `tests/unit/pnjl/test_legacy_pnjl_model.jl`
  - `tests/integration/models/test_models_legacy_adapter_smoke.jl`
  - `tests/integration/models/test_models_legacy_njl_smoke.jl`
  - `tests/integration/models/test_phase_legacy_path_detach_smoke.jl`（保留；该测试验证 phase 流程已断开旧路径引用，不依赖 legacy adapter 类型）
  - `tests/integration/relaxtime/test_transport_legacy_models_bridge_smoke.jl` → `tests/integration/relaxtime/test_transport_solver_backend_bridge_smoke.jl`
- [x] 更新 `tests/unit/runtests.jl` 和 `tests/integration/runtests.jl` 的 smoke 列表（经核对当前 curated smoke 未直接引用已删除文件，无需额外改动）

#### 1.2 消除 ThermoFacade 桥接层

- [x] 让 `solver/ImplicitSolver.jl` 直接调用 Models 接口，而非经由 ThermoFacade
- [x] 让 `solver/Conditions.jl` 直接使用 Models 接口
- [x] 将 `derivatives/`、`workflows/`、`scans/` 与 canonical `pnjl_physics/core/EquilibriumFacade.jl` 全部改为直接调用 Models 接口
- [x] 确认 ThermoFacade 无其他调用方后，删除 `src/models/pnjl_physics/core/ThermoFacade.jl`
- [x] 删除 `Models.jl` 中 `const Thermodynamics = Main.ThermoFacade` alias
- [x] 删除桥接专用测试并改写剩余调用方测试到主路径
- [x] 运行全套回归测试确认无影响（本轮 `tests/regression/runtests.jl` 419/419 通过）

#### 1.3 清理 redirect shim

- [x] 删除 `src/models/pnjl/core/Integrals.jl` redirect shim
- [x] 删除 `src/models/core/EquilibriumFacade.jl` redirect shim
- [x] 全局搜索所有引用方，改为直接引用新路径

#### 1.4 清理 deprecated 参数

- [x] `PNJLModel.jl`：移除 `solver_backend` 和 `fallback_legacy_on_failure` 参数
- [x] `WorkflowParamAdapters.jl`：移除 NamedTuple 兼容路径
- [x] `transport_provider.jl`：移除 `transport_provider(thermo_backend::Symbol)` 重载

#### 1.5 Models.jl 入口优化

- [x] 移除 `@eval Main const PNJL = ...` 等全局注入，改为要求调用方显式 `using .Models`
- [x] 移除 `calculate_U`, `calculate_U_derivative_T` 等非多重派发的兼容函数
- [x] 移除 `calculate_mass_vec(x_state::AbstractVector)` 等无模型参数的 fallback
- [x] 更新 Models.jl 顶部文档注释（移除"重构早期阶段"措辞）
- [x] 整理 export 列表：移除旧 thermodynamics/omega 包装与 Main 注入依赖，仅保留显式 `Models` 入口和主路径接口

---

### 阶段 2：架构增强

> 说明：本阶段整体降级为 backlog，不作为当前 active 执行面。

#### 2.1 transport_provider 泛化（暂缓）

- [ ] 暂缓执行；当前研究范围内输运系数分支仅要求支持 `PNJLModel`
- [ ] 后续若恢复该项，再为 `AbstractNJLModel` 提供默认 `transport_provider` 实现
- [ ] 后续若恢复该项，再评估 RPNJLModel / PNJLMagneticModel / NJLModel / NJL2Model 的 provider 策略

#### 2.2 消除 IncludeOnce 模式

- [x] 重构 `solver/ImplicitSolver.jl` 为标准 Julia submodule
- [x] 重构 `solver/Conditions.jl` 为标准 Julia submodule
- [x] 清理 `workflows/`、`scans/`、`pnjl_physics/`、`derivatives/`、`utils/` 中的 `IncludeOnce` 调用
- [x] 删除 `src/utils/IncludeOnce.jl`

#### 2.3 RPNJLModel 完善（视研究需要，已降级为 backlog）

- [x] 已完成与 `docs/reference/formula/models/rpnjl/rPNJL_core.md` 的公式一致性核对；当前实现已覆盖式 (3.27)–(3.31) 的核心扩展项
- [ ] 实现独立的 `number_densities`（如果 rPNJL 的分布函数与标准 PNJL 有差异）
- [ ] 验证 8-quark 耦合对热力学量/输运系数的影响
- [ ] 建立 rPNJL-specific regression baseline

---

### 阶段 3：验证测试（等待外部参考数据）

> 说明：本阶段依赖外部参考数据，当前不纳入 active 执行面；若后续出现新验证任务，应单独起新的 active 文档承载。

> 前置条件：用户从 Fortran/Mathematica/文献获取参考数据后启动

#### 3.1 Fortran 对照验证

- [ ] PNJL gap 方程解：与 Fortran 代码比对 (T, μ) → (M_u, M_d, M_s, Φ, Φbar)
- [ ] 热力学量：P, ρ, s, ε 对照
- [ ] 输运系数：η, σ, ζ 对照

#### 3.2 Mathematica 对照验证

- [ ] One-loop 积分 A0, B0 数值精度
- [ ] Meson propagator / polarization 函数
- [ ] Cauchy PV 积分精度

#### 3.3 文献参考值验证

- [ ] 相图 CEP 位置与已发表文献比较
- [ ] 输运系数温度依赖趋势与文献比较

---

### 阶段 4：工程实践改进（仅保留轻量后续项）

#### 4.1 测试基础设施

- [x] 统一回归测试入口到 `tests/regression/runtests.jl`
- [x] 迁移现有回归 gate 脚本到 `tests/regression/`（PNJL magnetic / transport / total-cross-section 已完成）
- [x] 建立 baseline 版本管理规范（v1 → v2 迁移流程）
  - 文档：`docs/guides/BASELINE_VERSION_MANAGEMENT.md`
  - 包含：命名规范、迁移流程、完整性检查清单、审查流程

#### 4.2 文档更新

- [x] 更新 `README.md` 中的架构描述（已添加基于多重派发的模型系统架构说明）
- [x] 更新 `docs/architecture/dependencies.manual.md`（已添加 Models 模块架构图和回归测试链路）
- [x] 清理 `docs/guides/PARAMETER_STRUCT_MIGRATION.md`（迁移已完成，已添加完成状态标记）

#### 4.3 CI 优化

- [ ] 评估是否需要进一步合并 `ci-validation.yml` 和 `nightly-full-regression.yml` 中的回归测试
- [ ] 如确有收益，再为 regression 层建立独立 CI workflow

---

## 四、迁移完成度详细清查

### 4.1 `src/models/legacy/` 目录

| 文件 | 状态 | 计划 |
|------|------|------|
| `LegacyNJLModel.jl` | 已 deprecated，44 行，纯委托 NJLModel | 阶段 1.1 删除 |
| `LegacyPNJLModel.jl` | 已 deprecated，157 行，委托 ThermoFacade | 阶段 1.1 删除 |

### 4.2 `src/models/pnjl_physics/` 目录

| 文件 | 状态 | 是否属于旧架构 |
|------|------|---------------|
| `PNJLCore.jl` | ✅ 活跃 | 否 — 是新架构核心物理实现 |
| `PNJLModel.jl` | 🔶 活跃但含 deprecated 参数 | 否 — 实现本身已原生化 |
| `PNJLIntegrals.jl` | ✅ 活跃 | 否 — 数值积分核心 |
| `PNJLDistributions.jl` | ✅ 活跃 | 否 — 分布函数供 transport 使用 |
| `PNJLMagneticModel.jl` | 🔶 MVP 状态 | 否 — 新架构实现，但功能不完整 |
| `QuarkDistribution.jl` | ✅ 活跃 | 否 |
| `core/ThermoFacade.jl` | 已删除 | 否 — 阶段 1.2 已完成 |
| `core/EquilibriumFacade.jl` | ✅ 活跃 | 部分 — 用于 solver 后的平衡态构建 |
| `core/ModelThermodynamics.jl` | ✅ 活跃 | 否 — models 侧热力学桥接 |
| `core/Integrals.jl` | ✅ 活跃 | 否 — PNJL 积分实现 |
| `core/MagneticIntegrals.jl` | ✅ 活跃 | 否 |
| `core/MagneticThermodynamics.jl` | ✅ 活跃 | 否 |

### 4.3 `src/models/solver/` 目录

| 文件 | 状态 | 是否属于旧架构 |
|------|------|---------------|
| `Solver.jl` | ✅ 活跃 | 否 — 新架构统一入口 |
| `ImplicitSolver.jl` | ✅ 活跃 | 否 — 已在阶段 1.2 改为直接调用 Models 接口 |
| `ConstraintModes.jl` | ✅ 活跃 | 否 |
| `SeedStrategies.jl` | ✅ 活跃 | 否 |
| `Conditions.jl` | ✅ 活跃 | 否 — 已在阶段 1.2 改为直接调用 Models 接口 |

### 4.4 `src/models/pnjl/core/Integrals.jl` (旧路径 redirect shim)

| 文件 | 状态 | 计划 |
|------|------|------|
| `src/models/pnjl/core/Integrals.jl` | redirect → `pnjl_physics/core/Integrals.jl` | 阶段 1.3 删除 |

### 4.5 `src/models/core/EquilibriumFacade.jl` (redirect shim)

| 文件 | 状态 | 计划 |
|------|------|------|
| `src/models/core/EquilibriumFacade.jl` | redirect → `pnjl_physics/core/EquilibriumFacade.jl` | 阶段 1.3 删除 |

---

## 五、关于回归测试 vs 验证测试的策略

### 5.1 定义区分

| 类型 | 回归测试 (Regression) | 验证测试 (Validation) |
|------|----------------------|----------------------|
| **目的** | 确保代码变更不改变输出 | 确保输出物理正确 |
| **参考源** | 自身历史版本（baseline CSV） | 外部参考（Fortran/Mathematica/文献） |
| **容差** | 严格（`rtol=1e-6` 量级） | 可适度宽松 |
| **前置条件** | 无（运行当前代码生成 baseline） | 需要外部参考数据 |
| **何时运行** | 每次提交 / nightly | 里程碑节点 |

### 5.2 建议执行序

1. **先做回归测试**（阶段 0）：不需要外部数据，可立即执行。生成的 baseline 本身就是当前代码数值行为的"快照"，为后续重构提供安全网。
2. **再做验证测试**（阶段 3）：等待用户获取 Fortran/Mathematica 参考数据后启动。验证测试可以复用回归测试的固定点选取和测试框架。

### 5.3 回归测试固定点选取原则

- **物理覆盖**：hadron 相 (低 T, 低 μ)、quark 相 (高 T)、crossover 区域、一阶相变附近、CEP 附近
- **参数覆盖**：xi=0（各向同性）和 xi≠0（各向异性）
- **模型覆盖**：每个模型类型至少 5 个点
- **可复现**：使用 TOML 配置文件中的 unittest/default 参数集
- **容差标注**：每个 `isapprox` 都显式标注 `rtol` 和 `atol`，避免隐式默认

---

## 六、优先级与执行路线图

当前建议只保留下列执行顺序：

1. 复跑 `tests/regression/runtests.jl` 与相关 smoke，确认 0.4 与默认 `kappa=0.1` 口径稳定。
2. 若验证通过，则本文件可归档。
3. 阶段 2/3 事项保持 backlog，等待研究需求或外部数据触发，不在本文件中继续并行推进。

```
阶段 0（已完成）
  └── 0.4 rPNJL 回归

阶段 4（可选后续）
  └── 4.3 CI 优化

backlog（不在当前 active 并行推进）
  ├── 2.1 transport_provider 泛化
  ├── 2.3 RPNJLModel 完善
  └── 3.x 外部参考验证
```

---

## 七、与现有 active 文档的关系

| 现有文档 | 关系 |
|----------|------|
| `2026-03-01_PNJL可选功能盘点与优先级任务单.md` | 互补 — 该文档聚焦"新功能扩展"，本文档聚焦"迁移收尾+测试补全" |
| `2026-03-02_相图自动化产线原生化执行台账.md` | 互补 — 该文档记录 phase pipeline 的实施过程，本文档 0.6 节将为其补充回归保障 |
| `2026-03-02_相图自动化产线原生化设计文档.md` | 互补 — 同上 |
| `2026-03-02_二分找CEP加强计划.md` | 互补 — CEP 功能增强，本文档阶段 0.6 为其建立回归基线 |
| `2026-01-22_PNJL求解时二阶偏导符号问题.md` | 独立 — 该问题为特定的数值 bug，不受本计划影响 |

---

## 八、备注

- 当前建议把本文件视为“rPNJL regression 收尾单”，而不再作为全量项目路线图继续扩张。
- 若 0.4 完成，应优先考虑将本文件归档；阶段 2/3 的 backlog 若重新启动，建议拆分成新的 active 文档单独承载。
- 阶段 3 的验证测试需要用户从 Fortran 程序或 Mathematica notebook 中提取参考数据后再启动。
