---
title: 项目结构治理与一致性修复任务单（可勾选）
archived: true
original: docs/dev/active/2026_02_18_项目结构治理与一致性修复任务单.md
archived_date: 2026-02-18
---


以下为原始内容（保留，以便审阅与历史参考）：

---

# 项目结构治理与一致性修复任务单（可勾选）

## 文档目的

将“项目结构/文档/流程信号不一致”问题收敛为可执行步骤，并按顺序一次性推进：

- 修复文档路径漂移与失效链接
- 建立 CI 单元 smoke 门禁
- 统一输出目录口径（`data/outputs`）

---

## 主要问题清单（来自现状审查）

### P0（立即修复）

- [x] `README.md` 仍指向不存在的 `docs/process/*`
- [x] `docs/guides/STATUS.md` 与 `src/simulation/README.md` 仍使用旧路径（`server.jl`/`test_unit`/`examples`/`doc`）
- [x] `docs/api/pnjl/Crossover.md` 链接到已迁移文件（应指向 archived）

### P1（本批次落地基础设施）

- [x] 缺少统一单元 smoke 的 CI 门禁（当前 workflow 侧重 benchmark/audit）
- [x] 为 `tests/unit/runtests.jl` 的 smoke 入口提供标准 CI 调用方式

### P2（本批次完成口径统一）

- [x] 文档统一声明 `data/outputs` 为唯一运行产物目录口径
- [x] 明确根目录 `outputs/` 不作为默认结果落盘位置

---

## 治理顺序（按序执行）

### Step 1 文档路径与链接修复（P0）

- [x] 更新 `README.md` 中 `docs/process/*` 相关描述为当前有效目录
- [x] 更新 `docs/guides/STATUS.md` 到当前启动/测试/文档路径
- [x] 更新 `src/simulation/README.md` 到当前启动/测试/文档路径
- [x] 修复 `docs/api/pnjl/Crossover.md` 的迁移后链接

### Step 2 CI smoke 门禁（P1）

- [x] 新增 `.github/workflows/unit-smoke.yml`
- [x] 以 `tests/unit/runtests.jl` + `UNIT_PROFILE=smoke` 作为门禁执行命令
- [x] 本地执行一次对应命令进行可运行性验证

### Step 3 输出目录口径统一（P2）

- [x] 在 `README.md` 与 `docs/guides/STATUS.md` 明确 `data/outputs` 为默认落盘目录
- [x] 明确 `outputs/` 非默认落盘目录（保留兼容，不作为规范路径）

### Step 4 回归与收尾

- [x] 运行关键命令验证（文档链接检查 + smoke 测试命令）
- [x] 回填本任务单的执行记录与完成定义

---

## 完成定义（Definition of Done）

- [x] 以上 Step 1~4 全部勾选完成
- [x] 文档中不再出现已迁移但失效的关键路径引用（本任务涉及范围内）
- [x] 新增 CI smoke workflow 可被仓库触发并可本地复现

---

## 执行记录

- 2026-02-18：创建任务单，准备按“P0 → P1 → P2 → 验证”顺序自动推进。
- 2026-02-18：完成 Step 1，修复 `README.md`、`docs/guides/STATUS.md`、`src/simulation/README.md`、`docs/api/pnjl/Crossover.md` 的关键路径与链接漂移。
- 2026-02-18：完成 Step 2，新增 `.github/workflows/unit-smoke.yml`，并执行本地 smoke 命令：`UNIT_PROFILE=smoke julia --project=. tests/unit/runtests.jl`。
- 2026-02-18：完成 Step 3，统一文档口径为 `data/outputs` 默认落盘，`outputs/` 仅历史兼容。
- 2026-02-18：完成 Step 4 验证；路径检索通过。初次 smoke 本地运行结果：`589 passed, 3 failed, 6 errored`。
- 2026-02-18：继续自动推进并修复 smoke 稳定性问题（workflow 调用歧义、A-map 容差过严、RPNJL 脆弱断言）；复跑结果：`690 passed, 0 failed, 0 errored`。

## 当前阻碍

- 无。当前任务范围内步骤已全部完成。
