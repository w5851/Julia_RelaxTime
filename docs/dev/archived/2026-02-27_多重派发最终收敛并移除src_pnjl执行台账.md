---
title: 多重派发最终收敛并移除src pnjl执行台账
archived: true
original: docs/dev/active/2026-02-27_多重派发最终收敛并移除src_pnjl执行台账.md
archived_date: 2026-03-01
---


以下为原始内容（保留，以便审阅与历史参考）：

---

# 多重派发最终收敛并移除src pnjl执行台账

更新日期：2026-02-27

---

## 0. 台账定位

- 本文档仅记录“执行事实”：变更点、命令、产物、结果。
- 本阶段开发任务定义统一维护在：
  - `docs/dev/active/2026-02-27_多重派发最终收敛并移除src_pnjl开发任务单.md`

---

## 1. 记录规范（强约束）

- [x] 执行记录与开发任务分开保存：
  - 任务目标/范围/DoD 只写“开发任务单”；
  - 执行过程/命令/结果只写“执行台账”。
- [x] 追加记录时采用“直接追加”策略：
  - 不要求回读本台账历史上下文；
  - 仅按统一模板追加到文档末尾；
  - 每条记录必须自包含（含目标、命令、产物、结果）。
- [x] 每条记录必须可追溯到输出产物（`data/outputs/results/*`）。

---

## 2. 执行记录

- [ ] 批次号：Batch P4-收口
  - 目标：完成 src/pnjl->src/legacy_pnjl 迁移收敛并通过 smoke 与三项门禁
  - 代码变更：迁移 src/pnjl 至 src/legacy_pnjl；收敛 models/workflow/solver/derivatives/tests 路径；补齐 ThermoFacade/ImplicitSolver 接口与 include 守卫；修复 world-age 与命名冲突
  - 验证命令：
    - `UNIT_PROFILE=smoke julia --project=. tests/unit/runtests.jl`
    - `julia --project=. scripts/dev/check_pnjl_migration_guard.jl --base HEAD --head HEAD`
    - `julia --project=. scripts/dev/check_data_output_path_guard.jl --base HEAD --head HEAD`
    - `julia --project=. scripts/dev/run_prune_wave_gate.jl --base HEAD --head HEAD`
    - `git ls-tree -r --name-only HEAD src/pnjl`
  - 产物：
    - `data/outputs/results/pnjl_prune_wave_snapshot_20260227_100414.txt`
    - `data/outputs/results/models_invokelatest_allowlist_delta_20260227_100414.txt`
    - `data/outputs/results/pnjl_scan_default_include_audit_20260227_100414.txt`
  - 结果：smoke(845/845)与门禁通过；提交级验证阻塞：HEAD 尚未提交，ls-tree 仍显示 src/pnjl
  - 主线映射（N1/N2/N3/N4）：N1/N2/N3/N4
