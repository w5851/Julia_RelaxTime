---
title: 技术债优先级清单与验收任务单（执行版，可勾选）
archived: true
original: docs/dev/active/2026-02-19_技术债优先级清单与验收任务单.md
archived_date: 2026-02-19
---


以下为原始内容（保留，以便审阅与历史参考）：

---

# 技术债优先级清单与验收任务单（执行版，可勾选）

## 目标

将当前项目的技术债治理工作拆分为可执行、可验收、可追踪的任务单，按优先级分为 P0/P1/P2。

## 执行顺序

1. 先完成 P0（质量门禁与主链路正确性）
2. 再推进 P1（结构解耦与回归强度）
3. 最后处理 P2（治理运营与长期维护）

---

## P0（最高优先级：阻断回归风险）

### P0-1：DEFAULT_SKIP 清零计划（分项转正）

- [x] 为以下 4 个 skip 用例建立逐项转正任务：
  - [x] `test_core_integrals.jl`
  - [x] `test_implicit_jacobian.jl`
  - [x] `test_scans.jl`
  - [x] `test_differential_cross_section.jl`
- [x] 每个用例补齐“失败根因 + 修复策略 + 回归点”记录。

**可执行脚本**

```powershell
julia --project=. scripts/dev/check_unit_skip_policy.jl
julia --project=. tests/unit/runtests.jl
```

**验收标准**

- [x] `scripts/dev/check_unit_skip_policy.jl` 通过。
- [x] `config/ci/unit_skip_policy.toml` 与 `tests/unit/runtests.jl` 一致。
- [x] `DEFAULT_SKIP` 条目数相较当前基线不增加（理想为下降）。

**执行证据（2026-02-19）**

- `julia --project=. scripts/dev/check_unit_skip_policy.jl`：通过（phase-2，`max_skip=0`，required entries 为空）。
- 4 个历史 skip 文件已分别执行并通过（`test_core_integrals.jl` 含 1 个既有 `broken`，为测试内显式标注，不再由 `DEFAULT_SKIP` 屏蔽）。
- `DEFAULT_SKIP` 已清零：`tests/unit/runtests.jl` 当前为空集合，策略文件已同步。
- 转正记录文档：`docs/dev/archived/2026-02-19_DEFAULT_SKIP转正记录.md`。

---

### P0-2：主链路固定点回归（PNJL + Transport）

- [x] 固化最小固定点回归集合（T/μ/ξ 若干代表点）。
- [x] 将固定点结果写入统一结果目录并纳入比对。
- [x] 对超阈值偏差给出失败退出与定位提示。

**可执行脚本**

```powershell
julia --project=. scripts/relaxtime/run_gap_transport_scan.jl --help
julia --project=. tests/unit/runtests.jl
```

**验收标准**

- [x] 固定点回归在 CI 中可重复。
- [x] 结果落盘路径统一为 `data/outputs/`。
- [x] 偏差超阈值时任务失败并输出可定位信息。

**执行证据（2026-02-19）**

- 固定点集合来源：`tests/baselines/relaxtime/baseline_transport_fixedpoints_v1.csv`（当前 5 个点，含 `ξ=0.2`）。
- 新增回归门禁脚本：`scripts/relaxtime/run_transport_fixedpoint_regression.jl`。
  - 默认落盘：`data/outputs/results/relaxtime/regression/transport_fixedpoint_regression_latest.csv`。
  - 默认容差：`rtol=8e-2, atol=1e-6`（支持 CLI 覆盖）。
  - 超阈值行为：脚本 `error` 退出并打印“点位 + backend + 指标 + actual/expected + abs/rel 误差”。
- 本地验证命令：`julia --project=. scripts/relaxtime/run_transport_fixedpoint_regression.jl`。
  - 结果：`[PASS] All points are within tolerance.`
- CI 可重复性：已接入 `.github/workflows/nightly-full-regression.yml`（新增 `Run transport fixedpoint regression gate` 步骤）。

---

### P0-3：单元测试语义收敛（unit 与 smoke 边界）

- [x] 清点 `tests/unit/` 中偏 workflow/integration 的条目。
- [x] 将非纯 unit 用例迁移到 `tests/analysis/` 或 `tests/perf/`（按性质）。
- [x] 保持 `UNIT_PROFILE=smoke` 的稳定绿色基线。

**可执行脚本**

```powershell
$env:UNIT_PROFILE='smoke'; julia --project=. tests/unit/runtests.jl
$env:UNIT_PROFILE='full';  julia --project=. tests/unit/runtests.jl
```

**验收标准**

- [x] `smoke` 通过且时长稳定（无明显回归）。
- [x] `full` 可运行并反映真实测试面，不混入脚本型检查。
- [x] 迁移后的目录职责与 `docs/dev/README.md` 约定一致。

**当前状态（2026-02-19）**

- `UNIT_PROFILE=smoke`：通过（`738 passed`，约 `2m18s`）。
- `UNIT_PROFILE=full`：通过（`1663 passed, 1 broken`，约 `5m24s`；`broken` 为既有测试内标注项）。
- 清点与迁移记录：`docs/dev/archived/2026-02-19_P0-3_unit_scope_收敛记录.md`。
- 已迁移非 unit 文档/报告：
  - `tests/unit/struct_migration/*.md` → `tests/analysis/struct_migration/`
  - `tests/unit/relaxtime/test_differential_cross_section_performance.md` → `tests/perf/relaxtime/`
- 迁移后校验：`tests/unit/**/*.md` 已清零。

---

## P1（中优先级：提升可维护性与门禁强度）

### P1-1：服务端入口解耦（full server 减负）

- [x] 将服务启动、路由装配、静态资源处理、业务计算进一步模块化。
- [x] 降低 `scripts/server/server_full.jl` 的职责密度。

**可执行脚本**

```powershell
julia --project=. scripts/server/server_full.jl
```

**验收标准**

- [x] `/health`、`/compute`、`/api/modules/pnjl-gap/run` 行为与改造前一致。
- [x] 启动脚本仅保留编排职责，核心逻辑下沉至 `src/simulation/`。
- [x] 相关 API 文档已同步更新。

**执行证据（2026-02-19）**

- 入口职责下沉：新增 `src/simulation/ServerLauncher.jl`，`scripts/server/server_full.jl` 仅保留环境激活 + 调用 `ServerLauncher.run_full_server(...)`。
- full server 模块化拆分：
  - `src/simulation/fullserver/routing.jl`
  - `src/simulation/fullserver/compute_handlers.jl`
  - `src/simulation/fullserver/pnjl_handlers.jl`
  - `src/simulation/fullserver/http_utils.jl`
  - `src/simulation/fullserver/shared.jl`
- 端点回归命令（端口 8091）：
  - `GET /health` -> `200`
  - `POST /compute` -> `200`
  - `POST /api/modules/pnjl-gap/run` -> `200`
- 文档同步：`src/simulation/README.md` 已更新 full server 分层结构与脚本职责说明。

---

### P1-2：nightly full 回归与失败可读性增强

- [x] 审核并收敛 nightly full 回归的失败日志格式。
- [x] 增加失败后的快速定位提示（模块/文件级）。

**可执行脚本**

```powershell
$env:UNIT_PROFILE='full'; julia --project=. tests/unit/runtests.jl
```

**验收标准**

- [x] `.github/workflows/nightly-full-regression.yml` 稳定运行。
- [x] 失败日志可直接定位到测试文件或子模块。
- [x] 无需二次猜测即可复现失败命令。

**执行证据（2026-02-19）**

- nighty full 配置增强：`.github/workflows/nightly-full-regression.yml` 为 unit full 步骤新增 `UNIT_VERBOSE_INCLUDE=1`，输出逐文件 include 轨迹。
- `tests/unit/runtests.jl` 增强失败可读性：
  - full include 失败时打印 `include failed: <relative-file>`；
  - 同时打印一条可直接复现命令：`UNIT_PROFILE=full UNIT_FILES='<file>' julia --project=. tests/unit/runtests.jl`。
- 针对 nightly 失败点修复并回归：
  - `tests/unit/pnjl/test_quark_distribution_aniso.jl` 增加 `QuarkDistribution.jl` 预加载（规避模块加载顺序问题）。
  - `tests/unit/relaxtime/test_transport_workflow.jl` 在 full 入口下复测通过（`33 passed`）。
  - `tests/unit/pnjl/test_quark_distribution_aniso.jl` 在 full 入口下复测通过（`7 passed`）。

---

### P1-3：配置矩阵防漂移（njl/pnjl/rpnjl）

- [x] 建立 profile 差异检查（字段完整性、关键参数一致性）。
- [x] 明确 default 与 unittest 配置的覆盖关系。

**可执行脚本**

```powershell
julia --project=. tests/unit/runtests.jl
```

**验收标准**

- [x] 配置差异有自动检查结果（脚本或测试）。
- [x] 新增/修改字段不会静默漂移。
- [x] 文档中有清晰的 profile 使用说明。

**执行证据（2026-02-19）**

- 新增检查脚本：`scripts/dev/check_model_profile_matrix.jl`
  - 检查域：`config/models/njl|pnjl|rpnjl`
  - 规则：`unittest` 仅允许覆盖 `default` 已存在字段；合并后必须具备关键参数字段。
- rPNJL unittest 覆盖关系显式化：新增 `config/models/rpnjl/unittest.toml`（空覆盖，语义为继承 default）。
- CI 接入：`.github/workflows/unit-smoke.yml` 新增 `Check model profile matrix` 步骤，并将 `config/**` 与脚本路径纳入触发。
- 文档更新：`docs/guides/configuration.md` 新增“当前项目 Profile 口径”与验证命令说明。

---

## P2（低优先级：治理优化与运营降噪）

### P2-1：active 文档池持续归档

- [x] 按周检查 active 文档是否超 60 天。
- [x] 对完成项执行归档并补齐 metadata。

**可执行脚本**

```powershell
julia --project=. scripts/dev/check_active_docs_governance.jl
julia --project=. scripts/dev/archive_docs.jl <filename.md>
```

**验收标准**

- [x] active 文档命名全部符合 `YYYY-MM-DD_*.md`。
- [x] 超期文档为 0 或有明确豁免说明。
- [x] archived 文档 frontmatter 完整可追溯。

**执行证据（2026-02-19）**

- 周期检查：`julia --project=. scripts/dev/check_active_docs_governance.jl` 通过（归档前/后均为 `OK`）。
- 已归档完成项（3 个）：
  - `2025-12-28_transport_optimization_analysis.md`
  - `2026-02-19_DEFAULT_SKIP转正记录.md`
  - `2026-02-19_P0-3_unit_scope_收敛记录.md`
- 归档校验：`julia --project=. scripts/dev/archive_docs.jl -c` 输出 `Valid: 61 / 61`。
- 归档后 active 池剩余 4 个在研文档，均符合命名规范且无超期。

---

### P2-2：输出目录口径彻底统一

- [x] 清理根目录 `outputs/` 的历史兼容依赖。
- [x] 统一所有脚本默认落盘到 `data/outputs/`。

**可执行脚本**

```powershell
julia --project=. scripts/relaxtime/run_gap_transport_scan.jl --help
```

**验收标准**

- [x] 新输出不再写入根级 `outputs/`。
- [x] 文档中仅保留 `data/outputs/` 作为默认口径。
- [x] 历史兼容路径有迁移说明。

**执行证据（2026-02-19）**

- 脚本默认路径修正：`scripts/debug/export_julia_baseline.jl`
  - `DEFAULT_RAW / DEFAULT_UNIFIED / DEFAULT_FORTRAN` 已从根级 `outputs/results` 迁移到 `data/outputs/results`。
- 命令可用性复核：`julia --project=. scripts/debug/export_julia_baseline.jl --help` 正常输出。
- 全量脚本扫描（`scripts/**/*.jl|py`）未发现新的根级 `outputs/results` 默认落盘配置（排除 `data/outputs` 后结果为空）。
- 迁移说明补齐：`docs/guides/configuration.md` 新增“9.3 输出目录迁移说明（root outputs -> data/outputs）”。

---

## 里程碑与打勾节奏（建议）

- [x] M1（1 周）：完成 P0-1、P0-3 的首轮收敛。
- [x] M2（2~3 周）：完成 P0-2 与 P1-1。
- [x] M3（4 周）：完成 P1-2、P1-3，并启动 P2 常态化治理。

## DoD（任务单完成定义）

- [x] P0 全部完成并在 CI 稳定通过。
- [x] P1 至少完成 2 项且有文档与测试证据。
- [x] P2 建立固定节奏（脚本化检查 + 周期执行记录）。
- [ ] 本任务单可归档到 `docs/dev/archived/`，并附执行证据链接。

---

## 专项任务单映射（去重）

### Transport 专项（已建立）

- 对应文档：`docs/dev/archived/2025-12-28_transport_optimization_analysis.md`
- 映射关系：
  - 本文 `P0-2 主链路固定点回归` ←→ Transport 专项中的“基线 smoke / fixed points / 容差治理”。
  - 本文 `P0-3 单元测试语义收敛` ←→ Transport 专项中的“输入校验单测、workflow smoke、bridge smoke”。
  - 本文 `P1（可维护性）` 部分条目 ←→ Transport 专项中的“共享积分主循环、热点微优化、API 口径一致性”。
- 执行规则：
  - Transport 细节变更优先在专项任务单打勾并记录证据。
  - 本总单只维护里程碑状态与跨模块协同，不重复拆解同一子任务。
