---
title: 相图自动化产线原生化执行台账
archived: true
original: docs/dev/active/2026-03-02_相图自动化产线原生化执行台账.md
archived_date: 2026-03-10
---


以下为原始内容（保留，以便审阅与历史参考）：

---

# 相图自动化产线原生化执行台账

更新日期：2026-03-02

---

## 0. 台账定位

- 本文档仅记录“执行事实”：变更点、命令、产物、结果。
- 本阶段开发任务定义统一维护在：
  - `docs/dev/active/2026-03-02_相图自动化产线原生化设计文档.md`

---

## 1. 记录规范（强约束）

- [x] 执行记录与开发任务分开保存：
  - 任务目标/范围/DoD 只写“开发任务单”；
  - 执行过程/命令/结果只写“执行台账”。
- [x] 追加记录时采用“直接追加”策略：
  - 不要求回读本台账历史上下文；
  - 仅按统一模板追加到文档末尾；
  - 每条记录必须自包含（含目标、命令、产物、结果）。
- [x] 每条记录必须可追溯到输出产物（`data/outputs/results/*`）。

---

## 2. 执行记录

- [ ] 批次号：Batch N1
  - 目标：完成PhaseCore原生算法迁移并打通CEP细化+最小产线验证
  - 代码变更：新增src/models/phase原生算法实现(detect_s_shape/maxwell_construction)、CEP二分细化、spinodal产物写盘、脚本薄化与Models入口接线，新增phase smoke测试并纳入unit-smoke
  - 验证命令：
    - `models/test_phase_core_algorithms_smoke.jl;models/test_phase_pipeline_smoke.jl='models/test_phase_core_algorithms_smoke.jl;models/test_phase_pipeline_smoke.jl'; julia --project=. tests/unit/runtests.jl`
    - `julia --project=. scripts/pnjl/calculate_phase_structure.jl --T_min=150 --T_max=150 --T_step=10 --rho_min=0.1 --rho_max=0.3 --rho_step=0.1 --solver_backend=legacy --output_dir=<tmp_dir>`
  - 产物：
    - `src/models/phase/PhaseCore.jl`
    - `src/models/phase/CEPDetector.jl`
    - `src/models/phase/PhasePipeline.jl`
    - `src/models/phase/PhaseArtifacts.jl`
    - `tests/unit/models/test_phase_core_algorithms_smoke.jl`
    - `tests/unit/models/test_phase_pipeline_smoke.jl`
    - `docs/dev/active/2026-03-02_相图自动化产线原生化设计文档.md`
  - 结果：通过：定向smoke 16/16，CLI最小调用可产出trho_scan.csv/first_order_boundary.csv/spinodal.csv/phase_summary.json
  - 主线映射（N1/N2/N3/N4）：N1
  - 备注：实现中发现JSON3严格模式不接受NaN，已在文档与代码中明确JSON缺失值写null

- [ ] 批次号：Batch N2
  - 目标：完成N2：crossover/report产物接入、晋升gate诊断增强、CEP二分前置条件回退策略
  - 代码变更：新增crossover_line构建与CSV导出、phase_report.md生成；run_phase_pipeline接入compute_crossover；promote gate增加schema/model/config_hash校验；CEPDetector增加非均匀rho网格与插值失败fallback
  - 验证命令：
    - `models/test_phase_core_algorithms_smoke.jl;models/test_phase_pipeline_smoke.jl='models/test_phase_core_algorithms_smoke.jl;models/test_phase_pipeline_smoke.jl'; julia --project=. tests/unit/runtests.jl`
    - `julia --project=. scripts/pnjl/calculate_phase_structure.jl --T_min=150 --T_max=150 --T_step=10 --rho_min=0.1 --rho_max=0.3 --rho_step=0.1 --solver_backend=legacy --compute_crossover --crossover_n_mu=3 --output_dir=<tmp_dir>`
  - 产物：
    - `src/models/phase/CrossoverLine.jl`
    - `src/models/phase/CEPDetector.jl`
    - `src/models/phase/PhasePipeline.jl`
    - `src/models/phase/PhaseArtifacts.jl`
    - `tests/unit/models/test_phase_core_algorithms_smoke.jl`
    - `tests/unit/models/test_phase_pipeline_smoke.jl`
    - `docs/dev/active/2026-03-02_相图自动化产线原生化设计文档.md`
  - 结果：通过：定向smoke 24/24，CLI可产出trho_scan.csv/first_order_boundary.csv/spinodal.csv/crossover_line.csv/phase_summary.json/phase_report.md
  - 主线映射（N1/N2/N3/N4）：N2
  - 备注：计划外问题：CEP二分依赖温度切片rho网格一致，已加入前置检测与fallback并在文档记录后续重采样方案

---

## 3. 收口说明

- 归档判断（2026-03-10）：本台账对应的实现批次已完成，且相关 smoke / regression / validation 证据已补齐。
- 后续 API 文档治理与目录收口不再继续写入本台账，转入独立任务单 `2026-03-10_相图API三层视图与导出索引治理任务单.md`。
