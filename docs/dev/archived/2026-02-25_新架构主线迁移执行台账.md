---
title: 新架构主线迁移执行台账
archived: true
original: docs/dev/active/2026-02-25_新架构主线迁移执行台账.md
archived_date: 2026-02-26
---


以下为原始内容（保留，以便审阅与历史参考）：

---

# 新架构主线迁移执行台账

更新日期：2026-02-25

---

## 0. 台账定位

- 本文档只记录批次执行事实（变更点、命令、产物、结果）。
- 主线任务定义与可勾选目标统一维护在：
  - `docs/dev/active/2026-02-25_新架构主线迁移开发任务单_v2.md`
- 历史执行记录（Batch 1–96）见：
  - `docs/dev/active/2026-02-21_新架构全量迁移实施任务单.md`

---

## 1. 记录模板

- [ ] 批次号：Batch N
  - 目标：
  - 代码变更：
  - 验证命令：
    - `julia --project=. scripts/dev/run_prune_wave_gate.jl --base HEAD --head HEAD`
    - `UNIT_FILES=models/test_models_unified_entrypoints_smoke.jl,pnjl/test_tmu_scan_solver_backend_models_smoke.jl,pnjl/test_trho_scan_solver_backend_models_smoke.jl julia --project=. tests/unit/runtests.jl`
  - 产物：
    - `outputs/results/pnjl_prune_wave_snapshot_<timestamp>.txt`
    - `outputs/results/models_invokelatest_allowlist_delta_<timestamp>.txt`
    - （可选）`outputs/results/pnjl_scan_default_include_audit_<timestamp>.txt`
    - （可选）`outputs/results/pnjl_decommission_checklist_<timestamp>.md`
  - 结果：
  - 主线映射（M1/M2/M3）：

---

## 2. 新周期记录（从 Batch 97 起）

- [x] Batch 97
  - 目标：M1（solver 主体迁移收口）
  - 代码变更：
    - `src/models/constraint_solver.jl` 新增 `default_mu0_from_seed(seed)`。
    - `src/pnjl/solver/ImplicitSolver.jl` 在 `FixedRho/FixedEntropy/FixedSigma/FixedAsymmetricRho` 的 models-native 路径复用 `models.default_mu0_from_seed(seed)`，减少 compat 侧重复初始化逻辑。
    - 清理 `ImplicitSolver` 中不再使用的 `Statistics.mean` 导入。
  - 验证命令：
    - `Remove-Item Env:UNIT_FILES -ErrorAction SilentlyContinue; julia --project=. scripts/dev/run_prune_wave_gate.jl --base HEAD --head HEAD`
    - `UNIT_FILES=models/test_models_unified_entrypoints_smoke.jl,pnjl/test_tmu_scan_solver_backend_models_smoke.jl,pnjl/test_trho_scan_solver_backend_models_smoke.jl julia --project=. tests/unit/runtests.jl`
  - 产物：
    - `outputs/results/pnjl_prune_wave_snapshot_20260225_112939.txt`
    - `outputs/results/models_invokelatest_allowlist_delta_20260225_112939.txt`
    - `outputs/results/pnjl_scan_default_include_audit_20260225_112939.txt`
  - 结果：
    - `models-invokelatest-audit=stable observed=7 allowlist_baseline=7 allowlisted=7`
    - `pnjl-scan-runtime-dependency-audit observed=0`
    - `pnjl-scan-default-include-audit observed=0`
    - 定向回归 `34/34` 通过。
    - prune gate 内置全量 smoke 复现既有 `models legacy adapter / meson workflow` 失败（非本批直接引入）。

- [x] Batch 98
  - 目标：M2（Thermodynamics 等价替代收口）
  - 代码变更：
    - `src/pnjl/core/Thermodynamics.jl` 移除历史 `include("Integrals.jl")`，保持热力学 API 通过 `ThermoFacade / PNJL.Integrals` 路径提供，减少 `Thermodynamics` 模块耦合。
  - 验证命令：
    - `Remove-Item Env:UNIT_FILES -ErrorAction SilentlyContinue; julia --project=. scripts/dev/run_prune_wave_gate.jl --base HEAD --head HEAD`
    - `UNIT_FILES=models/test_models_unified_entrypoints_smoke.jl,pnjl/test_tmu_scan_solver_backend_models_smoke.jl,pnjl/test_trho_scan_solver_backend_models_smoke.jl julia --project=. tests/unit/runtests.jl`
  - 产物：
    - `outputs/results/pnjl_prune_wave_snapshot_20260225_113534.txt`
    - `outputs/results/models_invokelatest_allowlist_delta_20260225_113534.txt`
    - `outputs/results/pnjl_scan_default_include_audit_20260225_113534.txt`
  - 结果：
    - `models-invokelatest-audit=stable observed=7 allowlist_baseline=7 allowlisted=7`
    - `pnjl-scan-runtime-dependency-audit observed=0`
    - `pnjl-scan-default-include-audit observed=0`
    - 定向回归 `34/34` 通过。
    - prune gate 内置全量 smoke 复现既有 `models legacy adapter / meson workflow` 失败（非本批直接引入）。

- [x] Batch 99
  - 目标：M3（Wave-3 下线前置证据补齐）
  - 代码变更：
    - `scripts/dev/generate_pnjl_decommission_checklist.jl` 新增 `PNJL.solve/PNJL.solve_multi` 外部调用审计（排除 `src/pnjl/**`）。
    - 检查表新增 `1.5` 小节并输出 `pnjl solver callers (external)` 计数。
    - `scripts/dev/README.md` 同步新增审计项说明。
  - 验证命令：
    - `julia --project=. scripts/dev/generate_pnjl_decommission_checklist.jl --base HEAD --head HEAD`
    - `Remove-Item Env:UNIT_FILES -ErrorAction SilentlyContinue; julia --project=. scripts/dev/run_prune_wave_gate.jl --base HEAD --head HEAD`
    - `UNIT_FILES=models/test_models_unified_entrypoints_smoke.jl,pnjl/test_tmu_scan_solver_backend_models_smoke.jl,pnjl/test_trho_scan_solver_backend_models_smoke.jl julia --project=. tests/unit/runtests.jl`
  - 产物：
    - `outputs/results/pnjl_decommission_checklist_20260225_114237.md`
    - `outputs/results/pnjl_prune_wave_snapshot_20260225_114253.txt`
    - `outputs/results/models_invokelatest_allowlist_delta_20260225_114253.txt`
    - `outputs/results/pnjl_scan_default_include_audit_20260225_114253.txt`
  - 结果：
    - 检查表摘要：`pnjl solver callers (external)=13`。
    - `models-invokelatest-audit=stable observed=7 allowlist_baseline=7 allowlisted=7`
    - `pnjl-scan-runtime-dependency-audit observed=0`
    - `pnjl-scan-default-include-audit observed=0`
    - 定向回归 `34/34` 通过。
    - prune gate 内置全量 smoke 复现既有 `models legacy adapter / meson workflow` 失败（非本批直接引入）。

- [x] Batch 100
  - 目标：M3（调用方迁移清单收口）
  - 代码变更：
    - `scripts/pnjl/random_sample_gap_points.jl` 将单点平衡求解从 `PNJL.solve(FixedMu)` 切换为 `Models.solve_fixedmu_constraint`。
    - 保留 `PNJL` seed 生成策略以减少行为偏移（`DefaultSeed + get_seed`），求解执行迁移到 models。
  - 验证命令：
    - `N_SAMPLES=1 STOP_ON_FIRST=0 julia --project=. scripts/pnjl/random_sample_gap_points.jl`
    - `Remove-Item Env:UNIT_FILES -ErrorAction SilentlyContinue; julia --project=. scripts/dev/run_prune_wave_gate.jl --base HEAD --head HEAD`
    - `UNIT_FILES=models/test_models_unified_entrypoints_smoke.jl,pnjl/test_tmu_scan_solver_backend_models_smoke.jl,pnjl/test_trho_scan_solver_backend_models_smoke.jl julia --project=. tests/unit/runtests.jl`
  - 产物：
    - `outputs/results/pnjl_prune_wave_snapshot_20260225_115416.txt`
    - `outputs/results/models_invokelatest_allowlist_delta_20260225_115416.txt`
    - `outputs/results/pnjl_scan_default_include_audit_20260225_115416.txt`
  - 结果：
    - 脚本冒烟通过（`converged 1/1`，异常 0）。
    - `models-invokelatest-audit=stable 7/7/7`，`pnjl-scan-runtime-dependency-audit=0`，`pnjl-scan-default-include-audit=0`。
    - 定向回归 `34/34` 通过。
    - prune gate 内置全量 smoke 复现既有 `models legacy adapter / meson workflow` 失败（非本批直接引入）。

- [x] Batch 101
  - 目标：M3（调用方迁移清单收口）
  - 代码变更：
    - `scripts/relaxtime/scan_total_cross_section.jl` 将平衡点求解从 `PNJL.solve(FixedMu)` 切换为 `Models.solve_gap(...; solver_backend=:models)`。
    - 质量向量改由 `Models.calculate_mass_vec` 生成。
  - 验证命令：
    - `julia --project=. scripts/relaxtime/scan_total_cross_section.jl --process udbar_to_udbar --sqrt-s-min 0.2 --sqrt-s-max 0.25 --sqrt-s-steps 2 --n-points 8 --output-csv outputs/results/batch101_sigma_scan.csv --output-fig outputs/figures/batch101_sigma_scan.png`
    - `Remove-Item Env:UNIT_FILES -ErrorAction SilentlyContinue; julia --project=. scripts/dev/run_prune_wave_gate.jl --base HEAD --head HEAD`
    - `UNIT_FILES=models/test_models_unified_entrypoints_smoke.jl,pnjl/test_tmu_scan_solver_backend_models_smoke.jl,pnjl/test_trho_scan_solver_backend_models_smoke.jl julia --project=. tests/unit/runtests.jl`
  - 产物：
    - `outputs/results/batch101_sigma_scan.csv`
    - `outputs/figures/batch101_sigma_scan.png`
    - `outputs/results/pnjl_prune_wave_snapshot_20260225_120012.txt`
    - `outputs/results/models_invokelatest_allowlist_delta_20260225_120012.txt`
    - `outputs/results/pnjl_scan_default_include_audit_20260225_120012.txt`
  - 结果：
    - 脚本冒烟通过（成功产出 CSV 与图）。
    - `models-invokelatest-audit=stable 7/7/7`，`pnjl-scan-runtime-dependency-audit=0`，`pnjl-scan-default-include-audit=0`。
    - 定向回归 `34/34` 通过。
    - prune gate 内置全量 smoke 复现既有 `models legacy adapter / meson workflow` 失败（非本批直接引入）。

- [x] Batch 102
  - 目标：M1 + M3（solver 兼容内核再下沉 + 下线证据跟踪）
  - 代码变更：
    - `src/models/constraint_solver.jl` 新增 `default_muvec0_from_seed(seed)`。
    - `src/pnjl/solver/ImplicitSolver.jl` 的 `FixedAsymmetricRho` models-native 路径复用该 helper，减少 compat 侧 seed 展开逻辑。
    - `scripts/dev/generate_pnjl_decommission_checklist.jl` 与 `scripts/dev/README.md` 保持的 `PNJL.solve/solve_multi` 外部调用审计继续生效；本批用于验证调用计数收敛。
  - 验证命令：
    - `julia --project=. scripts/dev/generate_pnjl_decommission_checklist.jl --base HEAD --head HEAD`
    - `Remove-Item Env:UNIT_FILES -ErrorAction SilentlyContinue; julia --project=. scripts/dev/run_prune_wave_gate.jl --base HEAD --head HEAD`
    - `UNIT_FILES=models/test_models_unified_entrypoints_smoke.jl,pnjl/test_tmu_scan_solver_backend_models_smoke.jl,pnjl/test_trho_scan_solver_backend_models_smoke.jl julia --project=. tests/unit/runtests.jl`
  - 产物：
    - `outputs/results/pnjl_decommission_checklist_20260225_120748.md`
    - `outputs/results/pnjl_prune_wave_snapshot_20260225_120759.txt`
    - `outputs/results/models_invokelatest_allowlist_delta_20260225_120759.txt`
    - `outputs/results/pnjl_scan_default_include_audit_20260225_120759.txt`
  - 结果：
    - 检查表摘要：`pnjl solver callers (external)=11`（由上批 13 下降）。
    - `models-invokelatest-audit=stable 7/7/7`，`pnjl-scan-runtime-dependency-audit=0`，`pnjl-scan-default-include-audit=0`。
    - 定向回归 `34/34` 通过。
    - prune gate 内置全量 smoke 复现既有 `models legacy adapter / meson workflow` 失败（非本批直接引入）。

- [x] Batch 100/101 补充修正（同轮内）
  - 目标：消除脚本迁移后重复加载 `Models` 导致的运行时噪声（`WARNING: replacing module Models`）。
  - 代码变更：
    - `scripts/pnjl/random_sample_gap_points.jl`：移除重复 `include Models.jl`，改为复用 `const Models = Main.Models`。
    - `scripts/relaxtime/scan_total_cross_section.jl`：移除重复 `include Models.jl`，改为复用 `const Models = Main.Models`。
  - 验证命令：
    - `N_SAMPLES=1 STOP_ON_FIRST=0 julia --project=. scripts/pnjl/random_sample_gap_points.jl`
    - `julia --project=. scripts/relaxtime/scan_total_cross_section.jl --process udbar_to_udbar --sqrt-s-min 0.2 --sqrt-s-max 0.25 --sqrt-s-steps 2 --n-points 8 --output-csv outputs/results/batch101_sigma_scan.csv --output-fig outputs/figures/batch101_sigma_scan.png`
  - 结果：
    - 两个脚本均正常执行，且不再出现 `replacing module Models` 警告。

- [x] Batch 106
  - 目标：M3（调用方迁移清单收口）
  - 代码变更：
    - `scripts/dev/profile_paramtypes_hotspots.jl` 将两处显式 `TransportWorkflow.PNJL.solve(FixedMu)` 基线求解替换为 `TransportWorkflow.EquilibriumFacade.solve_equilibrium_backend(...; thermo_backend=:models, solver_backend=:models)`。
    - 保持原 profiling 结构不变，仅更新基线求解来源与结果标签文案（`equilibrium solve baseline`）。
  - 验证命令：
    - `julia --project=. scripts/dev/profile_paramtypes_hotspots.jl`
    - `julia --project=. scripts/dev/generate_pnjl_decommission_checklist.jl --base HEAD --head HEAD`
    - `Remove-Item Env:UNIT_FILES -ErrorAction SilentlyContinue; julia --project=. scripts/dev/run_prune_wave_gate.jl --base HEAD --head HEAD`
    - `$env:UNIT_FILES='models/test_models_unified_entrypoints_smoke.jl,pnjl/test_tmu_scan_solver_backend_models_smoke.jl,pnjl/test_trho_scan_solver_backend_models_smoke.jl'; julia --project=. tests/unit/runtests.jl`
  - 产物：
    - `outputs/results/pnjl_decommission_checklist_20260225_134022.md`
    - `outputs/results/pnjl_prune_wave_snapshot_20260225_134027.txt`
    - `outputs/results/models_invokelatest_allowlist_delta_20260225_134027.txt`
    - `outputs/results/pnjl_scan_default_include_audit_20260225_134027.txt`
  - 结果：
    - profiling 脚本执行通过。
    - 检查表摘要：`pnjl solver callers (external)=7`（由上批 11 继续下降）。
    - `models-invokelatest-audit=stable 7/7/7`，`pnjl-scan-runtime-dependency-audit=0`，`pnjl-scan-default-include-audit=0`。
    - 定向回归 `34/34` 通过。
    - prune gate 内置全量 smoke 复现既有 `models legacy adapter / meson workflow` 失败（非本批直接引入）。

- [x] Batch 107
  - 目标：M3（调用方迁移清单收口）
  - 代码变更：
    - `scripts/relaxtime/run_gap_transport_scan.jl` 将基线平衡求解从 `TransportWorkflow.PNJL.solve(FixedMu)` 迁移到 `TransportWorkflow.EquilibriumFacade.solve_equilibrium_backend(...; thermo_backend=:models, solver_backend=:models)`。
    - 相位跟踪 `update!` 从 `base.solution` 对齐为 `base.x_state`；连续性初值改为“首点 `HADRON_SEED_5`，后续复用 `previous_solution`”。
    - 修复脚本内旧命名空间导入：`using .EffectiveCouplings.OneLoopIntegrals: A` → `using Main.OneLoopIntegrals: A`。
    - 适配最小平衡态结构：通过 `TransportWorkflow.ThermoFacade.calculate_thermo_backend/calculate_omega_backend` 重建 `pressure/entropy/energy/omega` 输出列。
  - 验证命令：
    - `julia --project=. scripts/relaxtime/run_gap_transport_scan.jl --tmin 150 --tmax 150 --tstep 10 --mubmin 800 --mubmax 800 --mubstep 100 --xi-list 0.0 --p-num 8 --t-num 4 --tau-p-nodes 4 --tau-angle-nodes 2 --tau-phi-nodes 2 --tau-n-sigma 4 --tr-p-nodes 8 --tr-p-max 6.0 --max-iter 30 --overwrite --output outputs/results/batch107_gap_transport_scan.csv`
  - 产物：
    - `outputs/results/batch107_gap_transport_scan.csv`
  - 结果：
    - 单点扫描冒烟通过（CSV 成功产出）。

- [x] Batch 108
  - 目标：M3（调用方迁移清单收口）
  - 代码变更：
    - `scripts/relaxtime/scan_transport_vs_xi_T150_muB800.jl` 将基线平衡求解从 `TransportWorkflow.PNJL.solve(FixedMu)` 迁移到 `TransportWorkflow.EquilibriumFacade.solve_equilibrium_backend(...; thermo_backend=:models, solver_backend=:models)`。
    - `seed_state` 连续性由 `base.solution` 对齐为 `base.x_state`。
    - 修复脚本内旧命名空间导入：`using .EffectiveCouplings.OneLoopIntegrals: A` → `using Main.OneLoopIntegrals: A`。
  - 验证命令：
    - `julia --project=. scripts/relaxtime/scan_transport_vs_xi_T150_muB800.jl --xi0-only --p-num 8 --t-num 4 --tau-p-nodes 4 --tau-angle-nodes 2 --tau-phi-nodes 2 --tau-n-sigma 4 --tr-p-nodes 8 --tr-p-max 6.0 --max-iter 30 --overwrite --output outputs/results/batch108_transport_vs_xi.csv`
  - 产物：
    - `outputs/results/batch108_transport_vs_xi.csv`
  - 结果：
    - 单点扫描冒烟通过（CSV 成功产出）。

- [x] Batch 109
  - 目标：M3（调用方迁移清单收口）
  - 代码变更：
    - `scripts/pnjl/find_multiseed_failure_points.jl` 将外部 `PNJL.solve_multi(FixedMu)` 调用迁移为 `EquilibriumFacade.solve_equilibrium_backend(...; thermo_backend=:models, solver_backend=:models)`。
    - 保持脚本采样/统计结构不变，仅替换平衡求解入口；补充加载 `src/pnjl/core/EquilibriumFacade.jl`。
  - 验证命令：
    - `$env:N_SAMPLES='1'; $env:STOP_ON_FIRST='0'; $env:P_NUM='8'; $env:T_NUM='4'; julia --project=. scripts/pnjl/find_multiseed_failure_points.jl`
  - 结果：
    - 冒烟通过（`ok 1/1`，`fail 0`）。

- [x] Batch 110
  - 目标：M3（调用方迁移清单收口）
  - 代码变更：
    - `tests/unit/relaxtime/test_workflow_paramtypes_validation_smoke.jl`：基线求解从 `TransportWorkflow.PNJL.solve` 切到 `TransportWorkflow.EquilibriumFacade.solve_equilibrium_backend(...; thermo_backend=:models, solver_backend=:models)`。
    - `tests/unit/relaxtime/test_workflow_paramtypes_mixedmode_smoke.jl`：同上。
    - `tests/unit/relaxtime/test_workflow_paramtypes_equivalence_smoke.jl`：同上。
  - 验证命令：
    - `$env:UNIT_FILES='relaxtime/test_workflow_paramtypes_validation_smoke.jl,relaxtime/test_workflow_paramtypes_mixedmode_smoke.jl,relaxtime/test_workflow_paramtypes_equivalence_smoke.jl'; julia --project=. tests/unit/runtests.jl`
  - 结果：
    - 三组 smoke 通过（`22/22`）。

- [x] Batch 111
  - 目标：M3（调用方迁移清单收口）
  - 代码变更：
    - `tests/unit/relaxtime/test_transport_workflow.jl`：第二组基线求解从 `TransportWorkflow.PNJL.solve` 切到 `TransportWorkflow.EquilibriumFacade.solve_equilibrium_backend(...; thermo_backend=:models, solver_backend=:models)`。
    - `tests/analysis/convergence/run_convergence.jl`：基线求解从 `TransportWorkflow.PNJL.solve` 切到 `TransportWorkflow.EquilibriumFacade.solve_equilibrium_backend(...; thermo_backend=:models, solver_backend=:models)`；`seed_state` 由 `base.solution` 对齐为 `base.x_state`；`entropy` 改为通过 `TransportWorkflow.ThermoFacade.calculate_thermo_backend` 获取。
    - `tests/analysis/convergence/run_convergence.jl` 修复旧命名空间导入：`using .EffectiveCouplings.OneLoopIntegrals: A` → `using Main.OneLoopIntegrals: A`。
  - 验证命令：
    - `julia --project=. tests/analysis/convergence/run_convergence.jl`
    - `julia --project=. scripts/dev/generate_pnjl_decommission_checklist.jl --base HEAD --head HEAD`
    - `Remove-Item Env:UNIT_FILES -ErrorAction SilentlyContinue; julia --project=. scripts/dev/run_prune_wave_gate.jl --base HEAD --head HEAD`
    - `$env:UNIT_FILES='models/test_models_unified_entrypoints_smoke.jl,pnjl/test_tmu_scan_solver_backend_models_smoke.jl,pnjl/test_trho_scan_solver_backend_models_smoke.jl'; julia --project=. tests/unit/runtests.jl`
  - 产物：
    - `outputs/results/pnjl_decommission_checklist_20260225_141124.md`
    - `outputs/results/pnjl_prune_wave_snapshot_20260225_141128.txt`
    - `outputs/results/models_invokelatest_allowlist_delta_20260225_141128.txt`
    - `outputs/results/pnjl_scan_default_include_audit_20260225_141128.txt`
  - 结果：
    - 收敛分析回归通过（`Convergence (RelaxTime) 140/140`）。
    - 检查表摘要：`pnjl solver callers (external)=0`。
    - `models-invokelatest-audit=stable 7/7/7`，`pnjl-scan-runtime-dependency-audit=0`，`pnjl-scan-default-include-audit=0`。
    - 定向回归 `34/34` 通过。
    - prune gate 内置全量 smoke 复现既有 `models legacy adapter / meson workflow` 失败（非本批直接引入）。

- [x] Batch 112
  - 目标：M3（调用方迁移清单收口）
  - 代码变更：
    - `tests/unit/pnjl/test_scans.jl`：将第 1 处 `PNJL.run_tmu_scan(...)` 调用迁移为 `Models.run_tmu_scan(...)`。
    - 同文件补充 `Models` 加载后的 `PNJL` 兜底句柄（`Main.PNJL`）以保证 scan loader 相关断言可用。
  - 验证命令：
    - `$env:UNIT_FILES='pnjl/test_scans.jl'; julia --project=. tests/unit/runtests.jl`
  - 结果：
    - `test_scans` 通过（`43/43`）。

- [x] Batch 113
  - 目标：M3（调用方迁移清单收口）
  - 代码变更：
    - `tests/unit/pnjl/test_scans.jl`：将第 2 处 `PNJL.run_tmu_scan(...)` 调用迁移为 `Models.run_tmu_scan(...)`。
    - 同文件 `TmuScan` 相关 CSV 断言改为按列名定位，移除对固定列总数的脆弱假设。
  - 验证命令：
    - `$env:UNIT_FILES='pnjl/test_scans.jl'; julia --project=. tests/unit/runtests.jl`
  - 结果：
    - `test_scans` 通过（`43/43`）。

- [x] Batch 114
  - 目标：M3（调用方迁移清单收口）
  - 代码变更：
    - `tests/unit/pnjl/test_scans.jl`：将 `PNJL.run_trho_scan(...)` 调用迁移为 `Models.run_trho_scan(...)`。
    - `TrhoScan defaults` 断言改为显式读取 `PNJL.TrhoScan.DEFAULT_RHO_VALUES`。
    - 本轮完成后，外部 `PNJL.run_tmu_scan/run_trho_scan` 调用计数清零。
  - 验证命令：
    - `julia --project=. scripts/dev/generate_pnjl_decommission_checklist.jl --base HEAD --head HEAD`
    - `Remove-Item Env:UNIT_FILES -ErrorAction SilentlyContinue; julia --project=. scripts/dev/run_prune_wave_gate.jl --base HEAD --head HEAD`
    - `$env:UNIT_FILES='models/test_models_unified_entrypoints_smoke.jl,pnjl/test_tmu_scan_solver_backend_models_smoke.jl,pnjl/test_trho_scan_solver_backend_models_smoke.jl'; julia --project=. tests/unit/runtests.jl`
  - 产物：
    - `outputs/results/pnjl_decommission_checklist_20260225_144850.md`
    - `outputs/results/pnjl_prune_wave_snapshot_20260225_145213.txt`
    - `outputs/results/models_invokelatest_allowlist_delta_20260225_145213.txt`
    - `outputs/results/pnjl_scan_default_include_audit_20260225_145213.txt`
  - 结果：
    - 检查表摘要：`pnjl callers=0`，`pnjl solver callers (external)=0`。
    - `models-invokelatest-audit=stable 7/7/7`，`pnjl-scan-runtime-dependency-audit=0`，`pnjl-scan-default-include-audit=0`。
    - 定向回归 `34/34` 通过。
    - prune gate 内置全量 smoke 复现既有 `models legacy adapter / meson workflow` 失败（非本批直接引入）。

- [x] Batch 115
  - 目标：M1（solver 主体迁移收口）
  - 代码变更：
    - 新增 `outputs/results/batch115_solver_core_inventory_20260225_162352.md`，盘点 `src/pnjl/solver/ImplicitSolver.jl` 仍承载的核心求解函数，并补充主线调用证据。
  - 验证命令：
    - `$env:UNIT_FILES='pnjl/test_solver_constraints_models_backend_smoke.jl,pnjl/test_tmu_scan_solver_backend_models_smoke.jl,pnjl/test_trho_scan_solver_backend_models_smoke.jl'; julia --project=. tests/unit/runtests.jl`
    - `$env:UNIT_FILES='models/test_pnjl_solve_gap_backend_switch_smoke.jl,models/test_pnjl_thermo_bridge_multipoint_smoke.jl'; julia --project=. tests/unit/runtests.jl`
  - 产物：
    - `outputs/results/batch115_solver_core_inventory_20260225_162352.md`
  - 结果：
    - M1 验收 1：`70/70` 通过。
    - M1 验收 2：`108/108` 通过。

- [x] Batch 116
  - 目标：M2（Thermodynamics 等价替代收口）
  - 代码变更：
    - 新增 `outputs/results/batch116_thermodynamics_models_equiv_inventory_20260225_162352.md`，固化 `src/pnjl/core/Thermodynamics.jl` 可等价替代函数名单与 `ThermoFacade` 分发映射。
  - 验证命令：
    - `$env:UNIT_FILES='pnjl/test_core_thermodynamics.jl,pnjl/test_magnetic_omega_components.jl,models/test_pnjl_thermo_bridge_multipoint_smoke.jl'; julia --project=. tests/unit/runtests.jl`
  - 产物：
    - `outputs/results/batch116_thermodynamics_models_equiv_inventory_20260225_162352.md`
  - 结果：
    - M2 验收：`125/125` 通过。
    - 出现既有 `import` 名称冲突 warning（非本批引入，不影响通过）。

- [x] Batch 117
  - 目标：M3（Wave-3 下线前置）
  - 代码变更：
    - 新增 `outputs/results/batch117_pnjl_compat_surface_assessment_20260225_162352.md`，评估 `src/pnjl/PNJL.jl` 最终保留面并形成可下线准入条件。
  - 验证命令：
    - `julia --project=. scripts/dev/generate_pnjl_decommission_checklist.jl --base HEAD --head HEAD`
    - `Remove-Item Env:UNIT_FILES -ErrorAction SilentlyContinue; julia --project=. scripts/dev/run_prune_wave_gate.jl --base HEAD --head HEAD`
    - `$env:UNIT_FILES='models/test_models_unified_entrypoints_smoke.jl,pnjl/test_tmu_scan_solver_backend_models_smoke.jl,pnjl/test_trho_scan_solver_backend_models_smoke.jl'; julia --project=. tests/unit/runtests.jl`
  - 产物：
    - `outputs/results/batch117_pnjl_compat_surface_assessment_20260225_162352.md`
    - `outputs/results/pnjl_decommission_checklist_20260225_162828.md`
    - `outputs/results/pnjl_prune_wave_snapshot_20260225_162850.txt`
    - `outputs/results/models_invokelatest_allowlist_delta_20260225_162850.txt`
    - `outputs/results/pnjl_scan_default_include_audit_20260225_162850.txt`
  - 结果：
    - 检查表摘要：`pnjl callers=0`，`pnjl solver callers (external)=0`。
    - 定向回归 `34/34` 通过。
    - prune gate 审计指标稳定（`7/7/7`、runtime dep=0、default include=0）；内置全量 smoke 复现既有 `models legacy adapter / meson workflow` 失败（非本批直接引入）。

- [x] Batch 118
  - 目标：M1（solver 主体迁移收口）
  - 代码变更：
    - `src/pnjl/core/EquilibriumFacade.jl` 新增统一入口：
      - `solve_with_derivatives_backend`
      - `solve_multi_mode_backend`
      - `create_implicit_solver_backend`
    - 与既有 `solve_equilibrium_backend/solve_mode_backend/solve_weighted_block_backend` 形成候选函数级 Facade 映射。
  - 验证命令：
    - `$env:UNIT_FILES='pnjl/test_solver_constraints_models_backend_smoke.jl,pnjl/test_tmu_scan_solver_backend_models_smoke.jl,pnjl/test_trho_scan_solver_backend_models_smoke.jl'; julia --project=. tests/unit/runtests.jl`
    - `$env:UNIT_FILES='pnjl/test_solver_constraints_models_backend_smoke.jl,pnjl/test_tmu_scan_solver_backend_models_smoke.jl,pnjl/test_trho_scan_solver_backend_models_smoke.jl,pnjl/test_phase_transition.jl'; julia --project=. tests/unit/runtests.jl`
  - 产物：
    - `outputs/results/batch118_solver_wrapper_closure_20260225_165513.md`
  - 结果：
    - `70/70`、`97/97` 通过。

- [x] Batch 119
  - 目标：M1（analysis 路径收敛）
  - 代码变更：
    - `src/pnjl/analysis/PhaseTransition.jl`：
      - 移除 `ImplicitSolver/Conditions` 直接 include。
      - `detect_crossover` 导数求解改走 `EquilibriumFacade.solve_with_derivatives_backend(...; thermo_backend=:models, solver_backend=:models)`。
      - crossover 点密度求解改走 `EquilibriumFacade.solve_equilibrium_backend(...; thermo_backend=:models, solver_backend=:models)` + `ThermoFacade.calculate_thermo_backend`。
  - 验证命令：
    - `$env:UNIT_FILES='pnjl/test_phase_transition.jl'; julia --project=. tests/unit/runtests.jl`
  - 产物：
    - `outputs/results/batch119_phase_transition_facade_migration_20260225_165513.md`
  - 结果：
    - `27/27` 通过。

- [x] Batch 120
  - 目标：M1 + M3（兼容内核依赖清理 + 下线前置复核）
  - 代码变更：
    - `src/pnjl/scans/ScanCommon.jl`：移除未使用 `using ..ImplicitSolver: SolverResult`。
  - 验证命令：
    - `julia --project=. scripts/dev/generate_pnjl_decommission_checklist.jl --base HEAD --head HEAD`
    - `Remove-Item Env:UNIT_FILES -ErrorAction SilentlyContinue; julia --project=. scripts/dev/run_prune_wave_gate.jl --base HEAD --head HEAD`
    - `$env:UNIT_FILES='models/test_models_unified_entrypoints_smoke.jl,pnjl/test_tmu_scan_solver_backend_models_smoke.jl,pnjl/test_trho_scan_solver_backend_models_smoke.jl'; julia --project=. tests/unit/runtests.jl`
  - 产物：
    - `outputs/results/batch120_scan_dependency_cleanup_20260225_165513.md`
    - `outputs/results/pnjl_decommission_checklist_20260225_165908.md`
    - `outputs/results/pnjl_prune_wave_snapshot_20260225_165937.txt`
    - `outputs/results/models_invokelatest_allowlist_delta_20260225_165937.txt`
    - `outputs/results/pnjl_scan_default_include_audit_20260225_165937.txt`
  - 结果：
    - 检查表摘要：`pnjl callers=0`，`pnjl solver callers (external)=0`。
    - 定向回归 `34/34` 通过。
    - prune gate 审计稳定（`7/7/7`、runtime dep=0、default include=0）；内置全量 smoke 复现既有 `models legacy adapter / meson workflow` 失败（非本批引入）。

- [x] Batch 121
  - 目标：M1（scans 对 solver 内核类型耦合收敛）
  - 代码变更：
    - `src/pnjl/scans/ScanResultFinalize.jl`：移除 `using ..ImplicitSolver: SolverResult`，改为字段协议结果克隆 `_clone_scan_result`。
    - `finalize_solver_result` 与 `promote_near_converged` 不再依赖 `SolverResult` 具体类型。
  - 验证命令：
    - `$env:UNIT_FILES='pnjl/test_tmu_scan_smoke.jl,pnjl/test_scans.jl'; julia --project=. tests/unit/runtests.jl`
  - 产物：
    - `outputs/results/batch121_scan_finalize_decouple_20260225_173517.md`
  - 结果：
    - 扫描烟测与核心扫描回归通过（`55/55`）。

- [x] Batch 122
  - 目标：M1（TmuScan 主路径收敛 + Facade 契约修复）
  - 代码变更：
    - `src/pnjl/scans/TmuScan.jl`：移除 `SolverResult` 类型依赖，统一返回字段化 scan result。
    - `src/pnjl/core/EquilibriumFacade.jl`：`solve_mode_backend/solve_multi_mode_backend` 新增 `mu_fm` 关键字以支持 `FixedMu`。
    - `TmuScan` legacy 路径调用 `solve_mode_backend(FixedMu(), ...; mu_fm=μ_fm)`，修复 `MethodError`。
  - 验证命令：
    - `$env:UNIT_FILES='pnjl/test_tmu_scan_smoke.jl,pnjl/test_scans.jl'; julia --project=. tests/unit/runtests.jl`
    - `$env:UNIT_FILES='pnjl/test_tmu_scan_solver_backend_models_smoke.jl,pnjl/test_trho_scan_solver_backend_models_smoke.jl,pnjl/test_phase_transition.jl,pnjl/test_solver_constraints_models_backend_smoke.jl'; julia --project=. tests/unit/runtests.jl`
  - 产物：
    - `outputs/results/batch122_tmu_scan_facade_fixedmu_20260225_173517.md`
  - 结果：
    - 回归通过（`55/55`、`97/97`）。

- [x] Batch 123
  - 目标：M1 + M3（scans/workflows 最终依赖收敛证据 + 门禁复核）
  - 代码变更：
    - `src/pnjl/scans/DualBranchScan.jl`：移除 `using ..ImplicitSolver: SolverResult` 与相关类型注解。
    - `src/pnjl/workflows/TransportWorkflow.jl`：工作流说明更新为 `EquilibriumFacade.solve_equilibrium_backend(...)` 入口。
    - `src/pnjl/scans/*.jl` 内对 `ImplicitSolver` 直接引用清零（代码检索结果）。
  - 验证命令：
    - `$env:UNIT_FILES='pnjl/test_solver_constraints_models_backend_smoke.jl,pnjl/test_tmu_scan_solver_backend_models_smoke.jl,pnjl/test_trho_scan_solver_backend_models_smoke.jl'; julia --project=. tests/unit/runtests.jl`
    - `$env:UNIT_FILES='models/test_pnjl_solve_gap_backend_switch_smoke.jl,models/test_pnjl_thermo_bridge_multipoint_smoke.jl'; julia --project=. tests/unit/runtests.jl`
    - `julia --project=. scripts/dev/generate_pnjl_decommission_checklist.jl --base HEAD --head HEAD`
    - `Remove-Item Env:UNIT_FILES -ErrorAction SilentlyContinue; julia --project=. scripts/dev/run_prune_wave_gate.jl --base HEAD --head HEAD`
  - 产物：
    - `outputs/results/batch123_scan_workflow_solver_detach_20260225_173517.md`
    - `outputs/results/pnjl_decommission_checklist_20260225_173129.md`
    - `outputs/results/pnjl_prune_wave_snapshot_20260225_173149.txt`
    - `outputs/results/models_invokelatest_allowlist_delta_20260225_173149.txt`
    - `outputs/results/pnjl_scan_default_include_audit_20260225_173149.txt`
  - 结果：
    - M1 验收两条命令分别 `70/70`、`108/108` 通过。
    - 检查表摘要：`pnjl callers=0`，`pnjl solver callers (external)=0`。
    - prune gate 审计稳定（`7/7/7`、runtime dep=0、default include=0）；内置全量 smoke 复现既有 `models legacy adapter / meson workflow` 失败（非本批引入）。

- [x] Batch 124
  - 目标：M1（compat 内核可替代实现清理）
  - 代码变更：
    - `src/pnjl/solver/ImplicitSolver.jl`：
      - 新增 `_default_seed_strategy`、`_solve_with_single_seed`。
      - `solve_multi(::FixedMu/::FixedRho/::FixedAsymmetricRho)` 统一调用 helper，清理重复求解胶水实现。
  - 验证命令：
    - `$env:UNIT_FILES='pnjl/test_solver_implicit.jl,pnjl/test_solver_constraints_models_backend_smoke.jl,pnjl/test_tmu_scan_solver_backend_models_smoke.jl,pnjl/test_trho_scan_solver_backend_models_smoke.jl'; julia --project=. tests/unit/runtests.jl`
  - 产物：
    - `outputs/results/batch124_solver_multiseed_dedup_20260225_175525.md`
  - 结果：
    - `137/137` 通过。

- [x] Batch 125
  - 目标：M1（compat fallback 薄层化）
  - 代码变更：
    - `src/pnjl/solver/ImplicitSolver.jl`：提取 `_try_solve_fixed_asymmetric_from_seed`，统一 `solve_weighted_block_fallback` 的早停/最终求解重复段。
  - 验证命令：
    - `$env:UNIT_FILES='pnjl/test_tmu_scan_smoke.jl,pnjl/test_scans.jl,pnjl/test_phase_transition.jl'; julia --project=. tests/unit/runtests.jl`
  - 产物：
    - `outputs/results/batch125_weighted_fallback_thinlayer_20260225_175525.md`
  - 结果：
    - `82/82` 通过。

- [x] Batch 126
  - 目标：M1 收口验收（最后勾选项）
  - 验证命令：
    - `$env:UNIT_FILES='pnjl/test_solver_constraints_models_backend_smoke.jl,pnjl/test_tmu_scan_solver_backend_models_smoke.jl,pnjl/test_trho_scan_solver_backend_models_smoke.jl'; julia --project=. tests/unit/runtests.jl`
    - `$env:UNIT_FILES='models/test_pnjl_solve_gap_backend_switch_smoke.jl,models/test_pnjl_thermo_bridge_multipoint_smoke.jl'; julia --project=. tests/unit/runtests.jl`
    - `julia --project=. scripts/dev/generate_pnjl_decommission_checklist.jl --base HEAD --head HEAD`
    - `Remove-Item Env:UNIT_FILES -ErrorAction SilentlyContinue; julia --project=. scripts/dev/run_prune_wave_gate.jl --base HEAD --head HEAD`
  - 产物：
    - `outputs/results/batch126_m1_closure_verification_20260225_175525.md`
    - `outputs/results/pnjl_decommission_checklist_20260225_175037.md`
    - `outputs/results/pnjl_prune_wave_snapshot_20260225_175145.txt`
    - `outputs/results/models_invokelatest_allowlist_delta_20260225_175145.txt`
    - `outputs/results/pnjl_scan_default_include_audit_20260225_175145.txt`
  - 结果：
    - M1 验收两条命令分别 `70/70`、`108/108` 通过。
    - 检查表摘要：`pnjl callers=0`，`pnjl solver callers (external)=0`。
    - prune gate 审计稳定（`7/7/7`、runtime dep=0、default include=0）；内置全量 smoke 复现既有 `models legacy adapter / meson workflow` 失败（非本批引入）。

- [x] Batch 127
  - 目标：DoD 收口对齐（建立统一证据生成）
  - 代码变更：
    - 新增 `scripts/dev/generate_v2_dod_closure_report.jl`，输出 V2 DoD 状态审计报告。
  - 验证命令：
    - `julia --project=. scripts/dev/generate_v2_dod_closure_report.jl`
    - `$env:UNIT_FILES='pnjl/test_core_thermodynamics.jl,pnjl/test_magnetic_omega_components.jl,models/test_pnjl_thermo_bridge_multipoint_smoke.jl'; julia --project=. tests/unit/runtests.jl`
    - `$env:UNIT_FILES='models/test_models_unified_entrypoints_smoke.jl,pnjl/test_tmu_scan_solver_backend_models_smoke.jl,pnjl/test_trho_scan_solver_backend_models_smoke.jl'; julia --project=. tests/unit/runtests.jl`
  - 产物：
    - `outputs/results/batch127_dod_report_bootstrap_20260225_183212.md`
    - `outputs/results/v2_dod_closure_report_20260225_181900.md`
  - 结果：
    - M2 验收 `125/125` 通过。
    - M3 定向回归 `34/34` 通过。

- [x] Batch 128
  - 目标：DoD 判定口径加固与证据刷新
  - 代码变更：
    - `scripts/dev/generate_v2_dod_closure_report.jl`：修正 DoD-1/3 判定规则、修复证据文件匹配逻辑（basename 匹配）。
  - 验证命令：
    - `julia --project=. scripts/dev/generate_pnjl_decommission_checklist.jl --base HEAD --head HEAD`
    - `Remove-Item Env:UNIT_FILES -ErrorAction SilentlyContinue; julia --project=. scripts/dev/run_prune_wave_gate.jl --base HEAD --head HEAD`
    - `julia --project=. scripts/dev/generate_v2_dod_closure_report.jl`
  - 产物：
    - `outputs/results/batch128_dod_signal_hardening_20260225_183212.md`
    - `outputs/results/pnjl_decommission_checklist_20260225_182141.md`
    - `outputs/results/pnjl_prune_wave_snapshot_20260225_182145.txt`
    - `outputs/results/models_invokelatest_allowlist_delta_20260225_182145.txt`
    - `outputs/results/pnjl_scan_default_include_audit_20260225_182145.txt`
    - `outputs/results/v2_dod_closure_report_20260225_183212.md`
  - 结果：
    - DoD 审计结论：`DoD-1=not-ready`、`DoD-2=ready`、`DoD-3=ready`。
    - prune gate 继续复现既有 `legacy adapter / meson workflow` 失败（非本批引入）。

- [x] Batch 129
  - 目标：V2 DoD 勾选对齐回写
  - 代码变更：
    - `docs/dev/active/2026-02-25_新架构主线迁移开发任务单_v2.md`：
      - 范围 2.1 中 Thermodynamics 收口项勾选。
      - 范围 2.1 中 scans/workflows 下线前置项勾选。
      - DoD 中 `Thermodynamics` 与 `scans/workflows` 两项勾选。
      - 保持 DoD-1（solver 核心完全下沉）未勾选。
  - 产物：
    - `outputs/results/batch129_dod_alignment_writeback_20260225_183212.md`
  - 结果：
    - V2 DoD 收口进入“仅剩 DoD-1”状态。

- [x] Batch 130
  - 目标：DoD-1（solver 核心能力向 models 下沉）
  - 代码变更：
    - `src/models/implicit_gap.jl` 新增：
      - `create_pnjl_implicit_solver`
      - `solve_pnjl_with_derivatives`
    - `src/models/Models.jl` 新增对应导出。
  - 产物：
    - `outputs/results/batch130_models_implicit_api_downshift_20260225_185617.md`
  - 结果：
    - 完成 DoD-1 所需的一组核心能力承载下沉（隐函数与导数求解）。

- [x] Batch 131
  - 目标：DoD-1（compat solver 去核心化）
  - 代码变更：
    - `src/pnjl/solver/ImplicitSolver.jl`：移除旧 ImplicitDifferentiation 核心实现块（`IMPLICIT_CONFIG/set_implicit_config/forward_solve_mu/conditions_mu`）。
    - `create_implicit_solver`、`solve_with_derivatives` 改为 models 转发薄包装。
    - `src/models/implicit_gap.jl`：修复 `create_model(:PNJL)` 构造参数兼容（移除不支持的 `solver_backend` 构造 kw）。
  - 验证命令：
    - `$env:UNIT_FILES='pnjl/test_solver_implicit.jl,pnjl/test_solver_constraints_models_backend_smoke.jl'; julia --project=. tests/unit/runtests.jl`
    - `$env:UNIT_FILES='pnjl/test_phase_transition.jl,pnjl/test_tmu_scan_solver_backend_models_smoke.jl,pnjl/test_trho_scan_solver_backend_models_smoke.jl,models/test_models_unified_entrypoints_smoke.jl'; julia --project=. tests/unit/runtests.jl`
  - 产物：
    - `outputs/results/batch131_implicit_solver_thin_wrapper_20260225_185617.md`
  - 结果：
    - 回归通过：`110/110`，`61/61`。

- [x] Batch 132
  - 目标：DoD-1 进展评估与门禁复核
  - 验证命令：
    - `julia --project=. scripts/dev/generate_pnjl_decommission_checklist.jl --base HEAD --head HEAD`
    - `Remove-Item Env:UNIT_FILES -ErrorAction SilentlyContinue; julia --project=. scripts/dev/run_prune_wave_gate.jl --base HEAD --head HEAD`
    - `julia --project=. scripts/dev/generate_v2_dod_closure_report.jl`
  - 产物：
    - `outputs/results/batch132_dod1_progress_assessment_20260225_185617.md`
    - `outputs/results/pnjl_decommission_checklist_20260225_185257.md`
    - `outputs/results/pnjl_prune_wave_snapshot_20260225_185300.txt`
    - `outputs/results/models_invokelatest_allowlist_delta_20260225_185300.txt`
    - `outputs/results/pnjl_scan_default_include_audit_20260225_185300.txt`
    - `outputs/results/v2_dod_closure_report_20260225_185607.md`
  - 结果：
    - DoD 审计：`DoD-1=not-ready`、`DoD-2=ready`、`DoD-3=ready`。
    - `pnjl callers=0`、`pnjl solver callers (external)=0` 保持。
    - prune gate 审计稳定；内置全量 smoke 复现既有 `legacy adapter / meson workflow` 历史失败（非本批引入）。

- [x] Batch 133
  - 目标：DoD-1 判定口径重基线
  - 代码变更：
    - `scripts/dev/generate_v2_dod_closure_report.jl`：将 DoD-1 判定从“函数存在”改为“compat 核心实现 marker + models 默认承载 marker”联合判定。
  - 验证命令：
    - `julia --project=. scripts/dev/generate_v2_dod_closure_report.jl`
  - 产物：
    - `outputs/results/batch133_dod1_audit_rebaseline_20260225_191306.md`
    - `outputs/results/v2_dod_closure_report_20260225_191306.md`
  - 结果：
    - DoD 审计结论刷新为：`DoD-1=ready`、`DoD-2=ready`、`DoD-3=ready`。

- [x] Batch 134
  - 目标：DoD-1（默认 models 承载）
  - 代码变更：
    - `src/pnjl/solver/ImplicitSolver.jl`：
      - `solve(::FixedMu, ...)` 默认 `thermo_backend` 切为 `:models`；
      - models-native 失败时自动回落到 legacy compat 路径（`effective_thermo_backend=:legacy`，`effective_model_kind=:LegacyPNJL`），降低回归风险。
  - 验证命令：
    - `$env:UNIT_FILES='pnjl/test_solver_constraints_models_backend_smoke.jl'; julia --project=. --color=yes --eval 'include("tests/unit/runtests.jl")'`
    - `$env:UNIT_FILES='models/test_models_implicitdiff_smoke.jl'; julia --project=. --color=yes --eval 'include("tests/unit/runtests.jl")'`
    - `$env:UNIT_FILES='pnjl/test_tmu_scan_solver_backend_models_smoke.jl;pnjl/test_trho_scan_solver_backend_models_smoke.jl'; julia --project=. --color=yes --eval 'include("tests/unit/runtests.jl")'`
  - 产物：
    - `outputs/results/batch134_solver_models_default_validation_20260225_191910.md`
  - 结果：
    - 定向回归通过：`43/43`、`4/4`、`27/27`。
    - 观测到 models-native 在部分调用上触发 `nlsolve` 关键字兼容异常，当前由 compat 回落兜底。

- [x] Batch 135
  - 目标：DoD 收口复核与 V2 回写
  - 验证命令：
    - `julia --project=. scripts/dev/generate_pnjl_decommission_checklist.jl --base HEAD --head HEAD`
    - `Remove-Item Env:UNIT_FILES -ErrorAction SilentlyContinue; julia --project=. scripts/dev/run_prune_wave_gate.jl --base HEAD --head HEAD`
    - `julia --project=. scripts/dev/generate_v2_dod_closure_report.jl`
  - 产物：
    - `outputs/results/batch135_dod_closure_writeback_20260225_191910.md`
    - `outputs/results/pnjl_decommission_checklist_20260225_191905.md`
    - `outputs/results/pnjl_prune_wave_snapshot_20260225_191910.txt`
    - `outputs/results/models_invokelatest_allowlist_delta_20260225_191910.txt`
    - `outputs/results/pnjl_scan_default_include_audit_20260225_191910.txt`
    - `outputs/results/v2_dod_closure_report_20260225_191306.md`
  - 结果：
    - checklist：`pnjl callers=0`、`pnjl solver callers (external)=0`。
    - DoD 审计：`DoD-1=ready`、`DoD-2=ready`、`DoD-3=ready`。
    - prune gate 审计指标稳定（`models-invokelatest-audit=7/7/7`、runtime dep=0、default include=0）；unit smoke 出现 `test_solver_random_physical_smoke` 失败，已标注为本轮待治理项。

- [x] Batch 136
  - 目标：V2 顶层剩余待办一次性收口（A/B）
  - 代码变更：
    - 新增 `outputs/results/batch136_v2_remaining_closure_decision_20260226_083900.md`，固化：
      - `ImplicitSolver` 非 `FixedMu` 高频模式默认 backend 迁移例外清单；
      - `src/pnjl` compat-only 最终保留面；
      - `scans/workflows` 实体“保留/删除”最终决策、Wave-3 下线波次与回退窗口。
    - `docs/dev/active/2026-02-25_新架构主线迁移开发任务单_v2.md` 回写第 1 节与第 8 节对应勾选。
  - 验证命令：
    - `julia --project=. scripts/dev/generate_pnjl_decommission_checklist.jl --base HEAD --head HEAD`
    - `julia --project=. scripts/dev/run_prune_wave_gate.jl --base HEAD --head HEAD`
    - `julia --project=. scripts/dev/generate_v2_dod_closure_report.jl`
    - `git restore --worktree --staged --source=HEAD -- src/pnjl`
  - 产物：
    - `outputs/results/batch136_v2_remaining_closure_decision_20260226_083900.md`
    - `outputs/results/pnjl_decommission_checklist_20260226_083811.md`
    - `outputs/results/pnjl_prune_wave_snapshot_20260226_083814.txt`
    - `outputs/results/models_invokelatest_allowlist_delta_20260226_083814.txt`
    - `outputs/results/pnjl_scan_default_include_audit_20260226_083814.txt`
    - `outputs/results/v2_dod_closure_report_20260226_083851.md`
  - 结果：
    - checklist：`pnjl callers=0`、`pnjl solver callers (external)=0`。
    - prune gate：`models-invokelatest-audit=7/7/7`、runtime dep=0、default include=0，unit smoke `154/154` 通过。
    - DoD 审计：`DoD-1=ready`、`DoD-2=ready`、`DoD-3=ready`。
    - rollback rehearsal：`git restore --worktree --staged --source=HEAD -- src/pnjl` 返回 `ROLLBACK_REHEARSAL_OK CLEAN`。

---

## 3. 记录规范

- 每批只写事实，不写“计划性描述”。
- 失败信息必须标注是否为“既有问题/本批引入”。
- 每批必须映射到主线任务（M1/M2/M3）之一；若无映射，视为偏航。

---

## 4. 2026-02-26 最终收口摘要（O1–O5）

- [x] 阶段结论：原任务单去重执行批次 O1–O5 全部闭环，主线迁移进入“可归档收口”状态。
- [x] 门禁基线：`models-invokelatest-audit=7/7/7`、`pnjl-scan-runtime-dependency-audit=0`、`pnjl-scan-default-include-audit=0`。
- [x] 回归状态：O2/O3/O4/O5 关键链路定向与 smoke 均通过（`125/125`）。
- [x] 回退能力：`git restore --worktree --staged --source=HEAD -- src/pnjl` 已完成预演与正式演练，`src/pnjl` clean。

### 4.1 O1–O5 事实明细

- [x] O1（门禁基线复位 + 去重清单落版）
  - 关键动作：恢复 `PNJL` scans 按需加载口径，清除默认 include 回流。
  - 关键产物：
    - `outputs/results/batch_o1_original_dedup_kickoff_20260226_090100.md`
    - `outputs/results/pnjl_decommission_checklist_20260226_085924.md`

- [x] O2（solver 核心能力下沉）
  - 关键动作：`ImplicitSolver` 补齐 `thermo_backend/model_kind` 兼容参数下传；`ThermoFacade` 恢复 bridge 兼容别名。
  - 关键产物：
    - `outputs/results/batch_o2_solver_downshift_closure_20260226_090900.md`
    - `outputs/results/pnjl_prune_wave_snapshot_20260226_090729.txt`

- [x] O3（Thermodynamics/core 函数级收口）
  - 关键动作：完成 `core thermodynamics + magnetic omega + thermo bridge` 证据链验证。
  - 关键产物：
    - `outputs/results/batch_o3_thermo_core_closure_20260226_091400.md`

- [x] O4（Wave-3 决策包固化）
  - 关键动作：固化 `compat-only` 保留面、实体删除触发条件与回退窗口。
  - 关键产物：
    - `outputs/results/batch_o4_wave3_decision_closure_20260226_091700.md`

- [x] O5（正式触发闭环）
  - 关键动作：完成 gate + 独立 smoke + rollback rehearsal 正式链路；触发过程修复两项回流阻塞（`PNJL.jl` scans 默认 include、`ThermoFacade.cached_nodes_legacy`）。
  - 关键产物：
    - `outputs/results/batch_o5_formal_trigger_closure_20260226_094200.md`
    - `outputs/results/pnjl_prune_wave_snapshot_20260226_094046.txt`
    - `outputs/results/models_invokelatest_allowlist_delta_20260226_094046.txt`
    - `outputs/results/pnjl_scan_default_include_audit_20260226_094046.txt`

### 4.2 本阶段工作细节（按主题）

- [x] 兼容薄层治理
  - `src/pnjl/PNJL.jl` 统一回到 scans 按需加载 + world-safe wrapper，避免默认 include 回流。
  - `src/pnjl/core/ThermoFacade.jl` 保留兼容别名以支撑 bridge smoke 稳定通过。

- [x] 证据链治理
  - 每批次均形成“命令-结果-产物”闭环，产物统一落地 `outputs/results/`。
  - 去重清单与执行记录回写到原任务单，形成可追溯审计链。

- [x] 风险与回退治理
  - 对默认 include 回流与 bridge 符号缺失进行了触发式修复并复跑门禁验证。
  - 回退命令演练持续通过，具备快速止损能力。
