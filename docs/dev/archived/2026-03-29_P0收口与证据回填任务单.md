---
title: P0 收口与证据回填任务单（承接 03-29 主任务单）
archived: true
original: docs/dev/active/2026-03-29_P0收口与证据回填任务单.md
archived_date: 2026-03-30
---


以下为原始内容（保留，以便审阅与历史参考）：

---

# P0 收口与证据回填任务单（承接 03-29 主任务单）

## 1. 目标

- 用 1 个短迭代收口主任务单中的未完成“必须项”，优先关闭 `B3`、`C4` 与短期验收中的证据缺口。
- 保持当前 localhost 主流程不回退，不引入超出 P0 范围的架构改造。

## 2. 输入与关联

- 主任务单：`docs/dev/active/2026-03-29_前后端分离中长期目标与前端短期待开发任务单.md`
- 前端核心代码：`web/js/api.js`、`web/js/ui.js`、`web/js/api.scan.test.mjs`
- 集成联调脚本：`web/js/api.scan.integration.test.mjs`

## 3. 范围

### 3.1 In Scope

- [x] S1 完成 `B3`：补齐数值字段非法（空值/非数值/越界）前端提交前拦截与提示。
- [x] S2 完成 `C4`：补齐网络超时/服务不可达可复现自动化测试与用户提示验证。
- [x] S3 完成“400 比例显著下降”证据方案：形成最小可复现统计口径与对比记录。
- [x] S4 回填主任务单对应勾选与证据位置，确保第 7/8/9 节一致。

### 3.2 Out of Scope

- [ ] O1 阶段 A/B/D 的中长期重构目标。
- [ ] O2 结果可视化增强（P1 范围）。
- [ ] O3 远程生产级部署与复杂运维链路。

## 4. 实施任务（可执行）

## 4.1 轻量执行顺序与命令清单

- [x] Step-1（T1）先补 `B3` 规则与单测：`node web/js/api.scan.test.mjs`
- [x] Step-2（T2）补超时/离线 mock 用例并复跑：`node web/js/api.scan.test.mjs`
- [x] Step-3（T3）补 400 统计输出并跑联调：`node web/js/api.scan.integration.test.mjs`
- [x] Step-4（T4）回填主任务单状态并执行：`julia --project=. scripts/dev/check_docs_consistency.jl`

### T1. 数值校验规则最小基线（B3）

- [x] T1-1 明确 `scan` 表单数值字段最小规则（非空、可解析、基本范围）。
- [x] T1-2 在 `web/js/api.js` 的 `validate_scan_request` 中补齐对应错误分支。
- [x] T1-3 在 `web/js/ui.js` 提交前错误提示中验证可读性与可定位性。
- [x] T1-4 在 `web/js/api.scan.test.mjs` 增加非法数值用例（至少 3 条）。

### T2. 超时/离线错误可复现验证（C4）

- [x] T2-1 在 JS 测试中加入 `AbortError` 模拟，验证提示“请求超时”。
- [x] T2-2 在 JS 测试中加入 `TypeError(fetch failed)` 模拟，验证提示“无法连接服务器”。
- [x] T2-3 验证重试逻辑在预期次数内触发，不出现静默失败。

### T3. 400 比例证据闭环

- [x] T3-1 定义统计口径：总请求数、前端拦截数、后端 400 数、400 占比。
- [x] T3-2 在联调脚本或测试日志中输出口径数据（至少一次基线 + 一次改造后）。
- [x] T3-3 在主任务单中追加简明结论与证据路径。

### T4. 文档回填与治理

- [x] T4-1 回填主任务单第 8 节 B3/C4 状态。
- [x] T4-2 回填主任务单第 7 节“400 比例显著下降”状态或阻塞说明。
- [x] T4-3 运行文档一致性检查并记录结果：`julia --project=. scripts/dev/check_docs_consistency.jl`（结果：`[docs-consistency] OK`）。

## 5. 验收标准（DoD）

- [x] D1 `B3` 勾选为完成，且有自动化测试通过证据。
- [x] D2 `C4` 勾选为完成，且有 `AbortError/TypeError` 用例通过证据。
- [x] D3 提供“400 占比”可比较记录（至少两组数据）。
- [x] D4 主任务单与本子任务单状态一致、证据路径可追溯。

## 7. 阶段证据（T1/T2）

- [x] 代码更新：`web/js/api.js`（B3 范围校验 + 错误归一化保留原始错误名）。
- [x] 测试更新：`web/js/api.scan.test.mjs`（新增 B3 负例与 C4 超时/离线 mock）。
- [x] 验证命令：`node web/js/api.scan.test.mjs`。
- [x] 验证结果：PASS（含重试次数断言）；Node 输出 `MODULE_TYPELESS_PACKAGE_JSON` 警告，不影响本任务验收。

## 8. 阶段证据（T3）

- [x] 代码更新：`web/js/api.js` 新增 `summarize_scan_request_metrics` 统计方法。
- [x] 测试更新：`web/js/api.scan.test.mjs` 新增 400 比例统计验证场景。
- [x] 口径输出：总请求 5、前端拦截 3、后端请求 2、baseline 400 占比 0.60、改造后 400 占比 0.00、下降 3。
- [x] 验证命令：`node web/js/api.scan.test.mjs`。

## 6. 风险与回退

- [ ] R1 若“越界”阈值难以统一，先采用最小安全阈值并在文档中显式声明。
- [ ] R2 若集成环境不稳定，先以单元 mock 保证可复现，再补联调证据。
- [ ] R3 若收口期间出现主流程回退，优先回滚到上一个可用提交并保留失败用例。
