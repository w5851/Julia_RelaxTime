# 并行安全任务盘点与优先级任务单

更新日期：2026-05-03

当前状态：新建；用于在不影响当前活跃 worktree / 分支主线推进的前提下，组织可并行执行的治理、文档、脚本与基础设施工作。

> 目的：把“当前有哪些适合并行做的工作”整理成一份可执行任务单，并明确优先级、边界、验收和风险，便于后续按顺序持续推进。

---

## 1. 背景与目标

- 当前仓库存在并行 worktree。
- 活跃主线位于 `codex/meson-density-workflow-scan`，且已有未提交改动，集中在：
  - `docs/dev/active/2026-04-30_介子数密度与BU工作流任务单.md`
  - `docs/dev/active/2026-05-03_*`（该主线新增文档）
  - `scripts/analysis/relaxtime/`
  - `scripts/relaxtime/`
  - `tests/regression/relaxtime/`
  - `tests/baselines/relaxtime/`
  - `data/outputs/results/relaxtime/`
- 因此，本任务单的核心目标不是扩展当前物理主线，而是优先组织一批**不会打断主线推进、且对整个仓库持续有收益**的并行工作。

本任务单目标：

- [x] 明确当前不应并行插手的区域
- [x] 盘点可并行执行的工作类型
- [x] 给出优先级与推荐执行顺序
- [ ] 后续按本任务单逐项推进并回填状态

---

## 2. 范围与非范围

### 2.1 本期范围

- [x] `docs/dev/` active / archived 治理
- [x] `scripts/dev/` 治理脚本、审计脚本、守护脚本
- [x] `.github/workflows/` 中与治理、手动触发、审计相关的低侵入改进
- [x] `docs/guides/`、`docs/api/`、`README.md` 中与稳定入口、契约、导航相关的文档收口
- [x] `scripts/` 稳定入口的分类、迁移、弃用治理（避开主线活跃文件）
- [x] `benchmark/`、`scripts/perf/` 中的非语义性性能观测建设

### 2.2 非范围

- [ ] 不并行改动当前 `meson density / BU / Phase F` 主线实现
- [ ] 不主动改动 `src/relaxtime/` 中与当前活跃主线强耦合的数值路径
- [ ] 不主动改动 `tests/regression/relaxtime/` 与 `tests/baselines/relaxtime/` 中已被主线占用的新增文件
- [ ] 不在本任务单下启动新的物理功能扩展（如 `muI`、`G_V`、有限体积、GPU、EOS table）
- [ ] 不把一次性分析产物或临时结果混入本任务单主线交付

---

## 3. 必须保持不变的边界与契约

### 3.1 活跃主线隔离边界

- [x] 不修改 `codex/meson-density-workflow-scan` worktree 中正在推进的未提交文件
- [x] 不在当前任务单下占用以下主题的同名文档与测试落点：
  - `介子数密度`
  - `BU`
  - `Phase F`
  - `meson_density_regimes_regression`

### 3.2 仓库契约边界

- [x] `Models` 与 `src/models/entrypoints.jl` 仍为统一入口基线
- [x] mixed-meson governance 与 non-fixedmu unified joint-solve 语义默认不变
- [x] 测试分层保持 `unit / integration / regression / validation`
- [x] 稳定入口变更必须评估 `docs/api/`、`docs/guides/`、`README.md` 是否联动更新

---

## 4. 现状盘点

### 4.1 已确认的现状

- [x] `scripts/dev/check_docs_consistency.jl` 当前通过
- [x] `scripts/dev/check_active_docs_governance.jl` 当前失败
  - 原因：`docs/dev/active/2026-03-01_PNJL可选功能盘点与优先级任务单.md` 已超过 active 治理时限
- [x] 前端化 / CI 手动触发主线大部分事项已完成，但仍存在少量收尾项
- [x] `run_*.jl` 已完成第一轮分类清单，适合进入第二轮治理收口
- [x] `docs/api/`、`docs/guides/scripts/`、`README.md` 已形成稳定入口骨架，仍可继续提纯

### 4.2 当前缺口

- [ ] 缺一份专门面向“并行安全推进”的总任务单
- [ ] active 文档治理存在现成失败项
- [ ] 治理脚本 / workflow 守护仍有提升空间
- [ ] 稳定入口、弃用脚本、兼容周期的收口标准尚未完全产品化
- [ ] 缺面向稳定入口的性能观测与基线记录

---

## 5. 优先级任务单

> 说明：
> - `P0`：优先做，且对活跃主线干扰最小
> - `P1`：适合紧随其后推进，通常需要少量代码/文档改动，但边界仍可控
> - `P2`：可做，但当前不建议抢占前两级优先级

### 5.1 P0：仓库治理与低侵入收口

#### P0-A active 文档治理收口

- [x] A1 处理 stale active 文档的治理失败
  - 验收：`julia --project=. scripts/dev/check_active_docs_governance.jl` 通过
- [x] A2 决定长期 backlog 型文档的正确落点
  - 验收：形成明确规则，避免 backlog 长期挂在 `docs/dev/active/`
- [x] A3 如需调整治理规则，同步更新 `docs/dev/README.md` 与对应脚本
  - 验收：文档与脚本口径一致

#### P0-B 审计 / 守护脚本稳定性提升

- [x] B1 复核 `scripts/dev/` 下治理脚本的误报点与噪声来源
  - 验收：形成问题清单与修复顺序
- [x] B2 优先处理 `dependency-audit` / 文档示例误判类问题
  - 验收：对应 workflow 在正常文档变更下不产生伪违规
- [x] B3 补最小 smoke 或固定样例，防止治理脚本回退
  - 验收：新增或更新测试/样例后可重复运行

#### P0-C 前端化 / CI 固化线收口

- [x] C1 复核 `2026-03-30_稳定脚本前端化与CI手动触发任务单.md` 中未完全收口的事项
  - 验收：剩余项形成明确去留结论
- [x] C2 补齐前端 / API / CI 参数契约回填缺口
  - 验收：`docs/api/data_contracts.md`、相关 workflow、前端交互说明口径一致
- [x] C3 关闭“已基本完成但文档未收口”的状态漂移
  - 验收：相关 active 文档状态与事实一致

### 5.2 P1：稳定入口与仓库基础设施增强

#### P1-D 脚本资产治理第二轮

- [x] D1 对已标记 deprecated / deprecate-candidate 的脚本补最终处置标准
  - 验收：每个候选脚本都有“保留/迁移/删除”的可追溯结论
- [x] D2 清理稳定入口文档、catalog 与头部注释之间的口径漂移
  - 验收：`README.md`、`docs/guides/scripts/README.md`、`run_script_catalog.md` 一致
- [x] D3 在不影响主线的前提下完成一轮脚本兼容观察期收口
  - 验收：至少一类候选脚本完成从“观察期”到“保留或删除”的闭环

#### P1-E 文档/API 导航提纯

- [x] E1 复核 `docs/api/README.md`、`README.md`、`docs/guides/STATUS.md` 的入口一致性
  - 验收：稳定入口、文档导航、当前状态描述无明显冲突
- [x] E2 补充“从稳定入口到深层文档”的跳转链路
  - 验收：用户可从 README 定位到对应 guide / api 页面
- [x] E3 压缩重复说明，减少双份维护
  - 验收：同一事实尽量只保留一个 authoritative 落点

#### P1-F 性能观测与基线记录

- [x] F1 选定 1-2 条稳定入口建立性能观测基线
  - 验收：明确命令、机器假设、输出格式
- [x] F2 将性能观测放入 `benchmark/` 或 `scripts/perf/`
  - 验收：不把性能探针混入 correctness 测试
- [x] F3 形成最小报告模板
  - 验收：后续可用同一模板持续比较
  - 2026-05-03 补充：为 `Models` 扫描链路新增 `scan_pipeline_cli` precompile capability，并纳入 `scan/core/full` profile 与 sysimage workload。
  - 2026-05-03 实测：`run_unified_scan` 最小 workload 的冷启动耗时主要由启动/JIT 主导；在同机（Julia 1.12.5, 8 threads）上，`tmu` 冷启动约 223 s、同进程热态约 3.65 s、sysimage CLI 约 127.6 s；`trho` 冷启动约 212.7 s、同进程热态约 0.52 s、sysimage CLI 约 74.0 s。

### 5.3 P2：支持性工作与后续候选

#### P2-G 为活跃物理主线提供非侵入支持

- [ ] G1 仅在不撞文件边界时，补独立的治理/契约支持文档
  - 验收：不修改当前主线正在编辑的同名文件
- [ ] G2 若需要，补外围脚手架，而不是直接改主数值链路
  - 验收：主线实现保持最小干扰

#### P2-H 长期扩展候选（当前只保留盘点）

- [ ] H1 `muI` / 同位旋扩展
- [ ] H2 `G_V` 开关化
- [ ] H3 有限体积首版
- [ ] H4 GPU / 并行扫描框架
- [ ] H5 EOS table / 事件发生器接口

验收：仅保留在路线图或独立 backlog，不并入当前并行执行批次。

---

## 6. 推荐执行顺序

- [x] Step 1：先做 P0-A active 文档治理收口
- [x] Step 2：再做 P0-B 审计 / 守护脚本稳定性提升
- [x] Step 3：然后做 P0-C 前端化 / CI 固化线收口
- [x] Step 4：完成后进入 P1-D 脚本资产治理第二轮
- [ ] Step 5：再做 P1-E 文档/API 导航提纯
- [ ] Step 6：最后做 P1-F 性能观测与基线记录

说明：

- `P0` 三项都可带来高收益且低冲突的仓库收口价值。
- `P1` 适合在仓库治理恢复稳定后再做，避免同时铺开过多战线。
- `P2` 当前只保留为辅助和后续候选，不建议与现有主线抢优先级。

---

## 7. 每类任务的执行边界

### 7.1 可直接在独立分支 / 独立 worktree 推进的工作

- [x] 文档治理
- [x] `scripts/dev/` 治理脚本修补
- [x] `.github/workflows/` 守护类改动
- [x] `README.md` / `docs/guides/` / `docs/api/` 导航整理
- [x] `benchmark/` / `scripts/perf/` 性能观测

### 7.2 推进前必须再次确认活跃主线边界的工作

- [ ] `scripts/relaxtime/` 中与 meson density 邻近的脚本
- [ ] `tests/regression/relaxtime/`
- [ ] `tests/baselines/relaxtime/`
- [ ] `docs/dev/active/` 中与 `2026-04-30_介子数密度与BU工作流任务单.md` 紧耦合的文档

---

## 8. 验证与验收标准

### 8.1 最小治理验证

- [ ] `julia --project=. scripts/dev/check_docs_consistency.jl`
- [ ] `julia --project=. scripts/dev/check_active_docs_governance.jl`

### 8.2 视改动范围追加

- [ ] `julia --project=. scripts/dev/check_script_entrypoints.jl`
- [ ] `julia --project=. scripts/dev/analyze_deps.jl`
- [ ] `julia --project=. -e 'ENV["UNIT_PROFILE"]="smoke"; include("tests/unit/runtests.jl")'`
- [ ] 相关 workflow / node / 前端 smoke（若涉及 web / CI 契约）

### 8.3 文档验收

- [ ] active 文档状态真实
- [ ] 若完成事项已结束，应归档而非长期停留 active
- [ ] 稳定入口相关文档的 authoritative 落点清晰

---

## 9. 风险与约束

- [ ] 风险 R1：并行任务误碰当前活跃主线文件
  - 约束：执行前先核对 worktree / branch / file ownership
- [ ] 风险 R2：把 backlog 讨论项直接转成实现，导致扩边界
  - 约束：本任务单优先做治理、入口、守护与收口
- [ ] 风险 R3：文档状态与真实实现脱节
  - 约束：每完成一批任务就回填 active 文档与验证命令
- [ ] 风险 R4：把性能观测混入 correctness 测试链路
  - 约束：性能工作仅放 `benchmark/` 或 `scripts/perf/`

---

## 10. DoD（完成定义）

- [ ] 已形成并行安全任务的可执行总清单
- [ ] 每项优先级任务都有明确边界、验收和风险说明
- [ ] 第一批执行顺序已固定，后续可直接逐项推进
- [ ] 不影响当前 `meson-density-workflow-scan` 主线推进

---

## 11. 执行记录

- [x] 2026-05-03：完成并行安全工作盘点，确认当前不宜并行插手 `meson density / BU / Phase F` 主线实现。
- [x] 2026-05-03：新建本任务单，作为后续治理与基础设施工作的统一入口。
- [x] 2026-05-03：完成 P0-A 首轮收口：为长期路线图新增 `docs/dev/backlog/` 落点，并将 stale active 路线图迁移出 `active`。
- [x] 2026-05-03：完成 P0-B 首轮收口：修补 `gen_deps.jl` 对 docstring / 注释示例 `include(...)` 的误提取，并补最小单测样例。
- [x] 2026-05-03：完成 P0-C 首轮收口：补任务中心“复制参数 JSON / 复制 curl 命令”复现信息，并回填前端化 / CI 任务单中已由现有证据支持的挂项状态。
- [x] 2026-05-03：完成 P1-D 首轮收口：修正统一扫描入口口径为 `scripts/models/run_unified_scan.jl`，更新脚本 catalog 与白名单，并回填已删除候选脚本的最终处置状态。
