# 核心算法性能优化与AB诊断任务单 执行台账

更新日期：2026-08-20

---

## 0. 台账定位

- 本文档仅记录“执行事实”：变更点、命令、产物、结果。
- 本阶段开发任务定义统一维护在：
  - `docs/dev/active/2026-08-19_核心算法性能优化与AB诊断任务单.md`

---

## 1. 记录规范（强约束）

- [x] 执行记录与开发任务分开保存：
  - 任务目标/范围/DoD 只写“开发任务单”；
  - 执行过程/命令/结果只写“执行台账”。
- [x] 追加记录时采用“直接追加”策略：
  - 不要求回读本台账历史上下文；
  - 仅按统一模板追加到文档末尾；
  - 每条记录必须自包含（含目标、命令、产物、结果）。
- [x] 每条记录必须可追溯到输出产物（`data/outputs/results/*`）。

---

## 2. 执行记录

- [ ] 批次号：A/B-M1-preflight
  - 目标：比较显式 n_max 时 magnetic finite-difference residual 是否可跳过无用中心 Omega 评估
  - 代码变更：新增 scripts/perf/pnjl/magnetic_residual_context_probe.jl；仅诊断脚本，不修改生产 solver
  - 验证命令：
    - `julia --project=. scripts/perf/pnjl/magnetic_residual_context_probe.jl`
    - `git diff --check`
  - 产物：
    - `scripts/perf/pnjl/magnetic_residual_context_probe.jl`
  - 结果：停止：约 40 秒无 stdout，未进入 Julia 模块加载后的测量循环；未产生 wall-time、parity 或优化结论；未触发 solve_magnetic_gap、磁场扫描或生产输出
  - 主线映射（N1/N2/N3/N4）：research/A-B-M1
  - 备注：与既有 worktree 冷启动预检一致；后续需在持久热 Julia 会话或匹配 sysimage 中重试，冷启动成本不得计入算法 A/B

- [ ] 批次号：Hessian-contract-closure
  - 目标：将 Hessian 明确为可选诊断，不作为默认 PNJL 生产过滤条件，并锁定显式分类不拒绝已收敛驻点
  - 代码变更：MagneticGapSolver 保留最低-Omega convenience state；补充磁场模型 focused test；同步磁场审计文档
  - 验证命令：
    - `julia --project=. scripts/dev/check_docs_consistency.jl`
    - `julia --project=. scripts/dev/check_active_docs_governance.jl`
    - `git diff --check`
    - `focused magnetic unit test (stopped after 50s without output)`
  - 产物：
    - `src/models/pnjl_physics/MagneticGapSolver.jl`
    - `tests/unit/pnjl/test_pnjl_magnetic_model.jl`
    - `docs/dev/active/2026-08-19_PNJLMagnetic语义与稳定性审计任务单.md`
  - 结果：静态解析、docs consistency、active-doc governance、diff check 通过；focused unit test 启动约 50 秒无输出后按停止条件中止，未取得运行时通过/失败结论
  - 主线映射（N1/N2/N3/N4）：research/magnetic-contract
  - 备注：Hessian 默认仍为 false；显式 classify_stability 仅添加标签，不因没有 local_minimum 将 state 置空。A/B-M1 冷启动 probe 同样未进入测量循环，保留待持久热会话或匹配 sysimage。

- [ ] 批次号：A/B-M1-partial
  - 目标：测量显式 n_max 时 magnetic finite-difference residual 跳过无用中心 Omega 评估的局部收益
  - 代码变更：仅运行诊断脚本 scripts/perf/pnjl/magnetic_residual_context_probe.jl；无生产 solver 代码修改
  - 验证命令：
    - `julia --compile=min --project=. scripts/perf/pnjl/magnetic_residual_context_probe.jl`
    - `git diff --check`
  - 产物：
    - `scripts/perf/pnjl/magnetic_residual_context_probe.jl`
  - 结果：5 个点完成，baseline/variant residual parity 均为 0；T=1 MeV, mu=0 MeV 的 eB=0.1/0.5/2.0 speedup=1.805x/1.762x/1.372x；T=1 MeV, mu=60 MeV 的 eB=0.1/0.5 speedup=1.058x/1.089x。因单点耗时约 0.77--1.66 s 且达到短测预算，未完成剩余点；未调用 solve_magnetic_gap、未运行磁场扫描、未写生产输出。
  - 主线映射（N1/N2/N3/N4）：research/A-B-M1
  - 备注：--compile=min 仅用于绕过候选 worktree 冷启动；绝对时间不用于生产 wall-time 估计，只比较同一进程内 baseline/variant。结果支持低温/显式 n_max 路径存在局部收益，需正常编译热进程复核后才能决定实现 PR。

- [ ] 批次号：Hessian-contract-runtime-verification
  - 目标：补充 Hessian 诊断合同的运行时验证结果
  - 代码变更：无新增生产代码；记录 compile-min 下的 focused magnetic unit test 结果
  - 验证命令：
    - `julia --compile=min --project=. include tests/unit/pnjl/test_pnjl_magnetic_model.jl`
    - `git diff --check`
  - 产物：
    - `tests/unit/pnjl/test_pnjl_magnetic_model.jl`
  - 结果：通过：PNJLMagneticModel 32/32；运行时间 27.8 s。验证显式 classify_stability=true 时仍保留 state/candidates，Hessian 只提供诊断标签。
  - 主线映射（N1/N2/N3/N4）：research/magnetic-contract
  - 备注：此前 Hessian-contract-closure 记录中的 focused test 未完成结论仅对应第一次普通编译启动；本条记录提供后续 compile-min 运行时证据，不改写历史。

- [ ] 批次号：task-semantics-status-reconciliation-2026-08-21
  - 目标：对齐 A/B-0 shared context 语义，并为历史 M1/Hessian 执行记录补充当前状态，不重写历史事实
  - 代码变更：仅修改性能任务单语义说明，并追加本状态对账记录；不修改生产 solver、性能探针或数值产物
  - 验证命令：
    - `git diff --check`
    - `julia --project=. scripts/dev/check_docs_consistency.jl`
    - `julia --project=. scripts/dev/check_active_docs_governance.jl`
  - 产物：
    - `docs/dev/active/2026-08-19_核心算法性能优化与AB诊断任务单.md`
    - `docs/dev/active/2026-08-19_核心算法性能优化与AB诊断任务单_执行台账.md`
  - 结果：语义修复已完成；A/B-0 明确为已实现 shared context 的收益复核；A/B-M1 明确为显式 n_max 条件下 center-elision 候选。当前状态：A/B-M1-preflight=stopped_no_measurement；Hessian-contract-closure=closed_verified_later；A/B-M1-partial=partial_pending_semantic_review；Hessian-contract-runtime-verification=closed_verified。未运行 A/B-M1，未触发 C0/C1/C2。
  - 主线映射（N1/N2/N3/N4）：research/A-B-M1; research/magnetic-contract
  - 备注：历史四条记录保持 append-only 不改写；A/B-M1-partial 暂不决定是否正常热进程复核，等待后续语义沟通。

- [ ] 批次号：A/B-PNJL-AD-FD-2026-08-22
  - 目标：在排除进程启动和首轮 JIT 影响的口径下，补充普通 PNJL 的 residual/Jacobian AD-vs-FD 对照，并复核磁场 probe 的稳态计时
  - 代码变更：新增 scripts/perf/pnjl/ordinary_pnjl_ad_fd_probe.jl；更新 magnetic_autodiff_probe.jl，使首调用与同进程 steady-state median 分开报告；不修改生产 solver 默认和扫描入口
  - 验证命令：
    - `git rev-parse HEAD`
    - `julia --version`
    - `git diff --check`
    - `PNJL_AD_FD_MAX_POINTS=3 PNJL_AD_FD_REPEATS=2 julia --project=. scripts/perf/pnjl/ordinary_pnjl_ad_fd_probe.jl`
    - `MAGNETIC_AD_PROBE_MAX_POINTS=1 MAGNETIC_AD_PROBE_REPEATS=1 julia --project=. scripts/perf/pnjl/magnetic_autodiff_probe.jl`
  - 产物：
    - `scripts/perf/pnjl/ordinary_pnjl_ad_fd_probe.jl`
    - `scripts/perf/pnjl/magnetic_autodiff_probe.jl`
  - 结果：普通 PNJL 三个点 smoke 通过：四种 residual/Jacobian 组合均 converged；首调用约 0.28--4.79 s，稳态 median 约 0.37--3.30 ms，证明冷启动/JIT 必须与算法计时分开。磁场一点 smoke 通过：finite 首调用 767.0 ms、稳态 9.44 ms；ForwardDiff 首调用 4793.6 ms、稳态 0.553 ms；f/g/iteration 均为 5/5/4，初始 residual parity 最大 7.824e-12。未调用 production scan、未写数值产物、未触发 C0/C1/C2。
  - 主线映射（N1/N2/N3/N4）：research/A-B-PNJL-AD-FD
  - 备注：首调用仅作 JIT 参考；steady-state median 才用于 AD/FD 算法比较。低节点 smoke 不能外推生产精度或完整磁场 equilibrium wall time。

- [ ] 批次号：A/B-PNJL-AD-FD-parity-2026-08-22
  - 目标：为普通 PNJL AD/FD probe 增加同一初始状态 residual parity 输出并完成一点评估
  - 代码变更：更新 ordinary_pnjl_ad_fd_probe.jl 输出 initial_residual_max_diff；不修改生产 solver
  - 验证命令：
    - `git diff --check`
    - `PNJL_AD_FD_MAX_POINTS=1 PNJL_AD_FD_REPEATS=1 julia --project=. scripts/perf/pnjl/ordinary_pnjl_ad_fd_probe.jl`
  - 产物：
    - `scripts/perf/pnjl/ordinary_pnjl_ad_fd_probe.jl`
  - 结果：通过：T=1 MeV, mu=0 MeV 四种组合均 converged；FD/AD residual 初始最大差异 1.441e-10；首调用 241--3946 ms，稳态 median 0.362--1.752 ms。仍为低节点诊断，未触发生产路径。
  - 主线映射（N1/N2/N3/N4）：research/A-B-PNJL-AD-FD
  - 备注：Parity 量是同一初始状态上的 residual 直接差异，不是最终解差异；冷启动首调用仍单独报告。

- [ ] 批次号：magnetic-ad-production-default-2026-08-23
  - 目标：将磁场五维 stationarity residual 收口为 ForwardDiff/AD 默认生产路径，移除生产 residual 的有限差分实现；保留 Hessian 诊断的外层有限差分
  - 代码变更：`magnetic_gap_residual` 使用固定 `n_max` 的 `ForwardDiff.gradient`；每个物理点在进入 seed loop 前解析一次 `n_max` 并在 primary/fallback 间复用；`NLsolve` 固定 `autodiff=:forward`；历史 AD 名称保留为兼容别名；M1 有限差分探针改为 retired marker
  - 验证命令：
    - `git diff --check`
    - `MAGNETIC_AD_PROBE_MAX_POINTS=1 MAGNETIC_AD_PROBE_REPEATS=1 julia --project=. scripts/perf/pnjl/magnetic_autodiff_probe.jl`
    - focused magnetic unit test
  - 产物：
    - `src/models/pnjl_physics/MagneticGapSolver.jl`
    - `tests/unit/pnjl/test_pnjl_magnetic_model.jl`
    - `scripts/perf/pnjl/magnetic_autodiff_probe.jl`
    - `scripts/perf/pnjl/magnetic_residual_context_probe.jl`
    - `docs/api/models/variants/magnetic/ModelAndConfig.md`
  - 结果：实现与验证进行中；既有低节点稳态证据为 AD `0.553 ms`、FD `9.436 ms`，f/g/iteration 均为 `5/5/4`，residual parity 最大 `7.824e-12`。该证据不外推完整生产精度或完整磁场扫描 wall time。
  - 主线映射（N1/N2/N3/N4）：research/magnetic-contract; research/A-B-PNJL-AD-FD
  - 备注：`finite_difference_step` 仍只用于可选 Hessian 外层诊断；不再进入 stationarity residual 或 NLsolve Jacobian。未运行完整磁场 equilibrium、C0/C1/C2。

- [x] 批次号：magnetic-thermal-cutoff-profile-2026-08-26
  - 目标：把 `pnjl_mag`/Julia 截断诊断确认的温度感知 thermal-tail 规则落实为可配置的生产 profile，并保证同一 `(T,mu,eB)` 点的全部 seed/primary/fallback attempt 使用同一 `n_max`。
  - 代码变更：`MagneticConfig` 新增 `n_max_policy`、`thermal_tail_factor`、`n_max_floor`、`n_max_cap`；新增公开 `resolve_magnetic_nmax`；默认 `magnetic_default.toml` 采用 MFIR、`p_num=128`、`pz_max=40`、`zeta_num=256`、`k=30`、floor=3、cap=10000；新增 `magnetic_source_parity.toml` 固定 `n_max=79` 供外部匹配；磁场扫描 CSV 增加 `cutoff_policy`/`configured_n_max` 追溯字段；统一 CLI 接受 profile 与截断参数。
  - 验证命令：
    - `julia --startup-file=no --project=. -e 'include("src/models/Models.jl"); ... resolve_magnetic_nmax ...'`
    - `julia --startup-file=no --project=. scripts/dev/export_pnjl_magnetic_fixedpoint_baseline.jl --scope smoke --output tmp/magnetic_fixedpoints_smoke_candidate_latest.csv`
    - `julia --startup-file=no --project=. scripts/dev/export_pnjl_magnetic_fixedpoint_baseline.jl --scope nightly --output tmp/magnetic_fixedpoints_nightly_candidate.csv`
    - `julia --project=. tests/unit/pnjl/test_magnetic_thermodynamics.jl`
  - 结果：短 smoke 解析成功；默认 profile 示例点解析为 `n_max=6804`，source-parity profile 解析为固定 `n_max=79`；新的 smoke/nightly 固定点输出已生成，旧基线差异来自有意的截断语义迁移而非容差放宽。相关 magnetic unit/regression、factory/interface/dimension/homomorphism 回归均通过；完整 equilibrium/magnetic scan、C0/C1/C2 均未运行。
  - 主线映射（N1/N2/N3/N4）：research/magnetic-contract; research/pnjl-mag-parity
  - 备注：`n_max_cap` 超限会显式 `ArgumentError`；显式 `n_max` 优先。`pnjl_mag` equilibrium 仍保持 diagnostic-only，source-parity 与 production-parity 不合并为同一 CSV。
