# 核心算法性能优化与AB诊断任务单 执行台账

更新日期：2026-08-20

---

## 0. 台账定位

- 本文档仅记录“执行事实”：变更点、命令、产物、结果。
- 本阶段开发任务定义统一维护在：
  - `docs/dev/active/2026-08-19_核心算法性能优化与AB诊断任务单.md`

---

## 1. 记录规范（强约束）

- [x] 执行记录与开发任务分开保存：
  - 任务目标/范围/DoD 只写“开发任务单”；
  - 执行过程/命令/结果只写“执行台账”。
- [x] 追加记录时采用“直接追加”策略：
  - 不要求回读本台账历史上下文；
  - 仅按统一模板追加到文档末尾；
  - 每条记录必须自包含（含目标、命令、产物、结果）。
- [x] 每条记录必须可追溯到输出产物（`data/outputs/results/*`）。

---

## 2. 执行记录

- [ ] 批次号：A/B-M1-preflight
  - 目标：比较显式 n_max 时 magnetic finite-difference residual 是否可跳过无用中心 Omega 评估
  - 代码变更：新增 scripts/perf/pnjl/magnetic_residual_context_probe.jl；仅诊断脚本，不修改生产 solver
  - 验证命令：
    - `julia --project=. scripts/perf/pnjl/magnetic_residual_context_probe.jl`
    - `git diff --check`
  - 产物：
    - `scripts/perf/pnjl/magnetic_residual_context_probe.jl`
  - 结果：停止：约 40 秒无 stdout，未进入 Julia 模块加载后的测量循环；未产生 wall-time、parity 或优化结论；未触发 solve_magnetic_gap、磁场扫描或生产输出
  - 主线映射（N1/N2/N3/N4）：research/A-B-M1
  - 备注：与既有 worktree 冷启动预检一致；后续需在持久热 Julia 会话或匹配 sysimage 中重试，冷启动成本不得计入算法 A/B

- [ ] 批次号：Hessian-contract-closure
  - 目标：将 Hessian 明确为可选诊断，不作为默认 PNJL 生产过滤条件，并锁定显式分类不拒绝已收敛驻点
  - 代码变更：MagneticGapSolver 保留最低-Omega convenience state；补充磁场模型 focused test；同步磁场审计文档
  - 验证命令：
    - `julia --project=. scripts/dev/check_docs_consistency.jl`
    - `julia --project=. scripts/dev/check_active_docs_governance.jl`
    - `git diff --check`
    - `focused magnetic unit test (stopped after 50s without output)`
  - 产物：
    - `src/models/pnjl_physics/MagneticGapSolver.jl`
    - `tests/unit/pnjl/test_pnjl_magnetic_model.jl`
    - `docs/dev/active/2026-08-19_PNJLMagnetic语义与稳定性审计任务单.md`
  - 结果：静态解析、docs consistency、active-doc governance、diff check 通过；focused unit test 启动约 50 秒无输出后按停止条件中止，未取得运行时通过/失败结论
  - 主线映射（N1/N2/N3/N4）：research/magnetic-contract
  - 备注：Hessian 默认仍为 false；显式 classify_stability 仅添加标签，不因没有 local_minimum 将 state 置空。A/B-M1 冷启动 probe 同样未进入测量循环，保留待持久热会话或匹配 sysimage。

- [ ] 批次号：A/B-M1-partial
  - 目标：测量显式 n_max 时 magnetic finite-difference residual 跳过无用中心 Omega 评估的局部收益
  - 代码变更：仅运行诊断脚本 scripts/perf/pnjl/magnetic_residual_context_probe.jl；无生产 solver 代码修改
  - 验证命令：
    - `julia --compile=min --project=. scripts/perf/pnjl/magnetic_residual_context_probe.jl`
    - `git diff --check`
  - 产物：
    - `scripts/perf/pnjl/magnetic_residual_context_probe.jl`
  - 结果：5 个点完成，baseline/variant residual parity 均为 0；T=1 MeV, mu=0 MeV 的 eB=0.1/0.5/2.0 speedup=1.805x/1.762x/1.372x；T=1 MeV, mu=60 MeV 的 eB=0.1/0.5 speedup=1.058x/1.089x。因单点耗时约 0.77--1.66 s 且达到短测预算，未完成剩余点；未调用 solve_magnetic_gap、未运行磁场扫描、未写生产输出。
  - 主线映射（N1/N2/N3/N4）：research/A-B-M1
  - 备注：--compile=min 仅用于绕过候选 worktree 冷启动；绝对时间不用于生产 wall-time 估计，只比较同一进程内 baseline/variant。结果支持低温/显式 n_max 路径存在局部收益，需正常编译热进程复核后才能决定实现 PR。

- [ ] 批次号：Hessian-contract-runtime-verification
  - 目标：补充 Hessian 诊断合同的运行时验证结果
  - 代码变更：无新增生产代码；记录 compile-min 下的 focused magnetic unit test 结果
  - 验证命令：
    - `julia --compile=min --project=. include tests/unit/pnjl/test_pnjl_magnetic_model.jl`
    - `git diff --check`
  - 产物：
    - `tests/unit/pnjl/test_pnjl_magnetic_model.jl`
  - 结果：通过：PNJLMagneticModel 32/32；运行时间 27.8 s。验证显式 classify_stability=true 时仍保留 state/candidates，Hessian 只提供诊断标签。
  - 主线映射（N1/N2/N3/N4）：research/magnetic-contract
  - 备注：此前 Hessian-contract-closure 记录中的 focused test 未完成结论仅对应第一次普通编译启动；本条记录提供后续 compile-min 运行时证据，不改写历史。

- [ ] 批次号：task-semantics-status-reconciliation-2026-08-21
  - 目标：对齐 A/B-0 shared context 语义，并为历史 M1/Hessian 执行记录补充当前状态，不重写历史事实
  - 代码变更：仅修改性能任务单语义说明，并追加本状态对账记录；不修改生产 solver、性能探针或数值产物
  - 验证命令：
    - `git diff --check`
    - `julia --project=. scripts/dev/check_docs_consistency.jl`
    - `julia --project=. scripts/dev/check_active_docs_governance.jl`
  - 产物：
    - `docs/dev/active/2026-08-19_核心算法性能优化与AB诊断任务单.md`
    - `docs/dev/active/2026-08-19_核心算法性能优化与AB诊断任务单_执行台账.md`
  - 结果：语义修复已完成；A/B-0 明确为已实现 shared context 的收益复核；A/B-M1 明确为显式 n_max 条件下 center-elision 候选。当前状态：A/B-M1-preflight=stopped_no_measurement；Hessian-contract-closure=closed_verified_later；A/B-M1-partial=partial_pending_semantic_review；Hessian-contract-runtime-verification=closed_verified。未运行 A/B-M1，未触发 C0/C1/C2。
  - 主线映射（N1/N2/N3/N4）：research/A-B-M1; research/magnetic-contract
  - 备注：历史四条记录保持 append-only 不改写；A/B-M1-partial 暂不决定是否正常热进程复核，等待后续语义沟通。
