# src 优化待办开发文档（基于 10 维体检）

## 1. 背景与目标

基于 `docs/dev/active/2026-03-27_src体检结果报告_10维度.md` 的证据化结论，整理一份可执行待办任务单。目标是按“低风险高收益”优先推进 `src/` 的安全性、可测试性与文档一致性改进。

## 2. 范围与非目标

### 2.1 范围

- 任务 A（P0）：`pnjl_scan_jobs` 输出路径安全收口。
- 任务 B（P1）：补 `pnjl_scan_jobs` 最小契约测试。
- 任务 C（P1）：清理 docs 旧路径引用（`src/pnjl/...` -> `src/models/...`）。

### 2.2 非目标

- 不在本轮做大规模性能优化。
- 不在本轮做 `TransportCoefficients/AverageScatteringRate` 结构重拆。
- 不引入新的物理模型或数值方案。

## 3. 现状盘点

- 已有体检结论：`docs/dev/active/2026-03-27_src体检结果报告_10维度.md`。
- 风险最高项定位在：`src/simulation/fullserver/pnjl_scan_jobs.jl`。
- 文档路径一致性存在历史残留，影响认知统一。

## 4. 实施任务单（可勾选）

## 阶段 A：安全收口（P0）

### Task A1: 增加 output_path 安全校验

**Files:**
- Modify: `src/simulation/fullserver/pnjl_scan_jobs.jl`

- [x] 定义允许写入根目录（限定到 `data/outputs/` 子树）。
- [x] 对请求 `output_path` 做 `abspath(normpath(...))` 归一化。
- [x] 若路径越界，返回明确错误（建议 `ArgumentError` 或 HTTP 4xx）。
- [x] 保持默认自动生成路径逻辑不变。

**验收标准:**
- 非法路径（如 `..` 逃逸）被拒绝。
- 合法路径与默认路径行为保持可用。

### Task A2: 最小验证

**Files:**
- Test: `tests/unit/simulation/test_pnjl_scan_jobs.jl`（新增）

- [x] 增加“合法路径通过”测试。
- [x] 增加“越界路径拒绝”测试。

**Run:**
- `julia --project=. -e 'ENV["UNIT_FILES"]="simulation/test_pnjl_scan_jobs.jl"; include("tests/unit/runtests.jl")'`

---

## 阶段 B：可测试性补齐（P1）

### Task B1: 建立 scan jobs 契约测试骨架

**Files:**
- Create: `tests/unit/simulation/test_pnjl_scan_jobs.jl`

- [x] 覆盖 create/status/result 基本生命周期。
- [x] 覆盖队列上限返回（429 或约定错误码）。
- [x] 覆盖参数门禁：`max_retries`、`xi_grid`。

### Task B2: 关联回归入口

**Files:**
- Modify: `tests/unit/runtests.jl`（如需注册新文件）

- [x] 确保 selector 能单独运行新测试。
- [x] 确保 smoke 模式不会显著拖慢。

**验收标准:**
- 新增测试可稳定复现。
- 不依赖外部服务与网络。

---

## 阶段 C：文档口径一致性（P1）

### Task C1: 清理旧路径引用

**Files（示例）:**
- Modify: `docs/api/PARAMETER_TYPES_API.md`
- Modify: `docs/guides/PARAMETER_STRUCT_MIGRATION.md`

- [x] 扫描 `docs/` 中 `src/pnjl/workflows` 残留引用。
- [x] 替换为当前主线 `src/models/workflows` 对应路径。
- [x] 保留必要迁移说明（若确需兼容层历史注释）。

### Task C2: 文档治理校验

- [x] 运行文档一致性脚本并记录结果。

校验记录：
- `julia --project=. scripts/dev/check_docs_consistency.jl` -> `OK`

**Run:**
- `julia --project=. scripts/dev/check_docs_consistency.jl`

**验收标准:**
- 文档口径与主线目录一致。
- 治理脚本通过。

---

## 阶段 D：交付收口

### Task D1: 汇总与 PR

- [ ] 准备 PR 摘要（A/B/C 三段：为什么改、行为变化、如何验证）。
- [ ] 附最小验证证据（单测命令 + 结果）。
- [ ] 标注风险关闭项（P0/P1）与未做项（P2）。

## 5. 里程碑

- M1：A 完成（安全风险关闭）。
- M2：B 完成（scan jobs 最小测试闭环）。
- M3：C 完成（文档口径统一）。
- M4：D 完成（可审阅交付）。

## 6. DoD（完成定义）

- [x] `output_path` 越界写路径被禁止，且有测试证明。
- [x] `pnjl_scan_jobs` 具备最小契约测试并可稳定运行。
- [x] docs 旧路径清理完成并通过一致性脚本。
- [ ] PR 描述包含验证证据与剩余风险说明。

## 7. 风险与回退

- 风险：路径校验过严导致已有调用方路径被拒。
  - 回退：保留明确白名单策略并在错误中提示允许路径模板。
- 风险：scan tests 引入时间波动。
  - 回退：控制样例规模，保持纯单元级无外部依赖。

## 8. 执行建议

- 建议按 A -> B -> C 顺序推进。
- 每个阶段独立提交，便于审阅与回滚。

## 9. 当前执行状态（2026-03-27）

- 阶段 A：已完成（路径安全收口 + 对应测试通过）。
- 阶段 B：已完成（新增 `test_pnjl_scan_jobs.jl`，覆盖生命周期/队列上限/参数门禁并纳入 smoke）。
- 阶段 C：已完成（核心文档旧路径引用已清理，治理脚本通过）。
- 阶段 D：进行中（待生成 PR 摘要与证据汇总）。
