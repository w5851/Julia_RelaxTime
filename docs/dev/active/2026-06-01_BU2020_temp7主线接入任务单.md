# BU2020/temp7 介子 Beth-Uhlenbeck 数密度主线接入任务单

更新日期：2026-06-01

## 背景与目标

本任务按 `D:\Desktop\prompt_temp.txt` 的边界，把 `temp7/particles2020_bu_kpi_independent_audit` 中已验证的 BU2020 证据接入主线。目标不是复制 `temp7`，而是在现有 `Models` / `MesonDensityWorkflow` / `MesonDensity` 链路内形成可审计、可配置、可验证的生产与诊断口径。

文献对象：

- Blaschke 等，2020，`Using the Beth-Uhlenbeck Approach to Describe the Kaon to Pion Ratio in a 2 + 1 Flavor PNJL Model`

证据路径：

- `D:\Desktop\Julia_RelaxTime\temp7\particles2020_bu_kpi_independent_audit`

## temp7 证据边界

- [x] 已读取 `README.md`、`literature_facts.md`、`FINAL_VERDICT.md`、`FIG2_BRANCH_SELECTION_NOTE.md`、`FIG4_NOANOM_PRESCRIPTION.md`。
- [x] FIG1 / FIG2 当前 temp7 verdict 为已复现成功。
- [x] FIG2 工作口径固定为 `T=90 MeV`、left panel `mu_q=350 MeV`、`mu_s=0.2mu_q`、high-`mu` hadron-like stationary branch、`eta=0 + pv_b0`、inverse-propagator phase、显示层 `fold_0_pi`。
- [x] FIG3 / FIG4 density layer 仍有文献未明示口径，不能把 `excitation_only_E_gt_mu`、`x_min`、`phase_side_stationary`、`low_energy_branch_subtraction` 直接升级为文献事实。
- [x] `E <= mu_M` 的 Bose pole 与 `B0/I2` 传播子奇点不是同一个问题，主线命名和文档必须分开。

## 主线已有实现盘点

- [x] 已有统一入口：
  - `Models.solve_gap_and_meson_density_point`
  - `Models.solve_gap_and_strict_bw_meson_density_point`
  - `Models.solve_gap_and_phase_shift_meson_density_point`
  - `Models.solve_phase_shift_meson_density_from_meson_point`
  - `Models.solve_phase_shift_derivative_reference_from_meson_point`
- [x] 已有 `MesonDensity.phase_shift_meson_number_density` 与 `phase_shift_meson_density_summary`。
- [x] 已有 `MesonDensity._phase_shift_scheme_symbol(:current/:gbu_reference)`，`gbu_reference` 对应 `delta - sin(2delta)/2`。
- [x] `OneLoopIntegrals.B0` 已在实数路径中实现 PV 实部与解析虚部。
- [x] `MesonChemicalProfiles` 已支持 `pi_plus/pi_minus/K_plus/K_minus` 与 `mu_u_minus_mu_s_signed`。
- [x] `FlavorChemicalProfiles` 已有 `blaschke2017_mu_s_0p2`，可复用为 `mu_s=0.2mu_q`。
- [x] phase-shift density helper 已把 `real_axis_mode=:finite_eta` 与 `:pv_b0_eta0` 显式分派。
- [x] phase-shift density helper 已把 Bose-domain policy 写入结果 metadata。
- [x] 已新增 BU2020 K+/pi+ 与 K-/pi- signed-`mu_K` profile，避免用 K- profile 冒充 K+。

## 接入范围与非范围

范围：

- [x] 为 phase-shift density 增加显式 `real_axis_mode`，区分 finite-eta smoothing 与 PV real-axis branch。
- [x] 为 phase-shift density 增加显式 `density_policy`，默认审计口径能标记 `E <= mu_M` unsafe。
- [x] 增加 BU2020 charged meson chemical profiles，覆盖 `K+/pi+` 与 `K-/pi-`、`mu_pi=100/134.5 MeV`、signed `mu_K`。
- [x] 产出一个主线多口径 CSV/summary，输出 stable、strict BW、phase-shift current、phase-shift GBU、charged channel 与 safety metadata。
- [x] 补充 unit / integration smoke 与 API 文档。

非范围：

- [x] 不复制 `temp7` 脚本进 `src/`。
- [x] 不把 FIG3/FIG4 诊断口径宣称为文献事实。
- [x] 本轮不改 mixed-meson governance、non-fixedmu unified joint-solve 语义，也不引入新的 FixedMu 分支选择策略；FixedMu 继续使用项目既有默认分支选择。
- [x] 本轮不更新 numerical regression baseline；本次只接入新口径与审计输出，未建立稳定数值 baseline。

## 公式到代码路径映射

| 口径 | 公式/定义 | 主线路径 | 状态 |
| --- | --- | --- | --- |
| stable | `d_M int dq q^2/(2pi^2) g(E_M-mu_M)` | `MesonDensity.stable_meson_number_density` | 已有 |
| strict_bw_stage1_reduced | reduced strict BW shell kernel | `MesonDensity.strict_bw_meson_number_density` | 已有 |
| strict_bw_stage2_qpole | q-dependent pole + strict BW kernel | `MesonDensity.strict_bw_qpole_meson_number_density` | 已有 |
| phase_shift_current | by-parts BU, weight `delta` | `MesonDensity.phase_shift_meson_number_density(...; scheme=:current)` | 已接入 real-axis/policy metadata |
| phase_shift_gbu_reference | by-parts BU, weight `delta - sin(2delta)/2` | `MesonDensity.phase_shift_meson_number_density(...; scheme=:gbu_reference)` | 已接入 real-axis/policy metadata |
| derivative reference | `g d(delta)/domega` 交叉检查 | `phase_shift_meson_number_density_derivative_reference` | 已有，不作为本轮生产默认 |

## real-axis / PV 计划

- [x] 增加 `real_axis_mode=:finite_eta`，保留 legacy 默认 `eta=1e-6`，并禁止 `eta <= 0` 伪装成 finite-width backend。
- [x] 增加 `real_axis_mode=:pv_b0_eta0`，固定 `eta=0`，通过 `polarization_aniso` 走实轴 B0 PV/analytic-imaginary 路径。
- [x] 结果 metadata 输出 `real_axis_mode`、`eta`、`polarization_backend`、`phase_convention`。
- [x] 保留 `phase_convention=:arg_propagator` legacy，并新增 `:arg_inverse_propagator` 作为 BU2020 诊断口径。

## density prescriptions

- [x] `stable`
- [x] `strict_bw_stage1_reduced`
- [ ] `strict_bw_stage2_qpole` 本轮未纳入 compact audit CSV；主线能力已有，后续如需 q-pole 对照应单独扩大运行预算。
- [x] `phase_shift_current`
- [x] `phase_shift_gbu_reference`
- [x] `phase_shift_derivative_reference` 仅保留为交叉检查 helper，不进入默认生产 CSV。

每条输出必须记录 `qmax/q_nodes/omega_min/omega_max/omega_nodes`、`real_axis_mode`、`eta`、`phase_scheme`、`density_policy`、`charge_resolved` 与 status/message。

## charged-channel 与化学势约定

- [x] 主线支持 `pi_plus/pi_minus/K_plus/K_minus` channel label。
- [x] `mu_u=mu_d=mu_q`、`mu_s=0.2mu_q` 可由 `blaschke2017_mu_s_0p2` 给出。
- [x] `mu_K=mu_u-mu_s` 与反向通道符号相反可由 `mu_u_minus_mu_s_signed` 给出。
- [x] 新增 BU2020 K+/pi+ 与 K-/pi- profile，避免用 K- profile 冒充 K+。

## Bose-domain policy

- [x] `:strict_normal_domain`：默认生产/审计口径；若积分支持触及 `omega <= mu_M`，返回 unsafe status，不静默跳过。
- [x] `:excitation_only_E_gt_mu`：显式诊断口径，只积分 `omega > mu_M`。
- [x] `:x_min_cut`：可选诊断口径，按 `(omega-mu)/T > x_min` 避开 Bose pole 邻域。
- [x] 输出 `unsafe_bose_count`、`min_E_minus_mu`、`bose_x_min`、`status`、`message`。

## no-anomalous policy

- [x] no-anomalous 已接入为显式 reconstructed diagnostic policy：`noanom_policy=:low_energy_branch_subtraction`。
- [x] 该口径按 temp7 审计定义，在 `K_plus` folded `0..pi` display phase 上删除 positive-energy Landau window 内的低能正相位连通分量，并输出 Landau-window 与 removed-component metadata。

## 输出 CSV / summary 字段

目标输出目录：

- `data/outputs/results/relaxtime/meson_density/bu2020_temp7_mainline_audit/`

最小 CSV 字段：

- state：`T_MeV`, `muq_MeV`, `muB_MeV`, `xi`, `mu_u_MeV`, `mu_d_MeV`, `mu_s_MeV`
- channel：`pi_channel`, `k_channel`, `charge_resolved`
- chemical：`mu_pi_MeV`, `mu_K_MeV`, `chemical_profile`, `flavor_profile`
- prescription：`regime`, `phase_scheme`, `real_axis_mode`, `eta`, `density_policy`, `noanom_policy`
- integration：`qmax`, `q_nodes`, `omega_min`, `omega_max`, `omega_nodes`
- result：`n_pi`, `n_K`, `kpi_ratio`
- safety/diagnostics：`unsafe_bose_count`, `min_E_minus_mu`, `bose_x_min`, `noanom_removed_component_count`, `noanom_landau_omega_min/max`, `status`, `message`

## 测试与验证策略

- [x] unit：symbol parsing、real-axis mode guard、Bose-domain policy、charged `mu_K` mapping。
- [x] unit：`real_axis_mode=:pv_b0_eta0` 的结果 metadata 不等同于 finite-eta backend。
- [x] integration smoke：phase-shift scan 与 BU2020 audit script 能输出多口径 CSV。
- [x] regression：本轮不更新 baseline；尚未把 BU2020 density 诊断口径升级为固定点回归承诺。
- [x] validation：FIG3/FIG4 未明示口径不作为 pass/fail 真值。

推荐运行：

```powershell
julia --project=. -e 'ENV["UNIT_PROFILE"]="smoke"; include("tests/unit/runtests.jl")'
julia --project=. -e 'ENV["UNIT_FILES"]="relaxtime/test_meson_density.jl,models/test_meson_density_workflow.jl,models/test_meson_chemical_profiles.jl"; include("tests/unit/runtests.jl")'
julia --project=. -e 'ENV["INTEGRATION_FILES"]="relaxtime/test_phase_shift_meson_density_scan_smoke.jl"; include("tests/integration/runtests.jl")'
```

本轮已运行：

```powershell
julia --project=. -e 'ENV["UNIT_FILES"]="relaxtime/test_meson_density.jl"; include("tests/unit/runtests.jl")'
julia --project=. -e 'ENV["UNIT_FILES"]="models/test_meson_density_workflow.jl,models/test_meson_chemical_profiles.jl,models/test_flavor_chemical_profiles.jl"; include("tests/unit/runtests.jl")'
julia --project=. -e 'ENV["INTEGRATION_FILES"]="relaxtime/test_phase_shift_meson_density_scan_smoke.jl"; include("tests/integration/runtests.jl")'
julia --project=. scripts/relaxtime/run_bu2020_meson_density_audit_scan.jl --output-dir data/outputs/results/relaxtime/meson_density/bu2020_temp7_mainline_audit --overwrite --p-num 4 --t-num 2 --max-iter 8 --qmax 3 --q-nodes 3 --omega-max 2 --omega-nodes 3
```

## DoD

- [x] 任务单真实反映完成/未完成状态。
- [x] `real_axis_mode=:finite_eta` 与 `:pv_b0_eta0` 在 API 与 metadata 中可区分。
- [x] `eta=0` 不会静默走 finite-width backend。
- [x] Bose unsafe 区域不会被默认静默 clamp/skip。
- [x] charged K+/K- 与 pi+/pi- 化学势 profile 清晰可审计。
- [x] 至少产出一个主线多口径 CSV 和 summary。
- [x] 相关 unit/integration smoke 通过或有明确失败原因。
- [x] `docs/api/` 和脚本说明按实际公开契约更新。

## 风险与下一步

- `pv_b0_eta0` 的数值一致性只能证明主线调用到了 B0 PV/analytic branch；是否完全复刻 FIG2 还依赖上游 branch selection。
- FIG3/FIG4 density policy 仍受文献未明示信息限制，输出必须保留 diagnostic 标签。
- no-anomalous 已按 temp7 `low_energy_branch_subtraction` 接入为显式诊断 policy；后续风险集中在扩大路径扫描/四口径生产脚本封装时的输出合同治理。
