# 介子数密度与 BU 工作流任务单

更新日期：2026-04-30

当前状态：已完成文献获取与公式口径整理；已完成稳定粒子极限与 BW 过渡验证；已完成 Phase E4 最小比较口径建立；已开始 Phase E5 更严格 BU 收口与数量级差异根因分析；当前更合理的下一步仍不是直接进入 Phase F。

> 目的：固化“介子数密度 / Kπ 比率 / Beth-Uhlenbeck (BU) 方法”这条新主线的工作流，明确先后顺序、范围边界、验证口径与暂缓事项，避免直接跳入实现导致链路失控。

---

## 1. 背景与目标

- 当前 Mott 论文主线已经完成一轮收口，正文展示边界已基本明确。
- 下一阶段拟转向介子数密度、`K/\pi` 数密度比值，以及后续可能的介子压强 / EOS 扩展。
- 仓库当前**没有现成的 BU / phase shift / meson number density 模块**，但已经具备可复用的 Mott / 极化函数 / 传播子底座：
  - `src/relaxtime/MesonMass.jl`
  - `src/relaxtime/PolarizationAniso.jl`
  - `src/relaxtime/MesonPropagator.jl`
  - `scripts/relaxtime/run_mott_phase_scan.jl`

本任务单的目标不是立刻实现，而是把后续执行顺序固定为：

- [x] 先确定总体工作流
- [ ] 后续按工作流逐步推进

核心顺序：

`文献整理 -> 公式与记号统一 -> 最小问题定义 -> BW/稳定极限验证 -> BU 相移实现 -> 数值扫描与图 -> 再考虑 EOS`

---

## 2. 范围与非范围

### 2.1 本期范围（当前文档覆盖）

- [x] 固化介子数密度主线的分阶段工作流
- [x] 明确每阶段输入、输出、验收标准
- [x] 明确 BU 实现前必须完成的降阶验证步骤
- [x] 明确 EOS 扩展不是当前并行任务

### 2.2 非范围（当前不做）

- [ ] 不直接跳过 workflow 对齐而扩张 `src/` 下的新接口实现
- [ ] 不直接设计完整 BU 数值接口并落代码
- [ ] 不并行推进介子压强 / EOS
- [ ] 不在本轮处理论文相关未提交改动

---

## 3. 现状盘点

### 3.1 已有能力

- [x] `π/K` 介子质量、宽度、Mott 阈值扫描已具备
- [x] `Π(\omega)` 数据导出链路已具备可选扩展
- [x] 极化函数与复极点机制已有独立分析文档
- [x] 论文主线与图像边界已收口到“两张核心图”

### 3.2 当前缺口

- [x] 已有稳定粒子极限介子数密度统一入口
- [x] 已有稳定粒子极限 `K/\pi` 数密度比值计算模块
- [x] 已有 Phase E3 最小 BU 相移计算链路
- [x] 已有围绕该主线的最小验证脚本 / 测试 / benchmark / 扫描标准
- [ ] 仍缺 stable / BW / Phase-E3 三条链的正式比较口径收口
- [ ] 仍缺基于当前扫描入口的最小图形集与 Phase F 图形收口

### 3.3 关键判断

- [x] 不能直接从现有 Mott 结果跳到 BU 全实现
- [x] 必须先完成文献与公式口径统一
- [x] 必须先完成 BW / 稳定极限验证，确认单位、简并因子、积分域、化学势口径和数值量级都正确

### 3.4 当前 workflow 对齐结论

- [x] 现有主 workflow 已由 `src/models/workflow_apps/MesonMassWorkflow.jl` 提供
- [x] `solve_gap_and_meson_point` 已经完成：
  - 平衡态求解
  - `quark_params` / `thermo_params` 构造
  - `meson_results`（质量、宽度、阈值、gap）返回
- [x] 当前扫描脚本 `scripts/relaxtime/run_gap_meson_mass_scan.jl` 与 `scripts/relaxtime/run_mott_phase_scan.jl` 都是消费该 workflow 返回值，而不是各自重写介子求解链
- [x] 因此介子数密度相关计算在代码流程上应放在“介子质量/阈值结果之后”作为后处理层，优先消费 `(quark_params, thermo_params, meson_results)`，而不应先在 script 层平行扩出另一条独立链路

---

## 4. 分阶段任务单

### Phase A：文献整理

- [x] A1 整理当前已导入 bib 中与介子数密度 / `K/\pi` 比率 / BU 直接相关的核心文献清单
  - 验收：形成“核心文献 / 候补文献 / 暂不使用文献”三类列表
- [ ] A2 提取每篇核心文献中的关键公式、适用条件和符号定义
  - 验收：形成统一摘录表，至少覆盖 `n_M`、`δ_M`、`g_M`、`d_M`、`μ_M`
- [x] A3 标注这些文献各自采用的是稳定粒子、BW 近似还是 BU 方法
  - 验收：每篇文献的“方法类型”与“可复用部分”明确可见

#### Phase A 当前归类结果（基于 `paper_lib/bib/介子数密度(及其比值).bib`）

核心文献：

- [x] `Blaschke:2016sqn`
  - 方法类型：BU
  - 可复用部分：`2+1` flavor PNJL 下 `π/K` Mott、相移、Levinson 定理、介子热力学主线
- [x] `Blaschke:2019col`
  - 方法类型：BU
  - 可复用部分：`K/\pi` 比值、`K^+` 异常模、从相移到比值的最短链路
- [x] `Blaschke:2020bzh`
  - 方法类型：BU + BW 对比
  - 可复用部分：BU 与 BW 的比较口径、`K/\pi` 比值、沿相变/冻结线的现象学解释
- [x] `Blaschke:2021yml`
  - 方法类型：BU + BW 对比
  - 可复用部分：EPNJL 版本的 `K/\pi` 比值、BU 与极点近似差异

候补文献：

- [x] `Friesen:2019ojk`
  - 方法类型：偏稳定粒子 / 极点口径（非完整 BU）
  - 可复用部分：SU(3) PNJL 下 `K/\pi` 比值的前置现象学背景
- [x] `costaPseudoscalarMesonsHot2004`
  - 方法类型：介子谱 / 热致密介质中的质量演化，不是数密度主文献
  - 可复用部分：`π/K` 道的介子谱与热介质输入背景
- [x] `Costa:2005cz`
  - 方法类型：谱与对称性恢复分析，不是数密度 / BU 主文献
  - 可复用部分：标量-赝标量谱、混合与恢复背景

暂不作为当前主线文献：

- [x] 仅提供介子谱、对称性恢复或混合角信息、但不直接给出 `n_M` / `K/\pi` / BU 热力学公式的文献
  - 当前代表：`Costa:2005cz`
  - 处理原则：保留为背景参考，不进入本轮最小公式摘录集合

### Phase B：公式与记号统一

- [x] B1 统一项目内与文献中的温度、化学势、能量、动量、相移、简并因子记号
  - 验收：形成一份“项目记号对照表”
- [x] B2 明确 `π` 与 `K` 道的简并因子、化学势设定、`μB=0` 下的默认约束
  - 验收：写出一套当前项目采用的最小物理口径
- [x] B3 明确数密度积分中采用 `\omega`、`q`、相移导数还是分部积分后的 `δ_M` 本体形式
  - 验收：给出“优先实现形式 + 不选另一形式的原因”

#### Phase B 当前结论

- [x] 已形成 `Phase B` 对照文档：
  - `docs/dev/archived/2026-05-01_PhaseB_介子数密度记号对照与最小物理口径.md`
- [x] 已固定项目主口径：
  - `π/K` 简单介子道的主传播子与极点条件以当前仓库 `1 - 4K\Pi = 0` 为准
  - 文献中的 `1 - 2P\Pi = 0` 只作对照，不替代项目主定义
- [x] 已固定 `μB = 0` MVP 的最小物理约束：
  - 平衡态默认 `\mu_u=\mu_d=\mu_s=0`
  - 聚合通道默认 `d_\pi = 3`、`d_K = 4`
  - 暂不引入 `\mu_\pi(x)`、`\mu_s(x)` 的现象学拟合
- [x] 已固定 BU 数值积分的当前优先实现形式：
  - 优先采用分部积分后的 `δ_M` 本体形式
  - 导数形式 `dδ_M/d\omega` 保留为理论参考与交叉验证口径

### Phase C：最小问题定义

- [x] C1 固定 MVP 范围：`μB=0`、`π/K`、静态均匀介质、只做数密度与 `K/\pi`
  - 验收：文档中明确列出本期范围
- [x] C2 明确最小输出物
  - 验收：至少包含 `n_pi(T)`、`n_K(T)`、`n_K/n_pi(T)` 或与 `T` 的关系图
- [x] C3 明确本期不触及的内容
  - 验收：写清楚“不做 EOS / 不做广义化学势扫描 / 不做 full phase diagram”

#### Phase C 当前结论

- [x] 已形成 `Phase C` 对照文档：
  - `docs/dev/archived/2026-05-01_PhaseC_介子数密度MVP范围与输出定义.md`
- [x] 已固定当前 MVP 的最小问题定义：
  - `μB = 0`
  - `π/K`
  - 静态、均匀介质
  - 仅输出 `n_pi(T)`、`n_K(T)`、`n_K/n_pi(T)`
- [x] 已固定当前最小交付物口径：
  - 一个可重复运行的最小数密度计算入口
  - 一个温度扫描结果表
  - 一张 `n_pi(T)` / `n_K(T)` 对照图
  - 一张 `n_K/n_pi(T)` 图
- [x] 已固定当前明确不做的内容：
  - EOS / 介子压强扩展
  - 广义化学势扫描
  - 完整 `T-μB` 相图
  - 冻结线现象学拟合（含 `μ_π(x)`、`μ_s(x)`）
  - 在数密度主线未稳定前做大而全接口抽象

### Phase D：BW / 稳定极限验证

- [x] D1 先实现稳定粒子极限数密度公式
  - 验收：能独立生成 `n_pi(T)`、`n_K(T)` 基线曲线
- [x] D2 在稳定极限上核对单位与量级
  - 验收：数值量级、温度趋势、`K/\pi` 比值方向合理
- [x] D3 若需要，再实现 BW 近似版本作为过渡验证
  - 验收：能说明 BW 相对稳定极限新增了什么、仍缺什么
- [x] D4 把 BW / 稳定极限结果与 BU 目标明确区分
  - 验收：文档中明确“这是验证台阶，不是最终目标”

### Phase E：BU 相移实现

- [x] E1 明确相移 `δ_M(\omega)` 与现有传播子 / 极化函数的关系式
  - 验收：写出项目内可落地的表达式来源
- [x] E2 评估是基于 `polarization_with_width` 还是 `meson_propagator_*` 链路构造相移
  - 验收：确定主实现入口，不允许双路径并行漂移
- [x] E3 设计最小 BU 数值积分方案
  - 验收：给出积分变量、范围、网格、数值稳定性要求
- [x] E4 给出 BU 与稳定 / BW 结果的比较口径
  - 验收：明确至少一个“应当出现的差异”
- [ ] E5 完成更严格 generalized BU / Levinson 口径收口
  - 验收：能解释当前 Phase-E3 与 stable/BW 数量级差异的主因，并明确正式升格条件

#### Phase E 当前前置结论

- [x] 已形成 Phase E 严格公式实现前置说明：
  - `docs/dev/active/2026-05-01_PhaseE_BU严格公式实现前置说明.md`
- [x] 已形成 Phase E1 最小相位诊断脚本入口：
  - `scripts/analysis/relaxtime/diagnose_bu_phase_shift_minimal.jl`
- [x] 已完成 Phase E1 首轮最小诊断跑通：
  - 输出目录：`data/outputs/results/relaxtime/analysis/bu_phase_shift_minimal/`
  - 当前默认窗口：`T = 208, 210, 212 MeV`，`q = 0`
  - 已能导出 `pi/K` 通道的 `Pi(omega)`、`D_M(omega)`、`phase_raw`、`phase_unwrapped`
- [x] 已形成 Phase E1 相位结构初判说明：
  - `docs/dev/active/2026-05-01_PhaseE1_相位结构初判说明.md`
- [x] 已形成 Phase E2 分支与高能行为初判说明：
  - `docs/dev/active/2026-05-01_PhaseE2_分支与高能行为初判说明.md`
- [x] 已形成 Phase E3 双积分收敛初判说明：
  - `docs/dev/active/2026-05-02_PhaseE3_双积分收敛初判说明.md`
- [x] 已形成 Phase E5 严格 BU 候选与根因分析说明：
  - `docs/dev/active/2026-05-02_PhaseE5_更严格BU收口与差异根因分析.md`
- [x] 已形成 strict BW 正式实现设计说明：
  - `docs/dev/active/2026-05-02_strict BW正式实现设计.md`
- [x] 已形成 strict BW Stage1 扫描结果说明：
  - `docs/dev/active/2026-05-02_strict BW Stage1扫描结果说明.md`
- [x] 当前转段判断：
  - `BW-proxy` 只保留为 D3 诊断代理，不作为后续稳定产出标准
  - 正式主线应切回 `delta_M(omega, q) = arg D_M(omega + i eta, q)`
  - 主实现入口优先依托 `PolarizationAniso` 与 `MesonPropagator` 传播子链
  - 数密度积分前，必须先完成相位分支、阈值支撑与 Levinson 约束检查
  - 当前已确认：高能端在 `omega ≈ 10` 下可自然回零，`omega` 方向尾部已出现良好衰减
  - 当前已初步确认：Phase-E3 与 stable/BW 的数量级差异主因更接近“BU 公式口径不够严格”，而不是积分窗口本身
  - strict BW 的下一实现阶段先落 `reduced strict BW` workflow 挂接，再保留 `q` 依赖复极点版本作为 Stage 2
  - generalized BU 候选 `delta - 1/2 sin(2delta)` 能将 `n_pi / n_K` 从大幅偏高压回到与 stable 同量级
  - 当前工程治理结论优先固定为：
    - 默认正式生产口径：近似 BU / 当前 `pi/K` 文献主流口径
    - 更严格参考口径：generalized BU
    - `stable/BW` 保留为降阶对照，不再要求 BU 必须数值接近它们
  - 当前剩余的主要问题已转为：如何把 generalized BU / Levinson / scattering-part 作为 stricter reference 收口，并明确近似 BU 结果的解释边界

### Phase F：数值扫描与图

- [ ] F1 设计介子数密度主图
  - 验收：至少定义横轴、纵轴、温度范围、比较对象
- [ ] F2 设计 `K/\pi` 比值图
  - 验收：明确与文献比对或内部对照目标
- [ ] F3 若 BU 落地后结果复杂，优先做最小解释图而不是一次性做大而全图组
  - 验收：图形集保持可解释、可复现、不过度扩张

### Phase G：EOS 后继扩展

- [ ] G1 只有在 Phase A-F 完成后，才重新评估介子压强 / EOS 扩展
  - 验收：形成单独后继任务，而不是隐含并入当前主线

---

## 5. 推荐执行顺序

- [ ] Step 1：先做 Phase A 文献整理
- [x] Step 2：完成 Phase B 公式与记号统一
- [x] Step 3：锁定 Phase C 最小问题
- [x] Step 4：进入 Phase D 降阶验证
- [x] Step 5：在验证通过后，再进入 Phase E BU 实现设计

说明：

- 只有当 Phase A-C 完成后，才有资格讨论“具体模块放哪、接口怎么写”。
- 只有当 Phase D 完成后，才有资格讨论“BU 数值结果是否可信”。
- 进入 `Phase D` 的下一步前，应先以现有 `MesonMassWorkflow` 为主链确定介子数密度的挂接点，再决定是否新增 workflow helper 或最小扫描脚本入口。

#### Phase D 当前进展

- [x] 已有最小稳定粒子极限模块：
  - `src/relaxtime/MesonDensity.jl`
- [x] 已有 workflow 挂接：
  - `src/models/workflow_apps/MesonDensityWorkflow.jl`
  - `Models.solve_meson_density_from_meson_point`
  - `Models.solve_gap_and_meson_density_point`
- [x] 已有最小扫描入口：
  - `scripts/relaxtime/run_meson_density_scan.jl`
- [x] D2/D3/D4 当前已完成到 Phase E 可继续推进的程度：
  - 单位与量级对账已完成
  - BW 过渡验证已完成
  - 与 BU 目标的“验证台阶”区分已形成
- [x] 已形成 D2 的最小验证脚本入口：
  - `scripts/analysis/relaxtime/validate_meson_density_stable_baseline.jl`
- [x] 已补一页 D2 验证说明：
  - `docs/dev/active/2026-05-01_介子数密度PhaseD2验证说明.md`
- [x] D2 当前最小验证结论（`xi=0, muB=0, T=150:10:190 MeV`）：
  - `n_pi(T)` 单调上升
  - `n_K(T)` 单调上升
  - `K/\pi(T)` 单调上升
  - 全部点满足 `n_pi > n_K > 0`
- [x] 已补充“数密度 workflow 不偏离介子质量 workflow”的验证方向：
  - 点内一致性：`meson_density.m_pi/m_K` 必须等于同点 `meson_results[:pi/:K].mass`
  - continuation 一致性：`T=190 MeV, xi=0` 的 continuation 求解结果应与 reference `mott_phase_scan.csv` 对齐
- [x] 已补充“扫描口径一致性”护栏：
  - density workflow 与 meson workflow 在 continuation 扫描下必须返回同一组 `pi/K` 质量结果
- [x] 已确认一个关键 workflow 风险并完成修正：
  - 若扫描脚本不继承 meson workflow continuation 状态，`K` 通道可能落到错误符号分支
  - 当前已通过 `continuation_state` helper 对齐到参考 `mott_phase` 扫描口径
- [x] D3 当前判断：
  - **建议保留 BW 过渡层**
  - 原因不是替代 BU，而是因为稳定粒子极限在接近 / 越过 Mott 区域后会失效
  - BW 层可作为“稳定极限失效后、完整 BU 实现前”的最小诊断桥梁，用于检查宽度进入后的量级与连续性
  - 进一步约束见：`docs/dev/active/2026-05-01_介子数密度PhaseD2验证说明.md`
- [x] 已形成 D3 最小设计说明：
  - `docs/dev/active/2026-05-01_介子数密度PhaseD3_BW最小设计说明.md`
- [x] 已形成 D3 最小公式口径：
  - `docs/dev/active/2026-05-01_介子数密度PhaseD3_BW最小公式口径.md`
- [x] 已形成 D3 最小分析脚本入口：
  - `scripts/analysis/relaxtime/validate_meson_density_bw_minimal.jl`
- [x] D3 当前收口原则：
  - 优先统一“扫描骨架”，不强行统一所有 workflow 的物理 continuation 状态对象
  - BW 先做分析层/最小后处理层，再决定是否升格为公共模块
- [x] D3 当前首轮试跑观察：
  - 在 `T = 150, 170, 190 MeV`、`muB = 0`、`xi = 0` 下，当前 workflow 返回 `Gamma_pi = Gamma_K = 0`
  - 因此该窗口内 `n_M^BW` 数值上退化为 `n_M^stable`
  - 参考 `mott_phase_scan.csv` 与当前 D3 脚本试跑都表明：`T = 210 MeV` 时宽度已非零
  - 当前最小脚本在 `T = 210 MeV` 给出：
    - `Gamma_pi ≈ 0.1743`
    - `Gamma_K ≈ 0.3409`
    - `K/pi: 0.57565 -> 0.59479`
    - `delta(K/pi) ≈ +0.01914`
  - 局部补点扫描 `T = 200:2:220 MeV` 的当前观察：
    - `200-206 MeV`：`BW = stable`
    - `208 MeV`：开始出现可见修正，`delta(K/pi) ≈ +0.00966`
    - `210-220 MeV`：修正持续增大，至 `220 MeV` 达到 `delta(K/pi) ≈ +0.04035`
  - 因此 D3 已进入“宽度修正开始可见”的窗口，下一步应围绕 `T ≈ 210 MeV` 及其邻域做局部补点，而不是盲目扩张 BW 实现

#### Phase E 当前进展

- [x] 已有最小相位诊断脚本入口：
  - `scripts/analysis/relaxtime/diagnose_bu_phase_shift_minimal.jl`
- [x] 已有 `omega` 方向最小积分诊断脚本入口：
  - `scripts/analysis/relaxtime/diagnose_bu_omega_integrand_minimal.jl`
- [x] 已有 `q + omega` 双积分最小诊断脚本入口：
  - `scripts/analysis/relaxtime/diagnose_bu_double_integral_minimal.jl`
- [x] E1/E2 当前最小结论：
  - `delta_M(omega, q) = arg D_M(omega + i eta, q)` 主线可读
  - 高能端在 `omega ≈ 10` 时已经自然趋近 `0`
  - 固定 `q = 0` 时，`omega` 方向积分核尾部已衰减到 `1e-11` 量级
- [x] E3 当前最小诊断结论（`xi = 0`, GL + 硬截断）：
  - `qmax = 8 -> 10 -> 12` 时，末端壳贡献已从 `~3e-2` 下降到 `~1e-3`
  - 说明当前真正需要继续收敛检查的是 `q` 方向，而不是 `omega` 高能尾部
  - 在 `qmax = 12` 下把 `q` 节点数从 `48` 提到 `64` 后，双积分估计改变量约为 `O(1%)`
- [x] E3 当前默认诊断口径建议：
  - `qmax = 12`
  - `q_nodes = 48`
  - `omega_max = 10`
  - `omega_nodes = 48`
- [x] 已补最小 BU 双积分 benchmark 入口：
  - `benchmark/relaxtime/bench_bu_double_integral_phase_e3.jl`
  - 当前可同时给出：
    - 单点绝对耗时
    - `equilibrium` 与 `post-equilibrium double integral` 在完整点计算中的相对占比
- [x] 已把 Phase-E3 数值内核提升到正式 helper / workflow 层：
  - `src/relaxtime/MesonDensity.jl`
    - `phase_shift_meson_number_density`
    - `phase_shift_meson_density_summary`
  - `src/models/workflow_apps/MesonDensityWorkflow.jl`
    - `solve_phase_shift_meson_density_from_meson_point`
    - `solve_gap_and_phase_shift_meson_density_point`
- [x] 已让 analysis / benchmark / workflow 共用同一套 Phase-E3 双积分数值内核，减少脚本实现与正式实现漂移
- [x] 已补最小正式扫描入口：
  - `scripts/relaxtime/run_phase_shift_meson_density_scan.jl`
  - 当前输出字段已覆盖：
    - `n_pi(T)` / `n_K(T)` / `K/pi(T)`
    - 当前 `q/omega` 默认积分配置
    - `pi/K` 两个通道的 `q_integral_estimate`
    - `omega_shell_at_qmax`
- [x] 已跑首轮最小正式扫描结果：
  - 输出：`data/outputs/results/relaxtime/scan/phase_shift_meson_density_scan_208_220_step2.csv`
  - 窗口：`T = 208:2:220 MeV`, `muB = 0`, `xi = 0`
- [x] 已补 Phase E3 结果字段与最小扫描说明：
  - `docs/dev/active/2026-05-02_PhaseE3_结果字段与最小扫描说明.md`
- [x] 已补 Phase E4 稳定 / BW / Phase-E3 比较口径说明：
  - `docs/dev/active/2026-05-02_PhaseE4_稳定BW与PhaseE3比较口径说明.md`
- [x] 已补 Phase E4 同口径并排比较 CSV：
  - `data/outputs/results/relaxtime/analysis/meson_density_phase_e4_compare.csv`
- [ ] Phase E 当前后续判断：
  - 虽然 `E1-E4` 最小任务已经落地，但当前比较结果显示 `Phase-E3` 与 stable / BW 存在数量级级别差异
  - 因此当前更合理的下一步不是直接进入 Phase F，而是优先继续更严格 BU 收口

#### 当前阶段判断

- [x] Phase A-C 已完成
- [x] Phase D 已完成到可作为 Phase E 前置验证台阶的程度
- [x] Phase E 的 `E1-E4` 最小任务已经落地
- [ ] Phase E 尚未进入“可直接转入 Phase F 展示”的状态
  - 当前缺口不是“没有实现”，而是比较结果提示还需继续更严格 BU 收口
  - 因此当前不建议把 Phase E 的现有数值直接包装成正式图形主结果
- [ ] Phase F 尚未开始正式收口
  - 当前虽已有最小正式扫描结果，但现阶段仍不建议直接推进正式图形集

---

## 6. 验收标准

- [ ] 工作流顺序明确且不自相矛盾
- [ ] 每一阶段都有可见输入、输出、验收口径
- [ ] 能区分“验证台阶”和“最终目标”
- [ ] EOS 被明确后置，不与当前主线并行
- [ ] 后续执行时可以直接按本文档逐项勾选推进

---

## 7. 风险与防跑偏约束

- [ ] 风险 R1：直接跳入 BU 实现，导致公式、记号、单位、相移定义混乱
  - 约束：必须先完成 Phase A-C
- [ ] 风险 R2：把 BW / 稳定极限误当成最终结果
  - 约束：实现时必须显式标注“验证台阶”
- [ ] 风险 R3：数密度主线与 EOS 扩展并行，导致任务失焦
  - 约束：EOS 单独后置
- [ ] 风险 R4：实现路径和现有 Mott / 极化函数主路径脱节
  - 约束：优先复用现有 `MesonMass` / `PolarizationAniso` / `MesonPropagator` 能力

---

## 8. 与第 16 点的关系

- [x] 当前不直接处理未提交改动
- [x] 当前只固化工作流与后续顺序
- [ ] 待与用户讨论后，再单独处理：
  - 哪些未提交改动属于 Mott 论文主线保留项
  - 哪些属于极化函数支线分析资产
  - 哪些临时数据 / 图 / 脚本可以迁移、归档或清理

---

## 9. DoD

满足以下条件即可视为本轮“工作流固化”完成：

- [x] 人工说明文档已同步更新到最新讨论结论
- [x] BU / 介子数密度工作流已固化为独立任务单
- [x] 已明确下一步应先做文献整理，而不是直接编码
- [x] 已明确第 16 点需要先与用户讨论，不直接执行
