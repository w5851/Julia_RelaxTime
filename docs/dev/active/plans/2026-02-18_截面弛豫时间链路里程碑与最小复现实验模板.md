# 截面/弛豫时间链路里程碑与最小复现实验模板（v1）

## 里程碑定义

### M1：接口与输入口径冻结

- 目标：统一输入参数单位、字段命名与默认配置。
- 验收：`README`/`STATUS`/API 文档字段一致，样例请求可复现。

### M2：固定点正确性回归

- 目标：在固定点集上保证 `η/σ/ζ` 与基线一致。
- 验收：满足 `rtol=8e-2`、`atol=1e-6`。

### M3：截面与散射率稳定性

- 目标：总截面、平均散射率在典型网格上无异常跳变。
- 验收：无 NaN/Inf，关键曲线连续性满足阈值。

### M4：跨实现一致性

- 目标：与历史 Fortran/旧基线报告进行对照。
- 验收：偏差在约定容差内并形成归档报告。

## 最小可复现实验模板

### 1) 实验元信息

- 实验名称：
- 日期：
- 执行人：
- 代码版本（commit）：
- 配置文件：

### 2) 输入

- 固定点：$(T,\mu,\xi)$ 列表
- 数值参数：积分节点、截断参数、seed 策略

### 3) 执行命令

```powershell
julia --project=. <script>.jl --config <config.toml>
```

### 4) 输出

- 结果文件：`data/outputs/results/...`
- 图/表文件：`data/outputs/figures/...`

### 5) 对比与判定

- 对比基线来源：
- 判定容差：`rtol` / `atol`
- 结论：通过 / 不通过

### 6) 归档

- 归档文档路径：`docs/dev/archived/YYYY-MM-DD_<name>.md`
- 必含内容：输入、命令、输出摘要、结论、后续动作
