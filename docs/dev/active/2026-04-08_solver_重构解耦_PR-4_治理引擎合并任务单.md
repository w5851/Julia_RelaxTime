# Solver 重构解耦 PR-4 任务单（治理引擎合并）

日期：2026-04-08
关联总纲：`docs/dev/active/2026-04-08_solver_重构解耦总纲领与PR路线图.md`
阶段目标：收敛治理语义为单主引擎，明确唯一排序规则与执行入口。

## 0. 协作与分支规则（本 PR 强制）

- 合并方式：`squash + delete`。
- 开工前：从最新 `main` 新建分支（建议：`refactor/solver-pr4-governance-unify`）。
- 主分支纪律：保持 `main` 干净，不在 `main` 直接开发。
- 工作区策略：本项目本 PR 不新建 worktree，直接在当前工作区切分支执行。
- 收尾：PR 合并后删除远端分支，并在本地删除已合并分支后回到 `main`。

## 1. 范围与非范围

### 前置条件

- 基线分支：`main` 已同步远端最新提交。
- 依赖关系：PR-3 已合并（ModeKernel 统一完成）。
- 开工检查（建议执行）：
  - `git switch main && git pull --ff-only`
  - `git switch -c refactor/solver-pr4-governance-unify`

### 范围

- 确立治理主引擎（推荐：以 `execute_attempt_pool` 为主）。
- 吸收 `RootPolicy` 的质量分级语义（good/degraded/bad/fallback）到主引擎。
- 明确唯一候选排序规则：
  1) `hard_constraint_ok`
  2) `residual_norm`
  3) `pressure` 或显式 score
  4) `seed_index` 稳定 tie-break
- 对次级引擎做降级封装或逐步退役。

### 非范围

- 不在本 PR 同时做大规模 mode 核重写（已由 PR-3 处理）。
- 不修改上游业务工作流接口。

## 2. 任务分解（可勾选）

- [ ] 完成治理主引擎选型决策记录（文档化）。
- [ ] 在主引擎补齐质量标签与统一成功判据。
- [ ] 统一 selector 的输入契约字段，消除隐式字段依赖。
- [ ] 为次级引擎提供兼容转调（过渡期）或标记 deprecated。
- [ ] 统一错误分类与错误消息规约（constraint_error/solver_error 等）。
- [ ] 增补 regression：attempt 次序、selection_reason、fallback 语义回归。
- [ ] 完成文档迁移：维护者指引中删除“双引擎歧义”描述。

## 3. 验证与验收标准

- [ ] attempt/fallback/selection 仅有一套主语义。
- [ ] 关键候选选择路径回归一致（含无候选通过时的降级选择）。
- [ ] solver 相关 unit/integration/regression 全绿。
- [ ] 对外接口兼容：调用方无需改参即可运行。

建议最小验证命令（按顺序）：

- `julia --project=. -e 'ENV["UNIT_FILES"]="models/test_solver_diagnostic_contract.jl,models/test_constraint_solver.jl,models/test_solver.jl"; include("tests/unit/runtests.jl")'`
- `julia --project=. -e 'ENV["REGRESSION_PROFILE"]="full"; include("tests/regression/runtests.jl")'`
- `julia --project=. -e 'ENV["INTEGRATION_PROFILE"]="smoke"; include("tests/integration/runtests.jl")'`

## 4. 风险与回退

- 风险：治理排序语义变化引发边界点选解差异。
- 回退：保留兼容开关和旧引擎薄封装，分阶段切默认值。

## 5. PR 交付物

- 代码变更：治理引擎主路径、selector 契约、兼容封装。
- 测试变更：治理语义回归与异常路径回归。
- 文档变更：任务单、架构说明、必要 API 文档同步。

## 6. 完成信号（Completion Signal）

- [ ] 本任务单条目已全部完成并回写，再创建 PR。
- [ ] PR 创建后等待 CI 全通过。
- [ ] PR 创建后等待 GitHub Copilot 自动 review（PR 触发）并完成 review 问题处理。
- [ ] PR 已通过评审并采用 `squash + delete` 合并。
- [ ] 本地执行：`git switch main`，删除本地开发分支。
- [ ] 本任务单状态已更新（含主引擎选型记录与兼容性结论）。

## 7. 终局收口检查（PR-4 完成后）

- [ ] 四个 PR 的任务单均已完成并回写验证证据。
- [ ] 总纲 DoD 全部勾选，且与实际代码状态一致。
- [ ] solver 维护者阅读路径与 API 文档已同步到最终状态。
