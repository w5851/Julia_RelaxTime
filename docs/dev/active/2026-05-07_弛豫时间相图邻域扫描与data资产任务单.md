# 弛豫时间相图邻域扫描与 data 资产任务单

更新日期：2026-05-07

当前状态：收尾中；phase-guided sampling 入口、模式 `a / b`（推荐正式模式名 `fixed-muB-phase-scaled` / `fixed-T-sparse-muB`）的 planning 骨架、canonical case、bulk 默认开启与 regression 治理已基本落地；canonical 图层已迁到正式 `data/outputs/figures/` 路径，mode a / mode b 的正式结果已按更细 `xi` 网格固化，当前进入 PR 收尾闭环检查。

> 目的：把已有 transport 物理能力组织成正式、可重复、可回归、可出图、可沉淀到 `data/` 层的结果产品，而不是继续停留在零散脚本与临时扫描阶段。

### 0.1 当前实现进展（2026-05-07）

- [x] 新增 phase-guided 上层入口：
  - `scripts/relaxtime/run_phase_guided_transport_scan.jl`
- [x] 新增模式 `a / b` 的 sampling plan/path generator：
  - `scripts/relaxtime/phase_guided_transport_scan_plan.jl`
- [x] 复用现有逐点执行链：
  - 下层仍走 `execute_gap_transport_scan_point!`
- [x] 新增 dry-run canonical 资产合同：
  - `sampling_plan.csv`
  - `README.md`
  - `effective_config.json`
  - `run_manifest.json`
- [x] 已完成最小真实执行验证：
  - mode b 单点 case 可正式落 `phase_guided_transport_scan.csv`
- [x] 统一入口已接线：
  - `scripts/relaxtime/run_scan.jl phase-guided-transport`
- [x] 已完成 first canonical non-dry-run case 正式落盘：
  - `data/outputs/results/relaxtime/transport/phase_guided/mode_b_fixed_T_sparse_muB/first_canonical_v1/`
- [x] 已补第一批 plot / README 资产：
  - `scripts/relaxtime/run_phase_guided_transport_plots.jl`
  - `data/outputs/figures/relaxtime/transport/phase_guided/.../plot_manifest.json`
- [x] 已建立首批 phase-guided regression 覆盖：
  - `tests/baselines/relaxtime/baseline_phase_guided_transport_mode_b_v1.csv`
  - `tests/regression/relaxtime/test_phase_guided_transport_mode_b_regression.jl`
- [x] 已完成 mode a canonical case：
  - `data/outputs/results/relaxtime/transport/phase_guided/mode_a_fixed_muB_phase_scaled/first_canonical_v1/`
- [x] canonical 口径已进一步收束：
  - mode a：固定 `mu_B = 0, 450, 900 MeV`，每个 `mu_B` 一张图、图内多条 `alpha_T`
  - mode b：固定 `T = 120, 160, 200 MeV`，每个 `T` 一张图、图内多条 `mu_B`
- [x] 已补 full 级更重 regression：
  - `tests/baselines/relaxtime/baseline_phase_guided_transport_mode_b_full_v1.csv`
  - `tests/regression/relaxtime/test_phase_guided_transport_mode_b_full_regression.jl`
- [x] 已同步更上层 README / 项目入口说明：
  - `README.md`
  - `docs/guides/scripts/README.md`
- [x] `compute_bulk` 已切换为默认开启：
  - `scripts/relaxtime/gap_transport_scan_cli.jl`
  - `scripts/relaxtime/phase_guided_transport_scan_cli.jl`
- [x] 已补 `data/reference/pnjl/crossover_dense.csv` 与 `crossover_dense.meta.json`
- [x] mode a 基线温度口径已收束：
  - 统一使用各向同性 `xi = 0` 的参考相线温度作为 `T_phase_base_MeV`
  - crossover / boundary / CEP reference 中的 `mu` 口径按 `mu_q` 解释，再与外层 `mu_B` 换算对接
- [ ] 当前 `crossover_dense.csv` 仍是面向 phase-guided v1 anchor 的显式锚点 dense reference（当前覆盖 `xi = -0.5, -0.2, 0.0, 0.2`，`mu_q = 0:90:450 MeV`），尚未扩展到更全域的 dense crossover base-line

### 0.2 本轮正式结果固化（2026-05-08）

- [x] mode a / mode b 的正式 canonical case 已统一切到更细 `xi` 网格：
  - `xi = -0.5:0.05:0.5`
  - 共 21 个 `xi` 点
- [x] mode a 正式结果已重跑并固化：
  - `data/outputs/results/relaxtime/transport/phase_guided/mode_a_fixed_muB_phase_scaled/first_canonical_v1/`
  - `data/outputs/figures/relaxtime/transport/phase_guided/mode_a_fixed_muB_phase_scaled/first_canonical_v1/`
  - 点数：`21 x 3(mu_B) x 3(alpha_T) = 189`
- [x] mode b 正式结果已重跑并固化：
  - `data/outputs/results/relaxtime/transport/phase_guided/mode_b_fixed_T_sparse_muB/first_canonical_v1/`
  - `data/outputs/figures/relaxtime/transport/phase_guided/mode_b_fixed_T_sparse_muB/first_canonical_v1/`
  - 点数：`21 x 3(T) x 3(mu_B) = 189`
- [x] mode a 图例已补具体温度：
  - 线标签不再只显示 `alpha_T`
  - 当前同时显示 `alpha_T` 与对应的 `T = alpha_T * T_phase_base`

### 0.3 PR 收尾前剩余闭环（2026-05-08）

- [x] 判断是否要把更细 `xi` 网格同步纳入 regression baseline
  - 当前 user-facing canonical 结果已升级到 21 点 `xi` 网格
  - 但 `tests/regression/` 中的 mode b baseline 仍是较轻的固定点集合
  - 本轮结论：**不纳入**；本 PR 维持现有 regression 覆盖范围
- [x] 判断是否要把 `crossover_dense.csv` 从锚点 reference 扩展到与正式 `xi` 网格一致
  - 当前 mode a 的 base 线只依赖 `xi=0`，因此正式结果已可稳定使用
  - 但 mode b 的 phase diagnosis / crossover proximity 对非锚点 `xi` 仍会走 nearest-anchor reference
  - 本轮结论：**暂不扩展**
  - 后续任务建议：优先设计基于 GitHub Actions 的 reference 生成与 artifact 固化流程，避免本机长时间运行
- [x] 更新 PR 说明中的最终正式口径
  - 明确写入 `xi = -0.5:0.05:0.5`
  - 明确写入 mode a / mode b 的最终正式结果目录与点数
  - Draft PR #103 已同步到 finer-`xi` 正式口径
- [x] 做一轮 PR closeout 级验证取舍说明
  - 需要明确“本轮以数据资产固化为主，是否额外重跑 regression / integration”
  - 若不补跑更重测试，需要在 PR 收尾说明中写清理由与残余风险
  - 本轮结论：**不补跑**更重 integration / regression closeout；接受“以正式数据资产固化为主”的收尾范围

---

## 1. 背景与目标

当前仓库在 `relaxtime / transport` 方向已经具备：

- [x] 单点平衡态求解与 phase-aware continuation
- [x] 单点 `tau` / `tau_inv`
- [x] `eta` / `sigma` / `zeta`
- [x] `kappa_*` 与部分输运组合量
- [x] `xi` 相关连续性与平滑性分析脚本
- [x] phase pipeline / crossover / CEP / spinodal 参考能力

但当前仍缺一条正式的“相图邻域 transport 结果产品化”主线：

- [ ] 缺少以相变线为参考的扫描模式
- [ ] 缺少固定温度、跨化学势的稀疏采样模式
- [ ] 缺少面向用户的 `data/` 层 canonical 结果目录
- [ ] 缺少与 `tests/regression/` 对应的正式治理口径
- [ ] 缺少 README / 项目简介层对新增能力的同步说明

本任务单的目标是：

1. 固化两类新的 transport 扫描模式；
2. 让这些模式与现有 `gap_transport_scan` / `xi_smoothness` / `phase pipeline` 共存；
3. 为用户提供稳定的 CSV + 图 + README 结果资产；
4. 为后续 regression / plot-review / data-layer canonical baseline 打通治理边界。

---

## 2. 范围与非范围

### 2.1 本期范围

- [x] 设计并实现 phase-guided transport 扫描模式 `a / b`
- [x] 固化 CSV 输出字段与图形组织规则
- [x] 将稳定结果沉淀到 `data/outputs/results/` 正式路径
- [x] 为 canonical case 建立 regression / plot-review / data-layer 三层分工
- [x] 明确与现有脚本入口的兼容策略

### 2.2 本期不覆盖

- [ ] 不新增新的 transport 物理公式
- [ ] 不新增 EPNJL 模型能力
- [ ] 不在本期引入 mesonic pressure / EOS 正式 workflow
- [ ] 不把等熵线 / 化学冻结线求解混入本期 transport 扫描主轴
- [ ] 不把介子质量扫描与 transport 扫描合并成同一任务

---

## 3. 现状盘点

### 3.1 已有能力

- [x] 主 transport CLI：
  - `scripts/relaxtime/run_gap_transport_scan.jl`
- [x] transport 扫描计划与编排：
  - `scripts/relaxtime/gap_transport_scan_plan.jl`
  - `scripts/relaxtime/gap_transport_scan_orchestration.jl`
- [x] 单点 transport workflow：
  - `Models.solve_gap_and_transport`
  - `Models.solve_transport_from_equilibrium`
- [x] `xi` 平滑性分析链：
  - `scripts/relaxtime/run_xi_smoothness_batch.jl`
  - `scripts/relaxtime/evaluate_xi_smoothness.jl`
- [x] phase 参考能力：
  - `Models.run_phase_pipeline`
  - `find_cep`
  - `scripts/pnjl/build_dense_phase_reference.jl`
- [x] 现有 transport CSV 已包含 flavor-resolved `tau`：
  - `tau.u`, `tau.d`, `tau.s`, `tau.ubar`, `tau.dbar`, `tau.sbar`

### 3.2 当前缺口

- [x] `gap_transport_scan_plan.jl` 当前只支持规则网格，不支持“相变线为 base”的温度倍率带模式
- [x] 当前没有“固定温度、离散 `mu_B`、连续 `xi`”的正式稀疏扫描模式
- [x] 当前没有专门面向用户的 canonical output 目录命名规则
- [ ] 当前没有把 `tau` flavor-resolved 输出与图层展示规则分开治理
- [x] 已将首个 canonical transport case 沉淀为 `data/` 层正式参考资产

---

## 4. 需求收口

### 4.1 新增扫描模式

本期新增两类 phase-guided transport 扫描模式：

补充命名口径：

- [x] `a / b` 保留为兼容短别名
- [x] 推荐对外使用更正式的模式名：
  - `fixed-muB-phase-scaled`
  - `fixed-T-sparse-muB`

#### 模式 a：固定 `mu_B`，沿相变线温度做倍率带

- [x] 对给定 `mu_B`，先取该点相变线参考温度 `T_phase(mu_B, xi)`
- [x] 对温度倍率带 `alpha_T = 1.0, 1.1, 1.2, ...` 做扫描
- [x] 对每个 `alpha_T`，连续扫描 `xi`

当前固定口径：

- crossover 区：
  - base 线使用 `crossover line`
- 一阶相变区：
  - base 线使用**一阶相变线本身**
  - 不使用两条亚稳态相边界线作为 base

#### 模式 b：固定温度，离散 `mu_B`，连续 `xi`

- [x] 固定若干代表性温度点
- [x] 温度点覆盖：
  - crossover 区
  - CEP 附近
  - 一阶相变区
- [x] 每个固定温度下，选择少量离散 `mu_B` 点
- [x] 对每个 `(T, mu_B)` 点连续扫描 `xi`

### 4.2 图形组织规则

- [x] 按 `mu_B/T` 分图
- [x] 按 `T/mu_B` 分线
- [x] 线的横轴统一为 `xi`
- [x] 纵轴优先看 `tau`，再看输运组合量

### 4.3 `tau` 的输出与画图口径

- [x] CSV 中允许一并输出所有 flavor-resolved `tau`
- [x] 画图时按需要区分展示
- [x] 默认主输出保留：
  - `tau_u`
  - `tau_d`
  - `tau_s`
- [x] 在 `mu_B = 0` 时允许 `tau_u = tau_d` 合并展示
- [x] 在有限 `mu_B` 下保留 flavor 分辨，不提前合并
- [x] `zeta` / `zeta_over_s` 当前默认参与 canonical case 计算（`compute_bulk=true`）

---

## 5. 与现有扫描模式的兼容策略

### 5.1 不替代现有规则网格扫描

- [x] 现有 `run_gap_transport_scan.jl` 继续保留为基础规则网格入口
- [x] 现有 `xi_smoothness` 相关批处理继续保留为平滑性诊断工具
- [x] 新增模式不直接改写旧模式语义

### 5.2 新模式作为“上层计划模式”接入

当前建议：

- [x] 复用现有 `gap_transport_scan_orchestration.jl`
- [x] 将新增的 `a / b` 模式实现为更高层的 sampling plan / path generator
- [x] 下层逐点执行仍走现有 `execute_gap_transport_scan_point!`
- [ ] 仍未把 phase-guided 上层 plan 进一步并回 `gap_transport_scan_plan.jl`；当前以新增脚本和独立 plan 模块实现，优先保持旧规则网格语义稳定

推荐理解：

1. phase pipeline 负责提供参考线 / 关键点；
2. transport sampling plan 负责展开 `(T, mu_B, xi)` 点列；
3. transport orchestration 负责逐点求解与 CSV 落盘；
4. post-processing 负责分图与 README 汇总。

### 5.3 与介子质量扫描 / 等熵线能力的边界

- [x] 等熵线 / 化学冻结线求解能力不在本期 transport 扫描主轴中引入
- [x] 该能力若后续用于介子质量或介子数密度扫描，应单独设计路径入口
- [x] 本期 transport 只消费 phase line / CEP / 一阶相变线这类参考几何对象

---

## 6. 输出合同

### 6.1 CSV 层

每个 canonical scan 的 CSV 当前至少应输出：

- [ ] 点位定义：
  - `T_MeV`
  - `muB_MeV`
  - `xi`
  - `mode`
  - `phase_reference_kind`
  - `alpha_T` 或离散 `mu_B` 分组标签
- [ ] 平衡态与 phase 诊断：
  - `equilibrium_converged`
  - `iterations`
  - `residual_norm`
  - `phase_curr`
  - `phase_structure`
- [ ] flavor-resolved `tau / tau_inv`：
  - `tau_u`
  - `tau_d`
  - `tau_s`
  - `tauinv_u`
  - `tauinv_d`
  - `tauinv_s`
- [ ] 输运量：
  - `eta`
  - `sigma`
  - `zeta`
  - `eta_over_s`
  - `zeta_over_s`
  - `sigma_over_T`
  - `sigma_over_T_over_eta_over_s`
  - `zeta_over_s_over_eta_over_s`
- [ ] 质量 / mean field / thermodynamics 背景：
  - `Phi`
  - `Phibar`
  - `m_u`
  - `m_d`
  - `m_s`
  - `pressure`
  - `energy`
  - `entropy`

说明：

- [x] CSV 层允许“一并输出”，供后续多种图层消费；
- [x] 图层不要求一次性展示所有字段。

### 6.2 图层

当前第一批推荐图：

- [ ] `tau_u / tau_d / tau_s` vs `xi`
- [ ] `eta` vs `xi`
- [ ] `sigma` vs `xi`
- [ ] `zeta` vs `xi`
- [ ] `eta_over_s` vs `xi`
- [ ] `sigma_over_T` vs `xi`

当前第一批组合图推荐：

- [ ] `tau` flavor-resolved 同图
- [ ] `eta / sigma / zeta` 分图
- [ ] `eta_over_s` 与 `sigma_over_T` 分图

### 6.3 data 层正式路径

建议正式输出根目录：

- `data/outputs/results/relaxtime/transport/phase_guided/`

建议子目录：

- `mode_a_fixed_muB_phase_scaled/`
- `mode_b_fixed_T_sparse_muB/`

每个 case 至少包含：

- [ ] 原始 CSV
- [ ] plot-ready CSV（如需要）
- [ ] figures/
- [ ] README.md
- [ ] provenance / manifest 文件

---

## 7. regression / plot-review / data-layer 分层

### 7.1 regression layer

- [ ] 锁定 canonical case 的数值不漂移
- [ ] 分层建议：
  - `core`：轻量点位 / 小 case
  - `full`：全路径 / 较重 case

### 7.2 plot-review layer

- [ ] 对不适合 strict validation 的结果保留趋势图
- [ ] 用户人工核对：
  - `xi` 连续性
  - 各 flavor `tau` 层级关系
  - 关键窗口是否出现异常尖峰 / 翻转

### 7.3 data-layer canonical assets

- [ ] 面向用户的正式参考结果保留在 `data/outputs/results/`
- [ ] 这类资产不等同于 `tests/regression/` baseline
- [ ] README 需显式写清：
  - 这是用户参考资产
  - 不是 validation 真值

---

## 8. 可执行任务分解

### Phase T1.0：现有能力与兼容边界盘点

- [x] 盘点现有 `run_gap_transport_scan` / `xi_smoothness` / `phase pipeline` 的职责边界
  - 验收：形成“保留 / 复用 / 新增”清单

### Phase T1.1：模式 a 采样计划

- [x] 设计固定 `mu_B` + `T/T_phase` 倍率带 + 连续 `xi` 的 sampling plan
  - 验收：至少能生成一组 canonical 点列
- [x] 明确 crossover 区与一阶区的 base 线选择逻辑
  - 验收：文档与代码口径一致

### Phase T1.2：模式 b 采样计划

- [x] 设计固定温度 + 离散 `mu_B` + 连续 `xi` 的 sampling plan
  - 验收：至少覆盖 crossover / CEP / 一阶区三类窗口

### Phase T1.3：CSV 输出合同与 data 路径

- [x] 固定输出字段表
  - 验收：字段表写入文档并在脚本中落地
- [x] 固定 `data/outputs/results/relaxtime/transport/phase_guided/` 目录结构
  - 验收：默认 canonical 路径与 `mode_a_fixed_muB_phase_scaled/`、`mode_b_fixed_T_sparse_muB/` 已在 CLI 中固化，且 mode a / mode b 首个正式 case 均已落盘

### Phase T1.4：图层与 README

- [x] 固定按 `mu_B/T` 分图、按 `T/mu_B` 分线的出图规则
  - 验收：`run_phase_guided_transport_plots.jl` 已按 `scan_group` 输出 `tau` flavor-resolved 与第一批 transport 组合图
- [x] 为 canonical case 生成 README
  - 验收：`first_canonical_v1/README.md` 已写明输入、模式、分组、plot manifest 与“用户资产而非 validation 真值”的解读边界

### Phase T1.5：回归治理

- [x] 为 canonical transport phase-guided case 设计 regression 覆盖
  - 验收：已新增 `test_phase_guided_transport_mode_b_regression.jl` 与 baseline，并纳入 `tests/regression/runtests.jl` 的 `CORE_SMOKE_FILES`
- [x] 对 data 层 canonical case 明确 plot-review / regression / user-facing baseline 三层边界
  - 验收：任务单、case README 与 PR 说明均已对齐“用户资产 != regression baseline != validation 真值”

### Phase T1.6：文档与项目简介同步

- [x] 在能力边界稳定后，更新 README / docs / GitHub 项目简介相关说明
  - 验收：根 README 与脚本入口 README 已可见 phase-guided transport 扫描能力、canonical 输出路径、正式模式名与 bulk 默认开启口径

---

## 9. 验收标准

- [ ] 已明确 `a / b` 两类扫描模式
- [ ] 已明确它们与现有扫描模式的兼容策略
- [ ] 已明确 CSV 全量输出、图层按需区分的规则
- [ ] 已明确 `tau` flavor-resolved 的主输出口径
- [ ] 已定义 `data/` 层正式输出路径
- [x] 已明确 regression / plot-review / data-layer 三层分工

---

## 10. 风险与回退

- [ ] 风险：新模式直接侵入旧入口，导致现有扫描语义漂移
  - 回退：新增上层 sampling plan，不覆盖旧网格模式
- [ ] 风险：CSV 字段过多，图层难以组织
  - 回退：CSV 全量保留，图层只选第一批 canonical 字段
- [ ] 风险：`tau` flavor-resolved 与整体输运量混在同图，解释混乱
  - 回退：`tau` 单独成组，输运系数组合单独成组
- [ ] 风险：full regression 计算过重
  - 回退：先 core 轻量锁定，再逐步升 full canonical case

---

## 11. DoD

满足以下条件即可视为本任务单设计完成：

- [ ] 已落到 `docs/dev/active/`
- [ ] 已明确 `a / b` 模式的输入、输出与边界
- [ ] 已明确与现有 transport 扫描能力的兼容策略
- [ ] 已明确 data-layer / regression / plot-review 的治理分工
- [ ] 已给出后续实施阶段与验收标准
