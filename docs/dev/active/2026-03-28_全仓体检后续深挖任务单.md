---
title: 全仓体检后续深挖任务单（A/B/C/D + P2-R5延伸）
status: active
created: 2026-03-28
owner: opencode-agent
---

# 全仓体检后续深挖任务单（A/B/C/D + P2-R5延伸）

## 1. 目标与来源

本任务单用于承接以下三份文档的后续开发价值项，并形成可执行排期：

- `docs/dev/archived/2026-03-28_全仓10维度体检结果报告.md`
- `docs/dev/archived/2026-03-27_src体检结果报告_10维度.md`
- `docs/dev/archived/2026-03-27_src-P1P2补遗任务单.md`

目标：在不扩散无关改动的前提下，继续提升安全性、观测性、治理一致性与可维护性。

## 2. 深挖价值结论（优先级）

### A（高）：统一错误契约最后一公里 + 信息泄露收口

现状：`compute`、`pnjl_handlers`、`pnjl_scan_jobs` 已统一 `error_code` 基线。

仍需推进：

- [ ] 引入 `message_id` 字段（可追踪、可检索）并形成最小契约说明。
- [ ] 收口 `pnjl_scan_jobs` 内部异常细节暴露（失败作业状态接口不再直接泄露 backtrace）。
- [ ] 对 3 条 HTTP 链路补一致性测试（字段齐套、状态码语义一致）。

验收：

- [ ] 错误响应契约为 `status/error_code/error/message_id`（诊断字段按需可选）。
- [ ] 对外 payload 不包含内部堆栈文本。

进展（2026-03-28，worktree: `feat/deep-dive-2026-03-28`）：

- [x] 引入 `message_id` 字段（可追踪、可检索）并形成最小契约说明。
- [x] 收口 `pnjl_scan_jobs` 内部异常细节暴露（失败作业状态接口不再直接泄露 backtrace）。
- [x] 对 3 条 HTTP 链路补一致性测试（字段齐套、状态码语义一致）。
- [x] 错误响应契约为 `status/error_code/error/message_id`（诊断字段按需可选）。
- [x] 对外 payload 不包含内部堆栈文本。

实现说明：

- 统一错误字段构造下沉到 `src/simulation/fullserver/shared.jl`：`_error_payload` + `_new_message_id`。
- `compute`/`pnjl-gap`/`pnjl-scan jobs` 三条链路的错误响应均补齐 `message_id`。
- `pnjl_scan_jobs` 内部错误改为 `internal_error` 仅内部保存；对外状态接口仅返回脱敏摘要 `error`。

验证记录：

- `julia --project=. -e 'ENV["UNIT_FILES"]="simulation/test_fullserver_compute_handlers.jl;simulation/test_fullserver_pnjl_handlers.jl;simulation/test_pnjl_scan_jobs.jl"; include("tests/unit/runtests.jl")'` ✅（69/69）

---

### B（高）：全仓旧路径残留扫尾自动化（doc gate）

现状：核心入口文档已修正，但全仓仍有大量历史文档/索引残留 `src/pnjl` 等旧路径。

任务：

- [ ] 盘点需治理范围（区分“历史归档允许保留”与“现行导航必须修正”）。
- [ ] 在 `scripts/dev/check_docs_consistency.jl` 增加更细粒度规则：
  - active/guides/api 主导航严格；
  - archived 历史内容可保留但禁止误导性“当前入口”表述。
- [ ] 修复首批高影响页面（API 目录入口、架构目录入口、主指南索引）。

验收：

- [ ] docs consistency 脚本可自动拦截新增误导路径。
- [ ] 主导航链路中不再出现旧主线路径误导。

进展（2026-03-28，worktree: `feat/deep-dive-2026-03-28`）：

- [x] 盘点需治理范围（区分“历史归档允许保留”与“现行导航必须修正”）。
- [x] 在 `scripts/dev/check_docs_consistency.jl` 增加更细粒度规则：
  - active/guides/api 主导航严格；
  - archived 历史内容可保留但禁止误导性“当前入口”表述。
- [x] 修复首批高影响页面（API 目录入口、架构目录入口、主指南索引）。
- [x] docs consistency 脚本可自动拦截新增误导路径。
- [x] 主导航链路中不再出现旧主线路径误导。

实现说明：

- `scripts/dev/check_docs_consistency.jl` 新增分层门禁：
  - `docs/guides`、`docs/api`、`docs/dev/active` 严格禁止旧主线路径误导；
  - `docs/dev/archived` 允许历史路径保留，但禁止“旧路径=当前/主线入口”误导表达。
- 修复首批高影响页面：
  - `docs/api/pnjl/DualBranchScan.md`
  - `docs/api/PARAMETER_TYPES_API.md`
  - `docs/api/data_contracts.md`
  - `docs/api/relaxtime/scattering/DifferentialCrossSection.md`
  - `docs/api/relaxtime/scattering/TotalCrossSection.md`
  - `README.md`

验证记录：

- `julia --project=. scripts/dev/check_docs_consistency.jl` ✅

---

### C（中）：scan jobs 状态治理增强（运维向）

现状：已有 `keep_max + TTL`。

任务：

- [ ] TTL 与保留上限改为可配置（env 或 config）。
- [ ] 新增 prune 指标（累计删除数、TTL 删除数、按数量删除数）。
- [ ] 在状态接口暴露最小治理指标（不泄露内部细节）。

验收：

- [ ] 长时运行可观测“队列存量变化与清理行为”。
- [ ] 新指标单测覆盖。

---

### D（中）：configuration 指南“实况层/概念层”分层

现状：路径已对齐，但部分章节仍偏通用模板，容易与“仓库现行实现”混淆。

任务：

- [ ] 拆分“仓库实况（当前支持）”与“概念示例（可选范式）”标记。
- [ ] 为示例段补“非现行实现”提示。
- [ ] 增加“最小可复现配置矩阵”章节（models/physics/workflows）。

验收：

- [ ] 新读者不再将概念样例误判为当前代码路径。

进展（2026-03-28，worktree: `feat/deep-dive-2026-03-28`）：

- [x] 拆分“仓库实况（当前支持）”与“概念示例（可选范式）”标记。
- [x] 为示例段补“非现行实现”提示。
- [x] 增加“最小可复现配置矩阵”章节（models/physics/workflows）。
- [x] 新读者不再将概念样例误判为当前代码路径（通过文档分层与显式提示收口）。

实现说明：

- 更新 `docs/guides/configuration.md`：
  - 新增“阅读指引：实况层 vs 概念层”；
  - 新增“最小可复现配置矩阵（models / physics / workflows）”；
  - 将 JSON/高级配置/迁移等章节明确标记为“概念示例（非现行实现）”；
  - 为示例型章节补充“非默认自动加载策略”提示。

验证记录：

- `julia --project=. scripts/dev/check_docs_consistency.jl`（待本轮文档改动后执行）

## 3. 关于 P2-R5（大文件可维护性拆分第一步）是否继续深挖

结论：**可以且值得继续深挖**，建议作为本任务单的 E 主线（高价值、可控风险）。

原因：

- 已完成“第一步试点”，具备继续推进的技术与回归基础。
- 当前 `TransportCoefficients` / workflow 边界仍存在可维护性改进空间。
- 与 A/C 两项（错误契约与状态治理）形成正交，可并行推进。

### E（高）：P2-R5 延伸（二阶段）

任务：

- [ ] 将 `TransportCoefficients` 继续按职责拆分到“参数校验/请求适配/积分核执行/结果组装”四层。
- [ ] 在不改公共 API 的前提下，减少跨层 `Main.*` 依赖触点。
- [ ] 为每一层新增最小定向测试（尤其契约层与边界条件）。

验收：

- [ ] 公共 API 与基线行为无回归。
- [ ] 文件职责边界可由文档清晰描述。

进展（2026-03-28，worktree: `feat/deep-dive-2026-03-28`）：

- [x] 将 `TransportCoefficients` 继续按职责拆分到“参数校验/请求适配/积分核执行/结果组装”四层。
- [x] 在不改公共 API 的前提下，减少跨层 `Main.*` 依赖触点。
- [x] 为每一层新增最小定向测试（尤其契约层与边界条件）。
- [x] 公共 API 与基线行为无回归。
- [x] 文件职责边界可由文档清晰描述（通过函数分层命名与测试覆盖体现）。

实现说明：

- `src/relaxtime/TransportCoefficientsValidation.jl`
  - 新增 `_validate_transport_request_contract`，统一“扩散背景参数齐套性”契约校验。
- `src/relaxtime/TransportCoefficients.jl`
  - 将 `transport_coefficients` 主流程拆分为：
    - 请求适配层：`_build_transport_request`
    - 积分核执行层：`_compute_eta` / `_compute_sigma` / `_compute_zeta` / `_compute_diffusion_matrix`
    - 结果组装层：`_assemble_transport_result`
  - 公共函数签名与返回结构保持不变。
- `tests/unit/relaxtime/test_transport_coefficients.jl`
  - 新增“request adapter layering”测试；
  - 新增“contract layer guards”测试（缺参场景与齐套场景）。

验证记录：

- `julia --project=. -e 'ENV["UNIT_FILES"]="relaxtime/test_transport_coefficients.jl;models/test_transport_workflow.jl"; include("tests/unit/runtests.jl")'` ✅（201/201）

## 4. 建议执行顺序

1. A（错误契约收口）
2. E（P2-R5 二阶段拆分）
3. B（文档门禁自动化）
4. C（状态治理增强）
5. D（configuration 分层整理）

## 5. 统一验证口径

- `julia --project=. -e 'ENV["UNIT_FILES"]="simulation/test_pnjl_scan_jobs.jl;simulation/test_fullserver_compute_handlers.jl"; include("tests/unit/runtests.jl")'`
- `julia --project=. -e 'ENV["UNIT_FILES"]="relaxtime/test_transport_coefficients.jl;models/test_transport_workflow.jl"; include("tests/unit/runtests.jl")'`
- `julia --project=. scripts/dev/check_docs_consistency.jl`
- `julia --project=. scripts/dev/analyze_deps.jl`

## 6. DoD

- [ ] A 与 E 至少完成一项并提供验证证据。
- [ ] docs consistency 门禁规则有明确增量（若执行 B）。
- [ ] active 文档状态与实际提交内容一致，可直接归档审计。
