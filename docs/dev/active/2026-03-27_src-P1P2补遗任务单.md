# src P1/P2 补遗任务单（体检报告完全覆盖）

## 1. 目的

本任务单用于补齐 `docs/dev/active/2026-03-27_src体检结果报告_10维度.md` 中尚未进入执行排期的 P1/P2 项，确保“体检结论 -> 任务单 -> 可验证落地”闭环完整。

关联文档：
- `docs/dev/active/2026-03-27_src体检结果报告_10维度.md`
- `docs/dev/active/2026-03-27_src优化待办开发文档.md`
- `docs/dev/active/2026-03-27_src-P2专项推进任务单.md`

## 2. 补遗范围

### P1-R1：扩散核语义一致性核对（TransportCoefficients）

**来源**
- 体检 3.1：扩散积分核与同文件 η/σ 写法不一致。

**任务**
- [x] 先写失败测试：固定输入下对扩散核 `f` 与 `f(1-f)` 语义差异建立可复现实验断言。
- [x] 明确“当前实现是设计预期还是偏差”并在文档留痕（结论：按 `docs/reference/formula/relaxtime/transport/KappaSeries_AndLambda_FromLegacyFortran.md` 保留 legacy 占据核 `F=f` 口径，不改为 `f(1-f)`）。
- [x] 若判定为偏差，实施最小修正并保持其余输运系数行为不回归（本项结论为“非偏差”，因此不改扩散核实现，仅固化口径测试）。

**验收**
- [x] 有可追溯测试证明结论。
- [x] 若有代码变更，selector + unit smoke 通过。

---

### P1-R2：`safe_inv(0)=Inf` 上游承接策略明确化

**来源**
- 体检 3.1：零散射率语义可能向上传播无穷值。

**任务**
- [ ] 盘点 `safe_inv` 下游使用点（至少覆盖 workflow 与关键输运输出）。
- [ ] 定义零散射率策略：保留 `Inf` 还是在上游做显式降级/标记。
- [ ] 用测试固定策略（避免未来语义漂移）。

**验收**
- [ ] 语义在文档中明确。
- [ ] 关键路径测试覆盖零散射率场景。

---

### P1-R3：workflow 复现元信息补充（profile/path）

**来源**
- 体检 3.6：`PHYSICS_PARAM_PROFILE` 驱动结果，需显式落盘。

**任务**
- [ ] 在 workflow 输出中增加只读复现元信息（至少 `physics_profile`、配置路径）。
- [ ] 补测试，验证该元信息存在且与环境变量一致。
- [ ] 更新对应 API 文档说明该字段用途与兼容性。

**验收**
- [ ] 元信息可用于复现实验参数来源。
- [ ] 不破坏现有调用接口。

---

### P1-R4：兼容别名退场节奏文档化

**来源**
- 体检 3.2：兼容别名长期保留导致语义分叉。

**任务**
- [ ] 列出当前兼容别名清单（含 `rate_lookup` 等）。
- [ ] 给出 deprecation 阶段策略（保留期、替代接口、移除门槛）。
- [ ] 在 docs/api 与 migration guide 补充退场时间线。

**验收**
- [ ] 用户可从文档直接获取“现在该用什么、旧接口何时退场”。

---

### P2-R5：大文件可维护性拆分（第一步）

**来源**
- 体检 3.5：`TransportCoefficients`/`AverageScatteringRate`/`TransportWorkflow` 体量大。

**任务**
- [ ] 仅做第一步拆分方案（不大改逻辑）：确定拆分边界与最小迁移顺序。
- [ ] 优先选 1 个模块做“低风险拆分试点”（建议 `TransportCoefficients` 的校验/适配层）。
- [ ] 保持公共 API 不变并补最小回归。

**验收**
- [ ] 文件职责更清晰。
- [ ] 行为与测试结果不回归。

## 3. 优先级顺序（建议）

1. P1-R1 扩散核语义核对
2. P1-R3 复现元信息补充
3. P1-R2 `safe_inv` 策略明确化
4. P1-R4 兼容别名退场文档化
5. P2-R5 大文件拆分试点

## 4. 统一验证口径

- `julia --project=. -e 'ENV["UNIT_FILES"]="relaxtime/test_transport_coefficients.jl"; include("tests/unit/runtests.jl")'`（涉及输运核时）
- `julia --project=. -e 'ENV["UNIT_FILES"]="models/test_transport_workflow.jl"; include("tests/unit/runtests.jl")'`（涉及 workflow 时）
- `julia --project=. -e 'ENV["UNIT_PROFILE"]="smoke"; include("tests/unit/runtests.jl")'`
- `julia --project=. scripts/dev/check_docs_consistency.jl`（文档变更时）

## 5. DoD

- [ ] 上述 5 个补遗项均进入可执行状态（有任务、有验收、有验证命令）。
- [ ] 至少完成前 2 项 P1 并给出验证证据。
- [ ] 与既有两份任务单无冲突，且状态一致。
