## Plan: 实现 rPNJL 功能集成

TL;DR：在不大幅重构现有 pnjl 的前提下，优先在 `calculate_mass_vec` 中加入八夸克项（`g1,g2`），并在 Polyakov 势实现中加入 Vandermonde 项与温度依赖的 `b2(T)`，复用已有积分、求解器和数值稳定处理；随后补充参数常量与测试用例以验证行为和稳定性。

### Steps
1. 修改常量：更新 Constants_PNJL.jl，加入 `g1`, `g2`, `kappa`, `T0`, `a0,a1,a2,b3,b4`（使用文档表3.1/3.2）。  
2. 更新质量方程：在 Core.jl 或现有 `calculate_mass_vec` 函数中实现式(3.31) 的 `g1`/`g2` 项，保持函数签名不变（兼容 `gap_conditions`）。  
3. 更新 Polyakov 势：在 Thermodynamics.jl 的 `calculate_U`/`calculate_U_derivative_T` 中实现式(3.27–3.29)（含 `b2(T)` 的式(3.28) 与 `κ ln[J]`），并增加 `safe_log` 保护。  
4. 数值稳健性：复用 src/pnjl/integration/Integrals.jl 的 LSE/数值技巧，增加 Vandermonde 下界 ε 与对极端 T 的 exp 限制。  
5. 测试与示例：新增 `scripts/rpnjl_validation.jl`，并在 unit 中加入 3–5 个测试（真空一致性、Vandermonde 行为、Tc 扫描、八夸克影响、收敛性）。  
6. 验证扫描：用已存在的扫描脚本（如 TmuScan.jl）运行 μ=0 扫描，确认 Tc 与文档一致并记录数值行为。

### Further Considerations
1. 单位与常量转换：`g1,g2` 文档为 MeV^-8，确认项目中常用能量/长度单元，是否需转换（MeV↔fm）。  
2. 数值夹紧策略：Vandermonde 多项式可能 ≤0，选项：夹紧到 ε / 抛出并记录错误 / 在求解器中跳过该点。你偏好哪种策略？  
3. 兼容性策略：要直接替换 `calculate_U` 与 `calculate_mass_vec`（最小侵入）还是增加 `:rpnjl` 形式开关以保留旧行为？请选择默认行为。

### 补充项、设计建议与用户回答（已合并）

#### 遗漏/需补充的要点
- **导数更新**：`derivatives/ThermoDerivatives.jl` 中若存在手写导数，需要同步包含 `g1,g2` 与 Vandermonde；若使用自动微分（AD），确认 AD 覆盖新增项并加入测试。  
- **配置与参数来源**：新增参数应在 `config/` 与 `src/Constants_PNJL.jl` 中统一登记，并明确加载优先级（config 覆盖源码默认值）。  
- **单元/集成测试**：增加针对 `derivatives` 模块的测试，验证解析导数与数值差分一致性；在相变附近增加多初值收敛性测试。  
- **扫描/流程兼容性**：验证 `scans/*`（例如 `src/pnjl/scans/TmuScan.jl`）中对 `calculate_U`/`calculate_mass_vec` 的调用不依赖硬编码系数或旧假设。  
- **参数单位与转换工具**：明确代码内部单位并在参数加载处做 MeV↔fm 的自动转换或记录告警。  
- **Vandermonde 数值策略记录**：统一实现 `safe_vandermonde`，在被夹紧时写入日志，便于后续回溯。  
- **性能基准**：新增基准脚本比较引入 rPNJL 前后的单点求解与扫描时间，防止性能回归。

#### 设计建议（最小侵入优先）
- 默认保留旧行为：通过 `model` 开关（例如 `:pnjl` / `:rpnjl`）在 `calculate_U` 与 `calculate_mass_vec` 内部分支实现新旧行为，减少对外部调用点影响。  
- 使用 `safe_vandermonde`/`safe_log` 封装 Vandermonde 的下界与日志处理，集中管理数值容错策略。  
- 优先复用现有自动微分路径（AD），避免手写导数；若存在手写导数，按需同步更新并增加回归测试。  
- 参数加载优先级：优先从 `config/pnjl/default.toml` 读取用户配置，代码内常量作为 fallback，并在文档中记录单位与默认值。

#### 用户的回答（已整合）
1. 单位制：代码内部常用单位为 `fm^-1`；`g1,g2` 在配置文件中可以使用 `MeV`（类似 `config/pnjl/default.toml` 中的 `Lambda_MeV = 602.3`），但传入核心计算前应转换到 `fm`。  
2. Vandermonde 可行性疑问：关于 `J(Φ,Φ̄)` 的正负，文档中未明确规定是否绝对为正；目前将其视为可能出现 ≤0 的情况并采用数值保护（见上文 `safe_vandermonde` 提案）。该点仍可作为设计决策项（见下“待决问题”）。  
3. `model` 开关：同意采用 `model` 开关（`:pnjl` / `:rpnjl`），但关于如何在仓库结构上表意（是否直接在 `src/pnjl` 内修改）存在两种方案：  
	- 最小侵入：在现有文件中增加分支实现（推荐，调用点不变）。  
	- 明确分离：新增子模块 `src/pnjl/rpnjl/*` 并在入口处按 `model` 路由（更清晰但改动更大）。请确认偏好。  
4. 导数策略：优先使用自动微分（AD），不用手写导数。是的，AD 就是“自动微分”。  
5. 关于 Tc 的获得：参考文档未给出明确 Tc 值。建议通过 μ=0 温度扫描（使用已有 `TmuScan`），以手征序参量的峰值、易感性峰或 Polyakov 圈的突变点来判定 Tc；需要你确认接受的判定准则（例如易感性峰 vs 序参量拐点）及允许误差范围。  
6. 任务拆分：同意我在 `docs/dev/active` 下创建子开发任务（必要时细化为 issue），我会按你的指示拆分并列出修改点、测试与估时。

#### 待决问题（请确认）
- Vandermonde 负值处理策略：选择（A）夹紧到 ε 并记录（推荐）、（B）抛异常让上层处理、还是（C）跳过该点？  
- `model` 实现位置：选择（A）在現有 `src/pnjl` 中以分支實現（最小侵入、推荐），或（B）创建 `src/pnjl/rpnjl` 子模块并通过入口路由（更清晰但改动大）。  
- Tc 判定准则与允许误差范围（例如 ±5 MeV / ±10 MeV）。  

---
更新完毕。确认上述三项待决问题后，我可将计划拆成可分配 issue（含修改文件/函数、测试步骤、估时），并在 `docs/dev/active` 建立子任务清单。

### 用户决策（已采纳）

以下为用户对子任务中待决问题的明确选择，已合并入实施计划：

- **Vandermonde 负值处理策略**：不做数值修正，按公式正常计算；若出现数值问题，仅在日志/控制台抛出**警告**（不抛异常、不自动夹紧、不跳过点）。实现时仍建议在 `safe_vandermonde` 中加入可配置的最小容忍值并输出警告信息，便于后续调试。  
- **模型架构选择**：采用文档 `docs/dev/active/2026_1_31多态+抽象接口+独立文件夹结构.md` 中的多态 + 抽象接口 + 独立文件夹方案（即按模型实现独立模块并通过工厂/入口路由选择）。实现时会：  
	- 在短期内保持对现有 `src/pnjl` 的最小侵入支持（提供兼容层与迁移路径），  
	- 并逐步按该多态方案重构为 `src/models/...`（或在 `src/pnjl` 下建立 `models` 子结构，按团队偏好调整）。  
- **Tc 判定**：不在本次实现中强行判定或固定 Tc（因参考文献未给出明确值）。如果需要后续比较，可通过 μ=0 的温度扫描并由用户指定判定准则（例如易感性峰或序参量拐点）及允许误差范围。

请确认这些决策；确认后我将把对应的子任务（issue）生成到 `docs/dev/active` 下并开始逐项实现计划（先从参数与常量、质量项、Polyakov 势入手）。