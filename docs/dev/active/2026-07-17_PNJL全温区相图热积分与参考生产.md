# PNJL 全温区相图热积分与参考生产任务单

创建日期：2026-07-17

状态：执行中；PR 1 已通过作者审核并以 [PR #137](https://github.com/w5851/Julia_RelaxTime/pull/137) 合并为 `main@c0cde620`；独立依赖/AGENTS 治理 [PR #138](https://github.com/w5851/Julia_RelaxTime/pull/138) 已合并为 `main@8dd760b5`；PR 2 已通过作者审核并以 [PR #139](https://github.com/w5851/Julia_RelaxTime/pull/139) 合并为 `main@be7545d9`；C0/C1 candidate workflow 已完整成功；C2 的 161 个数值 shard 与 replay merge/validator 已成功，但 level-3 的 80/80 区间均未通过 convergence gate。当前停在 convergence diagnostic，并以隔离 feasibility PR 验证弱 S 形判据与性能合同；尚未启动 C3/O1/formal production 或晋升 canonical reference

关联背景：Issue #130 后续 transport production 依赖新的高精度 phase reference；现有 `24/8` 与 `32/10`、`T=60--240 MeV` 结果只作为算法改造前 pilot，不晋升 canonical。

## 1. 背景与目标

现有 PNJL dense phase reference 使用固定 Gauss--Legendre 张量积网格：动量节点覆盖 `p in [0, 10] fm^-1`，角节点覆盖 `cos(theta) in [0, 1]`。`T=60 MeV` 已出现比更高温区明显的密度和 spinodal 收敛漂移；继续把温区向 `T=0` 延伸时，固定全区间节点不能自动解析费米面附近宽度约为 `O(T)` 的窄结构。

本任务的最终目标是建立不依赖旧稀疏相边界插值、覆盖明确全温区、带积分/相线/产物三级收敛证据的 PNJL phase reference，并让后续 transport production 显式绑定其 tag、manifest SHA 与热力学积分策略。

当前 PR 1 的目标是：

- 审计 phase thermodynamics 的全部角度依赖；
- 为 PNJL 热项引入显式、可审计的积分策略；
- 利用 RS 标量热核的角约化降低维数；
- 建立低温径向自适应与 `T=0` 数值语义；
- 保持现有统一 Models 入口、求解分支治理和默认固定网格兼容路径。

## 2. 范围与非目标

### 2.1 当前 PR 允许修改

- `src/models/pnjl_physics/` 中 PNJL 标量热项和数密度积分；
- `src/models/solver/`、`src/models/scans/`、`src/models/phase/` 中显式积分策略传递；
- dense phase reference CLI/workflow 的策略参数与 manifest；
- 对应 unit、integration、validation、benchmark/perf 与 API/guide 文档；
- 本活动任务单和 Issue #130 的依赖状态说明。

### 2.2 当前 PR 不修改

- Maxwell construction、spinodal/CEP 判定、rho/T/xi 网格细化算法；这些属于 PR 2；
- canonical `boundary.csv`、`cep.csv`、`spinodals.csv`、`crossover_dense.csv` 及其 manifest；
- transport、relaxtime、传播子、截面、散射率和输运系数积分核；
- mixed-meson governance 和 non-fixedmu unified joint-solve 语义；
- PNJL magnetic、rotation 或其他含独立角核的模型。

### 2.3 必须保持的契约

- 稳定入口继续通过 `Models` 与 `src/models/entrypoints.jl`；
- 原 `p_num/t_num` 固定张量网格路径保留为显式兼容/交叉验证策略；
- 新积分策略必须写入有效配置、CSV/manifest provenance 和验证报告；
- `xi > -1` 的 RS 定义域必须显式校验；
- 物理量继续使用自然单位，内部温度/动量为 `fm^-1`。

## 3. 当前能力与角度依赖审计

### 3.1 审计结论

对当前 `model_kind=:PNJL` phase 主路径，以下结论成立：

| 路径 | 角度/xi 进入位置 | 是否满足“角度只通过分布能量进入” | 处理 |
| --- | --- | --- | --- |
| `omega_components` 的 chi/poly/vac | 无 | 是 | 不变 |
| `PNJLIntegrals.calculate_log_sum` | 仅 `E_xi=sqrt(m^2+p^2(1+xi*cos(theta)^2))` | 是 | 可做 RS 角约化 |
| `model_rho`、`model_thermo` | 对上述标量压强做 AD | 是 | 继承同一积分策略 |
| `PNJLModel.number_densities` | 仅分布函数的 `E_xi` | 是 | 可做同一角约化 |
| phase scan / Maxwell / spinodal / CEP | 只消费标量状态和热力学量 | 是 | 不引入角核 |

因此对积分上限为无穷、且被积函数除 `E_xi` 外不含角度的标量热核，有

```math
\int_{-1}^{1} dc \int_0^\infty p^2 dp\,
F\!\left(\sqrt{m^2+p^2(1+\xi c^2)}\right)
=
\frac{2}{\sqrt{1+\xi}}
\int_0^\infty q^2 dq\,F\!\left(\sqrt{m^2+q^2}\right).
```

### 3.2 限定与缺口

- 现有球形有限截断 `p <= 10 fm^-1` 在变量替换后产生角度相关的 `q_max`，不能与固定 `q_max=10` 严格等同；新约化路径必须使用无穷区间或显式尾部误差控制。
- `T=0` 时现有热项、分布和 Polyakov 势含 `1/T`；不得把接近零温结果冒充严格零温结果。
- 通用逐调用自适应积分可能造成 AD 求解中的开销和数值路径跳变；必须用固定点 oracle、求解级收敛和 benchmark 判定是否可作为 production 内层算法。
- PNJL magnetic 与 transport kernel 含独立角结构，不适用本次标量约化。

## 4. 可勾选任务分解

### PR 1：phase thermodynamics 积分算法与全温区精度基础

- [x] 审计 `omega`、gap residual、`model_rho`、`model_thermo`、直接数密度和 phase pipeline 的角度依赖。
- [x] 定义显式热积分策略及参数校验，保留固定张量积策略。
- [x] 实现 RS 角约化的一维径向热势和数密度积分。
- [x] 实现低温径向自适应策略与独立误差估计，避免仅依靠提高固定 `p_num`。
- [x] 实现并验证 `T=0` 固定态热项/数密度/Polyakov 势极限；五变量 PNJL gap/phase solve 显式拒绝 `T<=0`。
- [x] 把积分策略通过 Models solver/scan/phase 稳定路径显式传递并写入 manifest。
- [x] 添加 fixed-state、ForwardDiff、TaylorDiff、gap solve 和小型 phase smoke 测试，并保护 MixedTaylorJet 既有路径。
- [x] 添加代表性低温性能探针；只在 profiling/benchmark 支持时优化热路径。
- [x] 更新稳定 API/guide 与本任务状态。
- [x] 创建独立代码 [PR #137](https://github.com/w5851/Julia_RelaxTime/pull/137)。
- [x] 作者审核并合并 PR #137（squash merge `c0cde620`）。

### PR 2：phase 网格和几何量误差控制

- [x] 把 rho refinement 从“只处理 unknown”升级为粗细网格 Maxwell/spinodal 输出误差控制。
- [x] 引入基于相线插值误差的自适应 T/xi 采样，保留 branch continuation 顺序。
- [x] 移除隐藏的 crossover `T_max=220 MeV` 截断，改为显式配置。
- [x] 收紧并记录 CEP bracket/不确定度，区分位置量、密度量和诊断导数 gate。
- [x] 将 Actions 改成可恢复的 xi shard + deterministic merge。

PR 2 当前实现范围包括 rho 粗细网格 Maxwell/spinodal 几何量 gate、T/xi 中点插值误差、显式 CEP bracket、显式 crossover 温区、同口径 crossover 热节点、`phase_grid_convergence.csv` 以及 xi 区间 shard/确定性合并。上述任务仅在聚焦回归、CLI、性能与治理验证全部通过并创建独立 PR 后勾选；本阶段不得写入 `data/reference/**` 或启动 transport production。

### PR 3：全温区 phase-reference 产物

- [ ] 明确正式温区，若包含 `T=0` 必须使用已验证的严格零温路径。
- [ ] 运行积分容差、phase 网格和独立 oracle 三层收敛。
- [ ] 生成新 tag/canonical sibling、图像、manifest 和 convergence evidence，不覆盖旧 reference。
- [ ] 经作者审核后决定 limited/full/modified promotion。

PR 3 scope lock：case slug 为 `fullrange_phase_reference_adaptive_prod_v1`，result/figure 根分别为 `data/outputs/results/pnjl/phase_diagram/<case_slug>/` 与 `data/outputs/figures/pnjl/phase_diagram/<case_slug>/`。五变量 phase solve 的正式温区定义为严格正温 `T=1--240 MeV`，不声称覆盖严格 `T=0`；初始温度锚点为 `1:5:236` 并显式追加 `240`，随后按相线误差自适应补点。xi 初始锚点为 `-0.5:0.05:0.5`，rho 范围为 `0:0.05:4`。正式计算和收敛 case 只通过 `.github/workflows/pnjl-dense-reference.yml` 从锁定 ref 触发，本机不运行完整 phase production。

首次从 endpoint-fix ref dispatch 时，GitHub 在创建 run 前以 `workflow_dispatch` properties `39 > 25` 拒绝请求，未产生计算或 artifact。PR 3 将高级容差/refinement 参数收束为单个白名单 `advanced_config_json`，保留 24 个顶层输入并增加属性上限回归测试；只有修复后的 ref 才能作为正式 source SHA。

2026-07-19 首轮完整收敛暴露了 workflow 粒度问题，而不是 20 个 matrix job 共用 runner：每个 matrix job 都运行在
独立临时 GitHub-hosted VM，但旧 interval shard 会在一台 VM 内串行计算端点和 adaptive midpoint。C0 有 8/20 shard
完成、12/20 在六小时处取消；C1 有 1/20 完成、19/20 取消。C0 `s000` 的单 xi wall time 约 `77--80 min`，
level 2 最坏五个 xi 已超过上限，level 3 最坏九个 xi 必然不可行。C2 run `29678111634` 经作者确认取消；C0/C1
成功片段下载到仓库外审计目录，仅为 `diagnostic-only`，不得满足完整 convergence gate 或进入 formal merge。

corrective pipeline 的锁定实现目标为：

- [x] 初始锚点与第一层中点均采用一 xi 一 shard；
- [x] 每层合并后复用代码中的 xi midpoint error，只为未收敛区间生成下一层 midpoint matrix；
- [x] Action 最多三层 refinement，禁止把多 xi 串行重新引入单 shard；
- [x] 增加显式 step timeout、周期 heartbeat 和失败 diagnostic artifact；
- [x] final merge 纳入各层 xi convergence records，partial artifact 仍不得晋升；
- [x] corrective PR 合并并由默认分支注册 workflow 后，从锁定 calculation SHA 串行运行 C0/C1/C2。

corrective pipeline 的本地聚焦验证：Python action planner/config/merge `15/15`，Julia dense-reference contract 与
staged midpoint planner `28/28`；unit skip policy、docs consistency、active docs、script entrypoints、data output path guard
与 PNJL migration guard 全部通过。数值 phase production 未在本机运行；workflow 可执行性已由下述独立 branch smoke
验证，仍需 PR checks 复核，且该 smoke 不构成 convergence 或 production 证据。

corrective pipeline 的独立 branch smoke [run 29692826423](https://github.com/w5851/Julia_RelaxTime/actions/runs/29692826423)
已在 `d202c129808b9f14c5646c8596c811901868c549` 通过：两个初始锚点与 level-1 midpoint 均为独立 one-xi
shard，随后的 xi midpoint 收敛评估、convergence record、确定性合并与最终 artifact 校验全部成功。
该 smoke 只验证 workflow 调度、带符号 xi 参数传递与 merge 契约，不构成 convergence 或 production 证据；正式
C0/C1/C2 仍须等 corrective PR 合并后从新的锁定 source SHA 依次重启。

PR #140 合并后的 C0 [run 29810188129](https://github.com/w5851/Julia_RelaxTime/actions/runs/29810188129)
在 `main@5df0866c` 完成全部 41 个 one-xi 数值 shard；`assess_xi_level1` 两次均确定性失败。仓库外逐文件审计
205 个 CSV 后定位到唯一 malformed row：`xi=0.3` 的 `phase_grid_convergence` reason 为
`midpoint_classification_changed_or_unresolved:valid,unknown,valid`，Julia writer 使用裸 `join(..., ',')`，使
13 列记录被解析为 15 列。该失败是 CSV serialization 合同缺陷，不是本档数值 solver 失败，也不构成 C0
convergence success。

本轮 corrective PR 的附加防返工合同为：

- [x] 三条 grid-convergence Julia writer 统一复用 RFC 4180 field escaping；
- [x] shard validator 与 merge 均在读取边界报告文件、物理行号及期望/实际列数；
- [x] 聚合 manifest 分别记录 `calculation_git_commit`、`postprocess_git_commit` 与原 workflow run ID；
- [x] 新增只消费已完成 shard artifacts 的 postprocess replay workflow，且 replay 不自动晋升 reference；
- [x] 用现有 41 个 C0 artifacts 做仓库外 diagnostic replay，确认修复后的 assessment/merge 路径可通过；
- [ ] corrective PR 合并后从新 main SHA 重跑 C0；本次 writer 位于 shard 计算提交内，旧 malformed shard 不作为正式复用输入。

后续若 C1/C2 或 final merge 只暴露 CSV、validator、merge 或 Action 后处理问题，不再机械重跑此前已经完整通过的
C0。只要 solver、数值内核、grid/refinement 语义和 effective config 均未变化，就冻结原 shard artifact，使用
`.github/workflows/pnjl-dense-reference-replay.yml` 重放相应档位，并以双 SHA provenance 审计。若上述任何计算语义
发生变化，则 replay 不适用，必须从受影响的最早收敛档位重新计算。

corrective pipeline 合并后的 C0 [run 29896085769](https://github.com/w5851/Julia_RelaxTime/actions/runs/29896085769)
已在 `main@fc5d50a5` 完成 staged assessment、必要 refinement、final merge 与 validator，状态为 candidate artifact
success，不等于 convergence gate 通过。随后串行启动的 C1
[run 29937506668](https://github.com/w5851/Julia_RelaxTime/actions/runs/29937506668) 同样绑定
`fc5d50a5`：attempt 1 仅 `xi=-0.075` 的 level-1 shard 达到 300 min timeout，failed-only rerun 后该 shard 成功，
至此 41 个 initial shard 已全部完成。attempt 2 的 `assess_xi_level1` 在下载这些跨 attempt artifacts 时收到
`Failed to GetSignedArtifactURL: 404 workflow run not found`；仓库外通过 GitHub REST/`gh run download` 对全部 41 个
artifact 的逐项审计成功，确认失败属于无凭证内部 artifact token 的 attempt 作用域，不是数值或 artifact 缺失。

本轮 source-run resume corrective [PR #142](https://github.com/w5851/Julia_RelaxTime/pull/142) 的合同为：

- [x] 主 workflow、assessment 与 final merge 的 artifact 下载显式使用 `actions: read`、GitHub token、repository 和 run ID；
- [x] 新增 source-run resume planner，要求源 artifacts 精确覆盖 initial anchors 与 level-1 midpoints，并验证统一 calculation SHA、tag 和 effective config；
- [x] 从源 manifest 重建后续 shard 的完整 CLI 参数，不接受调用者重新手填一套可能漂移的数值配置；
- [x] 后续 level-2/level-3 shard 显式 checkout 原 calculation SHA，workflow 调度和后处理使用修复提交；
- [x] assessment 与 final merge 可联合消费 source run 和 resume run artifacts，并记录 calculation SHA、postprocess SHA 与 source run ID；
- [x] resume 产物明确标记为 diagnostic candidate，不自动晋升 reference；
- [x] PR #142 经作者审核合并后，从默认分支注册并触发 resume workflow，继续 C1 staged refinement；在 C1 完整 success 前不启动 C2。

仓库外 C1 source audit 目录为
`D:\Desktop\Julia_RelaxTime_issue130_artifacts\c1_attempt2_download_probe_20260723`；41 个唯一 xi 完整覆盖
`-0.5:0.025:0.5`，resume planner 重建出的 source config SHA256 为
`20fc053f21025fed0d90cd9885e9ef92cdf1ce81142c091e0604335c31c45988`。该审计只证明 source-run resume 前置条件，
不构成 C1 convergence 或 production-grade 证据。

PR #142 已 squash 合并为 `main@78335ea0`。C1 source-run resume
[run 29992688367](https://github.com/w5851/Julia_RelaxTime/actions/runs/29992688367) 随后完整成功：复用 source run
`29937506668` 的 41 个 initial shard，不重算已完成初始网格；26/26 level-2 shard、level-2 assessment、final merge 和
validator 全部通过，候选网格共 67 个唯一 xi。聚合 provenance 为 calculation `fc5d50a5...`、postprocess
`78335ea0...`、source workflow run `29937506668`；仓库外下载目录为
`D:\Desktop\Julia_RelaxTime_issue130_artifacts\c1_resume_29992688367`。该 success 仍只表示 C1 candidate artifact，
不等于 convergence gate 或 canonical promotion。

C2 必须继续与 C0/C1 共用 calculation `fc5d50a5d22fe79f7cc52ef2ecbdb79376e256e1`。由于主 workflow 原先只能把
workflow head 同时当作 calculation/postprocess SHA，C2 前增加可选 `calculation_ref`：只接受不可变小写 40 字符 SHA，
所有数值 shard checkout 该提交，assessment/final merge 验证该 calculation SHA，当前 workflow head 仅作为
postprocess SHA。该变更只涉及 CI 调度与 provenance，不改变 solver、数值内核、grid/refinement 语义或 effective config。

corrective PR 的本地验证包括：CSV/merge/validator/Action contract Python `20/20`；三个直接 Julia writer/plan
测试分别 `26/26`、`23/23`、`8/8`；Python syntax、docs consistency、active docs、SOP governance、script
entrypoints、data output path guard 与 unit skip policy 全部通过。仓库外 diagnostic replay 首先确认未修补 artifact
在 `anchor-a016` 第 2 物理行以 `expected 13 fields, parsed 15` 早失败；仅在独立副本中引用该唯一 reason 后，
41 个 shard 的 merge/validator 通过，level-1 的 20 个区间完成评估：7 个收敛、13 个未收敛，生成 26 个
level-2 midpoint 候选；把 level-1 convergence records 合并后共 5623 条 grid records，双 provenance 为
calculation `5df0866c...`、postprocess `diagnostic-working-tree`、source run `29810188129`。这些结果只验证合同与
后处理可恢复性，不晋升旧 C0，也不替代修复合并后的正式 C0 重跑。

C2 [run 30073342572](https://github.com/w5851/Julia_RelaxTime/actions/runs/30073342572) 绑定 workflow/postprocess
`8916a240c87889ff4ab95a62cf698e86d106c148` 与 calculation
`fc5d50a5d22fe79f7cc52ef2ecbdb79376e256e1`，41 个 initial、40 个 level-2 和 80 个 level-3 数值 shard 共
161/161 全部成功；唯一失败为 `assess_xi_level3`。attempt 1 与 failed-only attempt 2 都只下载到 99 个 shard
artifact，随后把 21 个 initial anchors 误报为缺失。仓库外 `gh run download` 获取完整 161 个 shard 并完成
deterministic merge，证明这是 artifact 枚举超过 100 后的单页下载缺陷，不是数值或物理网格失败。

本轮分页下载 corrective 合同为：

- [x] assessment、主 merge、source resume 和 postprocess replay 的跨 job/run pattern 下载统一使用带
  `GH_TOKEN`/`GH_REPO` 的 `gh run download`，完整分页枚举 artifact；
- [x] shard 与 staged xi convergence plan 两类 artifact 使用同一下载合同；
- [x] replay 可选补做 final assessment，并显式接收 level、intervals JSON 与 position/density/Maxwell/response 容差；
- [x] replay final merge 联合消费 source run 的 161 个 shard、source level-1/level-2 records 和 replay 新生成的
  level-3 record；输出保持 diagnostic candidate，不自动晋升 reference；
- [ ] corrective PR 合并后，从 source run `30073342572` 补做 level-3 assessment、merge 和 validator；禁止重算
  已完成的 161 个数值 shard。

### PR 3 corrective feasibility：S 形临界判据与性能合同（2026-07-25）

- [x] 在 `scripts/analysis/pnjl_criticality_feasibility.jl` 建立隔离解析 cusp 原型；只复用
  `Models.detect_s_shape` 作为当前 baseline，不修改 production phase classifier。
- [x] 四态合同固定为 `resolved_s_shape / near_critical / resolved_monotone / unresolved`；拟合最小斜率只作为
  预警证据。原 full-window 对照的两个 resolved 状态要求独立、更密且覆盖完整区间的验证；新 rho-support
  级联则要求目标窗口内实际 secant 与一次 local cubic 拓扑一致，不能由全局负斜率、单个噪声 secant 或拟合符号单独触发。
- [x] 7 个预声明用例全部符合预期：当前 secant baseline 漏掉欠采样窄 S，并在带噪单调曲线上误报 S；多证据路径分别
  保持 `near_critical` 与 `unresolved`，只在全区间/中心窗口 3/5 阶拟合、留出检查和独立定向验证共同成立后升级窄 S
  为 `resolved_s_shape`。
- [x] 新增单进程性能表和启动探针，显式区分 analysis module include、首次 `Models` 加载与 warm analytic
  classification；PALC 历史 `125.4 s cold / 1.37 s warm` 只保留为非同口径证据。
- [x] 增加 rho-support 级联 feasibility：明显 S/明确非候选走零补点 fast path；欠采样候选合并已有 coarse 点与
  最多 12 个新 fixed-rho 点，仅做一次 cubic，并可复用上一温度的 spinodal 密度中心/宽度。默认反例中隐藏 S
  实际新增 8 点，7/7 状态匹配，带噪单调和全局下降均未误升级。
- [x] 增加递增温度序列与 `Delta rho^2` CEP 外推；7 个 resolved 低温点给出合成
  `T_CEP=1.0268768`（真值 `1.0`，误差 `0.0268768`，通过预声明 `0.05` gate）。该结果只授予
  `synthetic_candidate`，不构成 PNJL CEP 结论。
- [x] 500 次解析计数中，rho-support cascade 约 `59.48 us/case`，旧 full-window 多证据约
  `742.74 us/case`，前者约快 `12.5x`；但仍约为 secant baseline 的 `14.5x`，故不得宣称 production
  性能通过。旧路径标记 `performance_rejected`，级联仍需同物理 PNJL pilot。
- [ ] 在 GitHub Actions 上完成同物理 CEP 窄窗口 pilot，记录 equilibrium/residual、Jacobian、Newton、anchor、branch
  point、关键路径和总 runner-minutes；通过前保持 `diagnostic_candidate`，不得修改 C0/C1/C2 verdict 或启动
  C3/O1/formal production。

可追溯分析包位于 `docs/analysis/pnjl/criticality_feasibility_v1/`。当前证据只支持算法合同在解析反例上可行，
不支持 PNJL CEP 准确度提升、PALC 加速或 production replacement 结论。

### PR 4：transport production

- [ ] 显式绑定新 phase-reference tag、manifest SHA、热积分策略和容差。
- [ ] 使用相同 thermodynamics 口径重跑主 equilibrium、bulk derivatives 与 transport。
- [ ] 生成新 slug、CSV、图像、registry 和新旧差异审计，不覆盖旧 production。
- [ ] 经作者审核后决定 registry 是否晋升 `approved`。

## 5. 测试与验收标准

### 5.1 Unit

- 张量积与 RS 角约化在高节点、尾部可忽略的固定状态上一致；
- `xi=0` 退化正确，`xi<=-1` 明确报错；
- `T>0` 低温点无 NaN/Inf，严格 `T=0` 行为符合约定；
- 热势与直接数密度对 `mu/T/state` 的 ForwardDiff 有限且与独立有限差分一致。

### 5.2 Integration

- fixed-mu 与 fixed-rho Models 求解可显式选择新旧积分策略；
- phase smoke 的有效配置和 manifest 记录实际策略；
- non-fixedmu joint-solve、分支选择和 mixed-meson 非目标契约不变。

### 5.3 Validation

- 覆盖 `T=0/1/5/10/20/40/60/120/200/240 MeV` 中适用的代表点；
- 覆盖 `mu<m`、`mu≈m`、`mu>m` 与 `xi=-0.5,0,0.5`；
- 比较热势、gap residual、密度、质量、稳定分支和尾部误差；
- production 候选至少由更严格容差或独立高节点张量 oracle 复核；oracle 不得与 production 共用自适应分割算法。

### 5.4 性能

- 在低温 fixed-state 和小型 gap solve 上记录评估次数、wall time 和分配；
- 不设未经测量的固定加速比；若新策略明显慢于可接受范围，保留为 oracle，并在 production 路径采用经验证的分段固定规则。

### 5.5 PR 1 验证证据（2026-07-18）

- 独立二维高节点张量 oracle：`tests/validation/pnjl/test_phase_thermal_quadrature_validation.jl` 显式 selected 运行通过 `66/66`，覆盖 `T=1--240 MeV`、`xi=-0.5/0/0.5` 和 `mu<m`、`mu≈m`、`mu>m`，并增加节点数与尾部截断的 oracle 自收敛检查。测试体耗时约 `2.6 s`（完整冷启动进程 `121.6 s`）；该文件保留在 `full/selected` validation 层，不加入日常 `core` gate。
- unit：移除 QuadGK 后 `PNJLIntegrals 31/31`、`PNJLModel 29/29`、`implicit_gap 33/33` 和 unit core `1436/1436` 通过，覆盖标量/向量值无穷区间积分、局部误差、`maxevals` 失败语义、ForwardDiff、MixedTaylorJet 与统一 solver 非回归。
- integration：adaptive research phase smoke `17/17`，兼容 tensor production smoke `12/12`，integration smoke `18/18`；fixed-μ 与 fixed-ρ 代表点均成功收敛。本地 integration core 在 20 分钟工具上限处未返回汇总，PR CI `integration-core` 后续于 `10m6s` 通过。
- regression / validation：regression core `343/343` 通过，另有 1 个缺少 tau-xi 本地 fixture 导致的预期 `Broken`；validation core `510/510` 通过，未刷新 baseline 或放宽容差。
- 稳定 CLI：direct/bracket/default-template/manifest/preset 全文件 exit `0`；manifest quadrature 字段检查 `18/18`，CLI 非有限容差拒绝和 dense-reference parser/manifest 投影另行通过。
- 治理：docs consistency、active docs、script entrypoints、Models entry contract、solver leakage、PNJL migration、data output path guard 与 precompile profile coverage 全部通过；`Models` 冷加载检查确认 QuadGK 未进入 loaded modules，`data/**` 无工作树改动。precompile trace 两份采样在工具超时前均已完整生成；按治理脚本同一集合差规则汇总为 `with-sys=387/1025`、`delta=579/650`、`focus=86/120`，全部在预算内。最终 PR head `5a940e56` 的 12 项 GitHub Actions checks 全部通过，并 squash 合并为 `c0cde620`。
- fixed-state 性能探针：仓库内 16/32 阶自适应规则在当前本机样本中，`T=1 MeV` 约 `0.051--0.054 ms`、`T=5 MeV` 约 `0.091--0.094 ms`、`T=60 MeV` 约 `0.127--0.137 ms`；每次调用分配约 `2.1--7.3 KiB`。同一探针中 tensor `24/8` 约 `0.05--0.09 ms`、`32/10` 约 `0.08--0.15 ms`。这些结果只证明 fixed-state kernel 级证据，不外推为完整 phase solve 固定加速比。

### 5.6 PR 2 当前验证证据（2026-07-18）

- phase regression 首轮为 `51/57`，6 个失败全部来自两条已收敛 crossover 行；CEP、boundary、其余 crossover 状态/化学势共 51 项不漂移。原因是旧 `build_crossover_line` 未向下传递测试声明的 `p_num=8,t_num=4`，实际使用默认 `24/12`；修复后 crossover 与主 phase 统一使用声明节点。
- 通过 `scripts/dev/export_phase_pipeline_regression_baseline.jl --output <temp>` 独立重生候选并复跑确认确定性。保留旧 `baseline_phase_pipeline_v1.csv` 逐字节不变，新建 `baseline_phase_pipeline_v2.csv`：第 7 行 `T/rho/derivative` 相对变化约 `-0.113%/-2.20%/-2.86%`；第 8 行约 `+0.055%/-21.23%/+1044.38%`。后者仅作为低节点内部算法回归值，不作为物理收敛结论；未放宽 `rtol=1e-6`，切换 v2 后 phase regression `57/57` 通过。
- `scripts/perf/pnjl_phase_grid_convergence_probe.jl` 在 `T=110,120 MeV`、`p_num=8,t_num=4,iterations=20` 的代表性一阶相变 slice 上记录：legacy unknown-only 为 `30` 次 scan point / `1.43 s`；rho 几何量 gate 为 `88` 次 / `4.77 s`；再启用温度中点为 `132` 次、3 个温度切片 / `4.15 s`。评估次数明确量化新增成本；wall time 受单次本机噪声影响，不据此声称固定加速或严格比例。
- deterministic xi shard merge 的 Python 测试 `2/2` 通过；workflow YAML 与 Python 语法检查通过。dense-reference builder 在隔离测试模块内复用 `Main.Models`，contract test `11/11` 与 unit core `1476/1476` 通过，未再发生全局 `parse_args` 冲突。
- production/research mode compare `28/28` 通过。两种模式分别从一阶相变边界以上和完整请求温区搜索 crossover，因此定位温度接近但不保证坐标完全相同；回归先约束 `T_crossover`，再对端点附近陡峭的 `rho(T,mu)` 使用 `1%` 相对尺度比较。失败样本诊断为 `Delta T=0.168 MeV`、`Delta rho/rho≈0.67%`，未通过扩大同坐标绝对容差掩盖差异。
- 最终治理检查通过：docs consistency、active docs、script entrypoints、Models entry contract、solver leakage、data output path guard 与 `git diff --check`。旧 `baseline_phase_pipeline_v1.csv` 和 `data/**` 保持不变；本 PR 只新增 v2 内部回归基线，不生成、不导入、不晋升 phase reference，也不启动 transport production。

## 6. 里程碑

- M1：PR 1 合并，热积分策略与低温/零温基础可用。
- M2：PR 2 合并，相图几何网格具有误差控制。
- M3：PR 3 合并，全温区 reference 经作者晋升。
- M4：PR 4 合并，transport production 绑定新 reference。

## 7. Definition of Done

- [ ] 四个 PR 按依赖顺序完成且每个 PR 有独立验证证据。
- [ ] phase thermodynamics 的角度依赖、积分策略和适用范围在代码与文档中一致。
- [ ] 全温区 reference 的主位置量、密度量和诊断量分别通过预先定义的收敛 gate。
- [ ] 旧 reference、旧 transport production 和历史 registry 记录保持不变。
- [ ] 新 transport case 只在作者审核后晋升论文可用状态。

## 8. 风险与回退

- 若逐调用自适应积分破坏 AD/solver 稳定性：保留其作为 validation oracle，production 改用费米面感知、求解内冻结的分段固定节点。
- 若严格 `T=0` 的 Polyakov/热项极限无法在当前 PR 可靠验证：拒绝 `T=0` 输入，把正式 reference 下限设为经收敛验证的正温度，并另开零温任务。
- 若角约化审计发现额外角核：该调用方继续使用张量积路径，不把约化结论外推到 magnetic、transport 或其他模型。

### C2 replay audit facts (2026-07-24)

仓库外审计保留了 C2 source run `30073342572` 与 replay run `30150515573` 的事实：
161/161 数值 shard 成功，level-3 的 80/80 xi 区间均为 `converged=false`，主要原因是
`interpolation_tolerance_exceeded`；rho 层主要原因是 `geometry_tolerance_exceeded`。
按温区统计，rho failure 约为 `T<20: 94.6%`、`20≤T<60: 81.2%`、`60≤T<100: 72.7%`、
`100≤T<140: 62.1%`、`140≤T<180: 3.7%`、`T≥180: 0%`。温度侧主要出现
`midpoint_classification_changed_or_unresolved:valid,unknown,valid`。旧 C2 CEP CSV
仍按历史中点合同生成，不能作为新三态合同的物理收敛证据。

这些事实只说明旧 C2 candidate 的网格/插值 gate 未通过，不改变旧 reference，也不授权
启动 C3/O1 或 formal production。后续 production integration shadow 必须先证明 opt-in
rho-support cascade、memoized dense 与 independent oracle 的几何、solver-work 和
provenance 合同，再从新的 calculation SHA 重放 C0→C1→C2。

### CEP endpoint-local v4 dependency checkpoint (2026-08-06)

Issue #130 的 endpoint-local v4 full shadow 已在独立 calculation
`8fb86255c7894004cb1c52bbb03f9f53a4828411` 上完成：numeric run
`31067984700`，final aggregate replay `31068641378`，verdict 为
`full_hybrid_candidate`。hybrid 与 oracle 在 24 anchors 上三态一致，三个低温
endpoint 证书和成本 gate 均通过；证据包位于
`docs/analysis/pnjl_cep_endpoint_local_production_shadow_v4/`。

该 verdict 仍是 diagnostic candidate，不是 phase-reference 晋升许可。作者审核
代表性曲线、endpoint 区间、Maxwell/geometry 证书和成本之前，不启动新的 C0→C1→C2，
不改写旧 reference，也不启动 C3/O1 或 transport。若审核通过，后续须从该 production
calculation SHA 生成新的串行 C0/C1/C2 artifacts，再另开 phase-reference artifact PR。

### C0 production diagnostic materialization repair checkpoint (2026-08-07)

从 endpoint-local production calculation `8fb86255c7894004cb1c52bbb03f9f53a4828411`
启动的新 C0 run `31103188845` 绑定了 PR #185 merge SHA
`fa390d89c836598e37b9fb75e7f8e7368283df1e`，41 个 shard 均在 `solve_points` 阶段
失败，错误为 `MethodError: no method matching Float64(::Nothing)`。artifact 中 coarse、
cascade、Stage-B 曲线已经写出，说明本次失败位于 production sweep 汇总的可空 geometry
诊断物化，而不是 Maxwell、geometry gate 或 C0 convergence verdict；C1/C2 保持暂停。

当前修复分支 `codex/issue-130-c0-diagnostic-materialization-repair-v2` 只统一
`position_error_MeV`、`density_error` 和 `maxwell_area_gate` 的 summary 归一化：缺失值
使用既有 `Inf` sentinel，并记录缺失字段与 `nothing_to_inf_v1` provenance；嵌套
`rho_convergence_records` 继续保留 JSON `null`。默认 uniform、历史 cascade、hybrid
policy、equilibrium solver、旧 reference/evidence 和 transport 均不变。focused CI 通过
并合并后，才以新的 calculation SHA 重跑 C0；旧 `fa390d89…` run 不复用。

### C0 rho-support prior contract repair checkpoint (2026-08-07)

PR #186 已合并为 `main@d6cfa33edeace5aab2eb69f401ff543611a573c9`，但其后的 C0
`31139870481` 仍有 41/41 个数值 shard 失败。代表 shard 的 T=1 cascade 161 点和
Stage-B 641 点均 finite/converged，失败发生在下一温度的 previous-T prior 选择，而非
solver、Maxwell 或 geometry 数值阶段。

根因是 prior 可空字段过滤条件的 `continue` 逻辑写反：缺少
`cascade_support_center/gap` 时对 `nothing` 执行了 `Float64`，而有效 prior 又被丢弃。
当前 repair 只修复 nullable prior selection、最近温度选择及等距低温 tie-break，并补
focused regression；新 calculation SHA 前不复用 run `31139870481`，C1/C2、reference
和 transport 继续暂停。

### C0 acceptance and convergence telemetry (2026-08-08)

- [x] C0 run `31149826740` is accepted as a diagnostic-only convergence candidate; the
  calculation SHA is frozen at `ffa816df0a145f73d7490db1ed9ff10c92e017a4` and the final
  artifact remains outside the repository. The run completed 57/57 numerical shards;
  boundary, spinodal and crossover curves were finite/converged and had no duplicate keys.
- [x] The C0 artifact audit records 11,397 grid-convergence rows, including 1,650
  geometry/interpolation records that did not converge (rho 1,545, temperature 94, xi 11).
  There were no visible solver-incomplete/no-curve/non-finite failures in the downloaded
  outputs. CEP rows remain ambiguous; ten of 57 brackets are 0.125 MeV, so this is not a
  production-grade/reference artifact.
- [x] The telemetry/postprocess PR `codex/issue-130-c0-acceptance-convergence-telemetry`
  adds a versioned projection of each existing `phase_summary.json`, shard/merge aggregation,
  validator checks and a three-state convergence comparator. Historical artifacts without
  the summary are explicitly `telemetry_unavailable`; missing counters are null/unknown,
  never fabricated as zero. The change does not alter `src/models`, solver semantics or the
  frozen calculation SHA.
- [x] After this postprocess head is merged, run C1 with tag
  `issue130_endpoint_hybrid_c1_20260808_integral_tight` at `rtol=1e-10, atol=1e-12` and
  the frozen calculation SHA. C1 isolates low-temperature thermal-integration drift; it
  does not promise that every geometry record becomes converged.
- [x] Only if C1 has finite/converged curves, no solver/payload failures and no material
  physical drift, run C2 with tag `issue130_endpoint_hybrid_c2_20260808_grid_tight`.
  C2 restores C0 integration controls while tightening rho/T/xi geometry and refinement
  gates. Remaining non-converged records may be geometry/interpolation diagnostics when
  adjacent-stage physical outputs are stable; before C2 passes, do not promote reference or
  start C3/O1/transport.

### C2 comparator replay and bounded Stage-C follow-up (2026-08-09)

- [x] C1/C2 artifacts are complete and solver-free replay inputs. The comparator now treats
  public anchors as `xi=-0.5:0.05:0.5`, `T=1:5:236` plus `240`, compares area residuals,
  and records grid additions separately from classification regressions.
- [x] Replay package `docs/analysis/pnjl/c2_convergence_audit_v1/` records primary
  `classification_regression`: nine public first-order rows are ambiguous in C2, sixteen
  CEP brackets exceed `0.1 MeV`, and physical-mu crossover interpolation leaves a localized
  `xi=0.2875` high-mu risk. These are diagnostic facts, not reference-promotion evidence.
- [ ] Only after the comparator PR is merged, run the limited Stage-C density-certificate
  feasibility workflow at fixed calculation SHA `ffa816df…`; do not start a new C0/C1/C2,
  reference replay, C3/O1 or transport before its `feasible_candidate` gate.

### Stage-C density feasibility replay result (2026-08-09)

- [x] The numerical matrix `31296511813` completed all 30 jobs at calculation SHA
  `ffa816df0a145f73d7490db1ed9ff10c92e017a4`. The first aggregate failure was a postprocess-only
  Matplotlib MathText error; no solver or numerical artifact failed.
- [x] PR #193 merged the plot/replay contract repair as `main@11fbf9a71575ea207ed3233608c2bc2ade1ee346`.
  Replay `31300285869` completed with `solver_called=false`, source job count 30, and stable input
  and figure hashes.
- [x] The solver-free feasibility verdict is `integration_failed`: all three routes fail the state
  gate at cap 12/16/24 with 12 classification mismatches; geometry and cost pass, while seven CEP
  brackets and three `xi=0.2875` crossover-risk rows fail their existing contracts.
- [ ] Keep the package diagnostic-only. Do not create a production ranking PR or promote reference,
  start C3/O1, or bind transport until an author-approved route resolves the classification, CEP and
  crossover blockers.

### Stage-C feasibility contract v2 (2026-08-09)

- [x] Branch `codex/issue-130-stagec-feasibility-contract-v2` adds the solver-free Julia
  production-parity evaluator `scripts/analysis/pnjl_stagec_density_certificate_feasibility_v2.jl`.
  It fixes the source-run contract to numerical run `31296511813` and calculation SHA
  `ffa816df0a145f73d7490db1ed9ff10c92e017a4`; it does not call the equilibrium solver.
- [x] Stage A uses the production-hybrid `0.05 -> 0.025` levels. Stage B uses the full-domain
  independent-oracle `0.0125 -> 0.00625` levels (`rho_level=0`), while Stage C uses only the
  non-overlapping independent-oracle `0.003125` pool (`rho_level=1`); memoized dense is only
  the cost baseline. `rho_level` is authoritative, so targeted rows cannot silently enter a
  base grid.
- [x] The v2 replay records route candidate sequences, component-level geometry as
  `evaluable/pass/unresolved`, complete Stage-A/Stage-B/selected Stage-C union costs, and final
  output hashes. A fixed matplotlib plotter and aggregate-only workflow validate the 30-job
  numerical source matrix and preserve calculation/workflow dual provenance.
- [x] Local solver-free replay is a development self-check only: it returned `feasible_candidate`
  at cap `12` with `7985` hybrid unique keys versus dense baseline `9615`, no classification or
  geometry mismatch, and `solver_called=false`. It is not yet Actions evidence and does not
  authorize production integration.
- [ ] After focused CI, run the v2 aggregate replay against the completed source run and import
  its immutable evidence. Only an Actions `feasible_candidate` may authorize a production PR;
  otherwise keep the v2 package diagnostic-only and do not start new C0/C1/C2, CEP/crossover
  numerical runs, reference promotion, C3/O1, or transport.

### Stage-C feasibility contract v2 Actions and production integration (2026-08-09)

- [x] PR #194 merged at workflow head
  `72c49c26a061cac7b71e66543ebdfd2c91ec3fca`; aggregate replay `31308813742` passed
  source artifact/hash validation, solver-free Julia replay, plots, and evidence validation.
- [x] Immutable Actions evidence reports `feasible_candidate` with
  `stage_b_features_v1`, cap `12`, `7985` hybrid unique solves versus `9615` dense, zero
  classification/geometry/finite/candidate/cost failures over 15 anchors, and
  `solver_called=false`. The source run's `failure` conclusion remains provenance only.
- [x] Evidence was downloaded outside the repository at
  `D:\Desktop\Julia_RelaxTime_issue130_artifacts\stagec_feasibility_v2_actions_31308813742`;
  v1 evidence remains immutable.
- [ ] Production branch `codex/issue-130-stagec-density-production-v2` is implementing the
  selected policy verbatim. Focused CI and all numerical targeted/CEP/crossover/full shadow
  runs remain pending; no new C0/C1/C2, reference, C3/O1, or transport is authorized yet.

### Endpoint-local v2 full shadow candidate (2026-08-10)

- [x] The Stage-C feasibility v2 cap-12 candidate was accepted by Actions, and the production
  calculation SHA is frozen at `4c9703c3be45b76608ab57d375082e29418bfd05`.
- [x] Targeted numerical run `31350859850`, approved deep-oracle run `31350127588`, and deep-overlay
  replay `31351459964` completed successfully. The subsequent full numerical shadow `31351527152`
  completed all 9 jobs; aggregate replay `31352083775` is final with `24/24` anchors covered and
  verdict `full_hybrid_candidate`.
- [x] The full replay reports `12,845` hybrid unique solves versus `15,384` memoized-dense solves;
  runner times are `289.68 s` and `287.70 s` respectively, within the allowed 10% wall-time noise,
  with zero fallback/retry and no classification, geometry, endpoint, oracle, coverage or performance
  errors. The evidence package is isolated at
  `docs/analysis/pnjl/cep_maxwell_endpoint_local_production_shadow_v4_full_31352083775/`.
- [ ] This remains a diagnostic candidate pending author review of representative curves, Maxwell
  candidates, endpoint certificates and cost. Do not promote a phase reference or start C0/C1/C2,
  C3/O1 or transport from this automatic gate alone.

### C2 limited feasibility and cost stop (2026-08-12)

- [x] The limited-feasibility boundary is fixed at calculation SHA
  `4c9703c3be45b76608ab57d375082e29418bfd05`; C0/C1/C2 artifacts and the full-shadow evidence
  remain immutable. No solver, Maxwell, production policy, tolerance, reference or transport
  semantics are changed by this diagnostic workflow.
- [x] Added the versioned numerical fine-pool job, solver-free evaluator/collector and workflow
  `.github/workflows/pnjl-c2-limited-feasibility.yml`. The first runnable scope is `density`, with
  ten xi shards, the fixed fifteen-anchor matrix, one `0.00625 -> 0.003125` nested pool per xi,
  and route/cap comparison for `stage_b_features_v1`, `balanced_density_features_v2` and
  `geometry_feedback_v2` at caps `12/16/24`.
- [x] Source artifacts record both calculation/postprocess SHA, input hashes, solver telemetry,
  cache accounting and `solver_called=true`; aggregate output is solver-free and records
  `solver_called=false`. A 150 runner-minute cost stop and failed-only rerun contract prevent
  an incomplete or over-budget matrix from producing a feasibility verdict.
- [x] CEP and crossover scope plans are implemented but not numerically dispatched. CEP consumes
  only the frozen 17-bracket audit and permits at most 18 new slices; crossover fixes
  `xi=0.2875`, five exact mu values and nested diagnostic levels `20/40/80`.
- [ ] After focused CI and merge, dispatch only the density numerical scope with tag
  `issue130_c2_density_feasibility_20260812`. Download its immutable evidence to
  `D:\Desktop\Julia_RelaxTime_issue130_artifacts\c2_limited_feasibility_20260812\density`;
  stop after the density verdict and request authorization before CEP/crossover. Even a
  `density_feasible_candidate` is limited evidence and does not authorize C2 rerun, reference
  promotion, C3/O1 or transport.
