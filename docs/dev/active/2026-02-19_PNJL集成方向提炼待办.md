# PNJL 集成方向提炼待办（由历史灵感文档整理）

## 目标

将历史自由记录中的有效想法提炼为可执行事项，避免信息散落且无法闭环。

## 待办（可勾选）

### A. 服务与任务执行模型

- [ ] 设计 PNJL 扫描为“长任务 + 结果落盘 + 状态查询”模式，避免阻塞在线接口。
- [ ] 定义单点求解轻量接口（优先支持小负载快速返回）。
- [ ] 明确未来 ξ 扫描的 payload 与作业队列策略。

### B. 参数与配置统一

- [ ] 统一 PNJL 参数读取入口（TOML loader + profile 选择）。
- [ ] 评估 `config/` 下 profile 复用策略，减少重复配置漂移。
- [ ] 为关键参数增加校验与日志输出（便于回溯异常结果）。

### C. 数据资产分层

- [ ] 固化 `data/raw` / `data/processed` / `data/outputs` 的使用边界。
- [ ] 形成离线种子与在线输出的命名规则，避免结果污染。
- [ ] 给典型脚本补充“输入来源与输出路径”说明。

### D. 数值策略与扫描范围

- [ ] 评估稀疏采样与插值策略，形成最小可用方案（先保证稳健再扩展密度）。
- [ ] 结合现有数值文档补充推荐采样模板与禁用区间。
- [ ] 对关键扫描脚本增加最小固定点回归。

### E. PNJL 功能路线补齐

- [ ] 整理 T-μ 扫描与 T-ρ 扫描的统一输出契约。
- [ ] 梳理涨落计算的自动微分接入路径（含隐式求导边界）。
- [ ] 收敛“各向异性 PNJL + 相图产出”到可复现实验模板。

## DoD

- [ ] 每个大项至少落地 1 个脚本/文档变更。
- [ ] 与 `README` / `STATUS` / 对应 API 文档保持口径一致。
- [ ] 完成后归档本任务单并附执行证据链接。
