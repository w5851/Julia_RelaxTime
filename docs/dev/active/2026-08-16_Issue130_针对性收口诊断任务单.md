# Issue #130：针对性收口诊断任务单

状态：执行中；父任务为 `issue130-full-hybrid-author-review`。本任务只生成
solver/Maxwell/geometry 的诊断 overlay，不替代父任务的作者审核，也不晋升 phase
reference。三个固定 CEP 切片的 T 方向 bracket 已通过独立人工审核 overlay 收口；
当前不再重复运行原 `cep_brackets` 扫描。

## 范围与固定输入

- calculation SHA 固定为 `3c5f6b3c9bd535cff7657364dadb2efc31f2ea48`。
- workflow/postprocess 使用合并后的 head；所有数值 job 记录 calculation SHA、
  postprocess SHA、source run、输入 hash、cache/solver telemetry 和 `solver_called`。
- 历史 C0/C1/C2、full-shadow、reference 和 transport 产物保持不可变。

## Scope

- `regression_curves`：9 个 C2 分类回归点，保存 hybrid 与 independent fine-grid
  oracle 的完整 raw `rho-mu` 曲线、Maxwell candidate、geometry 和成本。
- `cep_brackets`：历史范围为 C2 失败的 3 个 bracket midpoint；该 T 方向诊断已由
  `cep_manual_bisection_audit_v2_31999149922` 及 `xi=0.5` 高侧扩展
  `32014476047` 完成并记录，不再作为待运行 scope。中点仍不升级为单值 CEP。
- `cross_axis_audit`：只读输入 artifact；缺少完整原始 geometry 表时返回
  `cross_axis_input_missing`，禁止把缺失输入解释为通过。

## 不变合同

- route selection 不读取 oracle 标签；oracle 只在 aggregate overlay 阶段做事后 gate。
- endpoint policy 为 `three_crossing_endpoint_local_v2`，candidate policy 为
  `unique_three_crossing_sign_change_v2`；不放宽既有 geometry/Maxwell 容差。
- aggregate 只写独立 evidence artifact，不写 `data/reference/**`。

## 当前下一步

- 保留 T 方向 CEP overlay 为不可变诊断证据：
  `xi=0.125`、`0.39375`、`0.5` 的 bracket 均为 `0.0625 MeV`，状态为
  `closed_by_author_review`。
- 不重复 dispatch `scope=cep_brackets`；下一项是独立的 μ 方向
  endpoint-local refinement，先做 solver-free target-list/cost preflight，再决定是否
  运行 numerical pilot。
- μ refinement 的目标是填补原生 crossover 采样与 CEP proxy 之间的 gap，不改写 C2
  artifact，也不把 `mu_q>mu_CEP` 的响应峰作为 crossover 锚点。

## 验收顺序

- [x] runner、collector、versioned workflow、manifest/claim ledger 和 focused tests。
- [ ] focused CI 与治理检查通过，创建 ready PR。
- [ ] 合并后只 dispatch `scope=regression_curves` numerical。
- [ ] 下载并审核 9 个 raw 曲线、candidate/geometry overlay 和成本；该审核与已完成的
  T 方向 CEP overlay 分开记录。

## 停止条件

solver failure、非有限/重复 key、provenance/hash 不一致或 aggregate input 缺失时停止；
任何 targeted verdict 均不自动触发 C0/C1/C2、phase-reference、C3/O1 或 transport；
μ endpoint refinement 需要单独的 solver-free preflight 和作者授权。

## T 方向 CEP overlay（已完成）

证据位于仓库外不可变目录
`D:\Desktop\Julia_RelaxTime_issue130_artifacts\cep_manual_bisection_audit_v2_31999149922`。
`author_decision_overlay_v2.json` 的 `author_review_complete_for_fixed_slices=true`，
三个 bracket 均为 `0.0625 MeV`，`remaining_manual_decisions=0`；原始 C2、reference
和 transport 均未修改。`xi=0.5` 的高侧无 S 形判断来自
`cep_xi05_high_side_extension_v1` 的 aggregate/local-zoom evidence
（run `32011954899` / `32014476047`）。
