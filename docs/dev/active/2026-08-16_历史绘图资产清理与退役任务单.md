# 历史绘图资产清理与退役任务单

创建日期：2026-08-16

状态：PR A 已合并；PR B 已完成作者批准的 allowlist 操作并补充 P1 Mott namespace 整理，当前进入验证、选择性暂存和 PR 更新阶段

## 1. 目标

在不改变数值语义、不覆盖正式产物、不删除历史证据的前提下，建立 PNG/PDF/SVG 资产 registry 和清理候选报告，为后续逐项退役提供可审计输入。

本任务的术语是 `asset inventory + dry-run + allowlist cleanup`。历史资产真正退出正式工作树时，另称为 `figure asset retirement`，不得与本 PR A 混在一起。

## 2. PR A 范围

- 扫描 `data/outputs/figures` 中 Git 已跟踪的 PNG、PDF、SVG。
- 记录文件 hash、大小、格式元数据、manifest 关联、provenance sidecar 和 Git tracked 状态。
- 按 case/variant 聚合资产，生成 `asset_registry.json` 和 `cleanup_candidates.csv`。
- 将历史/未注册 manifest、多个 variant、外部引用未知项标为 `owner_review_only`。
- 记录当前未跟踪资产被排除且不修改的规则。

## 3. 非范围

- 不删除、移动、重命名或覆盖任何 PNG/PDF/SVG。
- 不扫描或处理当前未跟踪的 C1/C2/pilot 资产，不把它们纳入可删除候选。
- 不修改 CSV/JSON 数值输入、solver、Maxwell、C2、reference、transport 或绘图数值语义。
- 不根据文件名、时间、格式或单纯 byte hash 自动判定历史资产可以删除。
- 不把 `docs/analysis` 诊断证据自动迁移到正式图目录。

## 4. 分类和动作语义

| 分类 | PR A 动作 | 后续默认策略 |
| --- | --- | --- |
| `contract_case` | 记录 | 保留，除非作者确认存在冗余 variant |
| `legacy_manifest` | `owner_review_only` | 默认保留为 `legacy` |
| `legacy_unregistered` | `owner_review_only` | 先确认论文/报告引用，再决定是否归档 |
| `invalid_manifest` | `owner_review_only` | 先修复或保留证据，不直接删除 |
| `diagnostic` | 不进入本 registry | 继续保留在 `docs/analysis` |
| `pilot_review` | 不进入 tracked-only registry | 保留当前工作树，等待作者另行决定 |

PR A 的 `proposed_action` 不允许取 `delete` 或 `move`。任何实际退役必须在后续 PR 中提供路径、SHA-256、理由、替代路径和作者批准。

## 5. 作者人工审核 gate

作者只需审核 registry 的 `review_groups`，不需要逐个打开全部图像。每个审核组需要回答：

1. 是否仍被当前论文、supplement、报告或外部幻灯片引用；
2. 多个 case/variant 中哪个是 canonical；
3. 历史版本是继续原地保留、进入可恢复 quarantine，还是列入后续删除候选。

仓库外引用无法由脚本可靠判断，因此没有作者确认的资产默认继续保留。

## 6. 后续 PR B 入口

PR B 可以先以 dry-run/preflight 形式建立；只有作者完成人工审核后，才执行实际退役动作。执行 PR B 必须：

- 使用作者批准的精确 allowlist；
- 在执行前再次校验路径和 SHA-256；
- 优先使用可恢复归档，不直接 `Remove-Item`；
- 记录 relocation/deletion manifest；
- 检查仓库引用、manifest 输出和文档链接没有断裂；
- 不处理未经明确批准的 C1/C2/pilot 或正式历史证据。

## 7. 验收

- registry 可由 `scripts/plotting/inventory_figure_assets.py` 在 clean checkout 重建；
- dry-run 报告明确写出 `delete_performed=false`、`move_performed=false`；
- 所有资产均有 SHA-256，PNG/PDF/SVG 的基本格式元数据可检查；
- `cleanup_candidates.csv` 只包含人工审核项，不包含删除命令或模糊 glob；
- Python inventory 单元测试、plotting contract 测试和仓库治理检查通过。

## 8. 作者审核完成记录

作者逐组审核了 registry 的 82 个 review group。完整决定记录见：

docs/analysis/governance/figure_asset_registry_v1/author_review_decisions.md

本次人工审核已经覆盖：

- 保留为 legacy 的早期低精度图；
- 论文正式图或候选正式图；
- 迁入 docs/analysis 的文献/误差/诊断图；
- coarse xi 网格 case 的 xi005 重命名；
- dense transport v1/v2 的语义差异和保留策略；
- 明确删除的 case 目录和整棵 xi_smoothness_sampling 目录。

其中 v2 与 v1 的 transport 语义比较说明分别记录在两个 v2 结果目录的 COMPARISON_TO_V1.md。该说明不修改 README、manifest、CSV 或图像 hash。

截至本记录：

- delete_performed=false；
- move_performed=false；
- rename_performed=false；
- C1/C2/pilot 未跟踪资产未处理；
- PR B 的删除、迁移和重命名 allowlist 已具备作者批准依据，但尚未执行。

## 9. PR B 执行建议和人工介入点

PR B 进入实际文件操作前，先生成一次新的 preflight manifest。该 manifest 只允许来自作者审核记录的精确路径，并逐项记录：绝对路径解析结果、Git tracked 状态、当前 SHA-256、计划动作、理由、替代路径或目标路径。若路径不存在、状态不是预期值、SHA-256 与 registry 快照不一致，或命中未跟踪 C1/C2/pilot 资产，应停止执行并重新审核；不得用宽泛 glob 补偿。

执行顺序建议如下：

1. 对保留项只做分类和必要的 `legacy` 说明，不重绘、不覆盖、不修改数值文件。
2. 对 analysis 迁移项用显式路径移动图像证据，保留结果 CSV 和 provenance，并在目标目录补 README；迁移后记录源/目标清单与 hash。
3. 对删除项只按 allowlist 处理，先记录删除前 hash 和仓库引用，再提交精确删除；不直接使用未验证的递归删除命令。
4. 对 coarse transport case 使用显式 `git mv` 完成 `xi005` 改名，只更新路径型元数据和引用，不改 CSV 内容或 transport 数值语义。
5. 执行后重新扫描引用、manifest、文件 hash 和 Git diff，确认 solver、Maxwell、C2、reference、transport 数值文件及未跟踪 pilot 均未被改动。

作者已完成 PR B 的一次性操作性审核：执行前 preflight manifest 与本记录的 allowlist/hash 一致，analysis 迁移目标的 README 也已审核通过。无需重新逐个审核已经确认保留的 legacy PNG。若发现新的论文/补充材料/报告外部引用，PR B 应暂停并回到对应 review group，而不是自行改变动作。

## 10. PR A 合并前核验记录

2026-08-16 完成本轮记录复核：

- `check_docs_consistency.jl`：通过；
- `check_active_docs_governance.jl`：通过；
- `check_sop_governance.jl`：通过；
- `git diff --cached --check`：通过；
- 提交 `8de77c0f` 已推送到 `codex/figure-asset-inventory`，当时 PR A 尚未执行任何删除、迁移或重命名；
- 当前工作树中的未跟踪 C1/C2/pilot 资产未被暂存或修改。

## 11. PR A 收尾与 PR B 启动记录（执行前快照）

2026-08-16 完成以下操作：

- PR A #220 `docs: add historical figure asset inventory and dry-run` 已 squash 合并；远端合并提交为 `49d87b2c`。
- PR A 的 8 项 CI 检查全部通过；旧分支 `codex/figure-asset-inventory` 已删除。
- PR B 分支 `codex/figure-asset-retirement` 初始从合并提交 `49d87b2c` 建立，随后已同步到最新 `origin/main` 的 `2462dd27`。
- `scripts/plotting/build_figure_cleanup_preflight.py` 生成 `docs/analysis/governance/figure_asset_registry_v1/cleanup_preflight_v1.json`。当前预检摘要为：1 个显式文件删除、7 棵 tracked 目录树删除（367 个成员）、9 个文件迁移、4 棵 coarse transport 目录树改名（66 个成员）、37 个路径引用候选、19 个未跟踪资产根目录排除。
- 预检记录 `delete_performed=false`、`move_performed=false`、`rename_performed=false`，并明确 `manual_review_required=true`。freezeout 下四个未跟踪 strict/pilot 变体不在 PR B allowlist 内。
- 本 PR 新增三个 analysis 迁移目标的 README，说明输入、用途和 provenance 边界；实际 `git rm`、`git mv`、递归删除和数值文件修改均未执行。
- 用户工作树中未跟踪的 C1/C2/pilot 文件及 `tmp/` 保持未暂存、未修改。

以上为 PR B 执行前快照。作者随后确认审核通过，并确认 freeze-out manifest 采用“只更新路径字段、记录旧/新 hash”的处理方式；实际执行结果见下一节。

## 12. PR B 实际执行记录

2026-08-16，作者审核通过后已在 `codex/figure-asset-retirement` 执行精确 allowlist。执行结果由以下 manifest 记录：

- `docs/analysis/governance/figure_asset_registry_v1/retirement_execution_v1.json`；
- 生成器：`scripts/plotting/build_figure_retirement_manifest.py`；
- preflight 输入：`docs/analysis/governance/figure_asset_registry_v1/cleanup_preflight_v1.json`。

执行摘要：

- 删除 1 个显式文件和 7 棵目录树，共 368 个删除条目；
- 迁移 9 个 analysis 图像/manifest 文件；8 个 PNG 保持字节级 hash 不变；
- freeze-out `plot_manifest.json` 仅更新两个图像路径字段，并记录迁移前后 hash；
- 4 棵 coarse transport 结果/图像树改名，共 66 个条目；
- 37 个旧 case slug 引用通过精确 token 替换更新，未重算数值；
- 未跟踪 C1/C2/pilot 资产和 `tmp/` 保持未处理；
- solver、Maxwell、C2、reference、transport 数值语义和 CSV 数值内容未改动。

当前尚未完成的是 PR B 的最终选择性暂存、治理检查、提交和远端 PR 更新；在这些检查完成前，本任务保持 active，不归档。

## 13. P1 Mott namespace 整理补充

在 PR B 执行后复核正式图像目录时，发现 `paper_p1_mott_phase_isentropic` 与 `mott_phase` 的顶层命名空间存在语义重复。两者不是图像或数值副本：前者包含 P1 Mott/isentropic production case，后者包含其他 Mott/meson 图族 case。

2026-08-16 已在本 PR 内完成以下窄范围整理：

- 将 `data/outputs/figures/relaxtime/paper_p1_mott_phase_isentropic/production_20260531/` 移至 `data/outputs/figures/relaxtime/mott_phase/paper_p1_mott_phase_isentropic/production_20260531/`；
- 更新目标 `plot_manifest.json` 的 5 个图像路径字段；5 张 PNG 保持字节级 SHA-256 不变；
- 将 `scripts/relaxtime/run_paper_p1_pipeline.jl` 的默认图像输出切换到新的 Mott namespace，并同步更新 P1 脚本说明；
- 不移动 `data/outputs/results/relaxtime/paper_p1_mott_phase_isentropic/`、`data/reference/pnjl/paper_p1_mott_phase_isentropic_20260531/` 或任何 CSV/JSON 数值产物；
- 迁移前后的路径和 hash 由 `docs/analysis/governance/figure_asset_registry_v1/relocation_execution_v1.json` 记录，清理 preflight/retirement 快照保持不可变。

本补充不重跑 PNJL，不改 solver、Maxwell、C2、reference 或 transport 数值语义。
