# 统一扫描入口兼容退场映射表（Wave-E）

## 目标

完成扫描脚本兼容退场，仅保留统一治理层单一路径：

- 统一脚本入口（Wave-E）：`scripts/models/run_unified_scan.jl`
- 统一模型入口：`Models.run_tmu_scan` / `Models.run_trho_scan` / phase-workflow entrypoints

## 模型能力矩阵（Wave-E 目标）

状态字段标准枚举：`pending` / `active` / `hard_deprecated` / `removed` / `archived` / `blocked`

| model_kind | T-mu 扫描 | T-rho 扫描 | phase/workflow | 备注 |
|---|---|---|---|---|
| `PNJL` | pending | pending | pending | 基线模型 |
| `NJL` | pending | pending | pending | 与 PNJL 同范式接口 |
| `RPNJL` | pending | pending | pending | 已在部分脚本/入口支持 |
| `PNJLMagnetic` | pending | pending | pending | 需统一进扫描入口能力矩阵 |
| `Rotation` | pending | pending | pending | 需统一进扫描入口能力矩阵 |
| `GasLiquid` | pending | pending | pending | 需统一进扫描入口能力矩阵 |
| `pnjl_aniso` | pending | pending | pending | 作为 `PNJL` 参数化模式（`xi/profile`），非独立 `model_kind` |

## 兼容退场映射

| 旧路径 | 统一路径（Wave-E） | Wave-E 目标状态 | 删除/归档门槛 | 风险与回退 |
|---|---|---|---|---|
| `scripts/pnjl/run_tmu_scan.jl` | `scripts/models/run_unified_scan.jl scan tmu --model-kind ...` | pending (removed/archived) | 命令级 parity + smoke/regression + docs/api 同步 | 若外部调用未清零，短期 hard-deprecated 并保留迁移提示 |
| `scripts/pnjl/run_dense_trho_scan.jl` | `scripts/models/run_unified_scan.jl scan trho --profile dense ...` | pending (removed/archived) | 调用方迁移完成 + 回归稳定 | 保留短期别名并记录 blocked 原因 |
| `scripts/pnjl/run_adaptive_trho_scan.jl` | `scripts/models/run_unified_scan.jl scan trho --profile adaptive ...` | pending (removed/archived) | workflow 无脚本分叉依赖 + smoke/regression 通过 | 必要时保留 thin wrapper 一个发布周期 |

## Wave-E 约束

- compat 退场必须有阈值证据，不允许无证据强删。
- 统一入口必须覆盖当前实现模型族。
- `pnjl_aniso` 必须按参数化模式处理，禁止引入新的并行脚本分叉。

## 验证建议

- `julia --project=. -e 'include("tests/integration/models/test_wavee_unified_scan_cli_smoke.jl")'`
- `julia --project=. -e 'include("tests/regression/pnjl/test_wavee_unified_scan_stability.jl")'`
- `julia --project=. -e 'ENV["UNIT_PROFILE"]="smoke"; include("tests/unit/runtests.jl")'`
- `julia --project=. -e 'ENV["INTEGRATION_PROFILE"]="smoke"; include("tests/integration/runtests.jl")'`
- `julia --project=. -e 'ENV["REGRESSION_PROFILE"]="smoke"; include("tests/regression/runtests.jl")'`
- `julia --project=. scripts/dev/check_unit_skip_policy.jl`
- `julia --project=. scripts/dev/check_docs_consistency.jl`
- `julia --project=. scripts/dev/check_active_docs_governance.jl`

## PR #45 收尾前置门（Wave-E 启动约束）

- PR: `https://github.com/w5851/Julia_RelaxTime/pull/45`
- 必须满足：`state=OPEN -> MERGED` 后方可执行 E3 破坏性退场动作。

## 执行记录

- [x] 2026-04-01：创建 Wave-E 统一扫描入口兼容退场映射表草案，作为 E2/E3 的状态管理与审计基线。
