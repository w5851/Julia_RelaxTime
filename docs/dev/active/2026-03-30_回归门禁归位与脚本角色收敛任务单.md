# 回归门禁归位与脚本角色收敛任务单

## 1. 目标

- 明确回归真值断言以 `tests/regression/` 为唯一主入口。
- 将 `scripts/*run_*_regression.jl` 角色收敛为“便捷运行/导出报告”，避免双份断言。

决议更新（2026-03-30）：

- 不再保留 `scripts/*run_*_regression.jl` 作为长期入口。
- 回归门禁脚本能力统一迁入 `tests/regression/`，并按专题波次完成迁移与删除。

## 2. 范围

- [ ] 盘点现有 regression 脚本与 `tests/regression` 的断言重复点。
- [ ] 将关键断言迁移或对齐到 `tests/regression`。
- [ ] 脚本保留运行封装，但不再承载唯一门禁判定。
- [ ] 更新文档说明与调用建议。

## 3. 实施步骤

- [ ] R1 建立“脚本 vs 测试”责任矩阵。
- [ ] R2 补齐 `tests/regression` 缺失断言（含失败反例）。
- [ ] R3 调整脚本文案，声明其非唯一门禁来源。
- [ ] R4 在 CI 路径中显式使用 `tests/regression/runtests.jl`。
- [ ] R5 删除 `scripts/` 下对应 regression 入口并回填迁移映射。

## 4. 验证

- [ ] `julia --project=. -e 'ENV["REGRESSION_PROFILE"]="smoke"; include("tests/regression/runtests.jl")'`
- [ ] `julia --project=. -e 'ENV["REGRESSION_PROFILE"]="full"; include("tests/regression/runtests.jl")'`
- [ ] 典型 regression 包装脚本可运行且输出与测试断言口径一致。

## 5. 对接关系

- 上游：`docs/dev/active/2026-03-30_求解器多初解竞争与约束选优任务单.md`
- 下游：`docs/dev/active/2026-03-30_专题脚本单入口point-scan收敛任务单.md`
- 并行对接：`docs/dev/active/2026-03-30_稳定脚本前端化与CI手动触发任务单.md`（Batch-D 门禁口径）

## 6. 状态

- [ ] 待启动（建议作为 PR-2 配套内容）
