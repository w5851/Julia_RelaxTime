interface:
  display_name: "Codex Task Harness"
  short_description: "Restore multi-track scope and triage state."
  default_prompt: "Use $codex-task-harness to recover the task ledger, classify new work, and bound a multi-track request."
